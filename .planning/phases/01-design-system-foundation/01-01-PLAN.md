---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - astro.config.mjs
  - tsconfig.json
  - src/styles/global.css
  - src/layouts/Layout.astro
  - src/pages/index.astro
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - components.json
  - .prettierrc
  - .node-version
autonomous: true
requirements:
  - FNDN-01

must_haves:
  truths:
    - "Running pnpm dev starts the Astro dev server without errors"
    - "React islands render in the browser with client:load directive"
    - "Tailwind 4 utility classes apply styling correctly"
    - "shadcn/ui Button component renders with default styling"
    - "DM Sans font loads via Astro 6 Fonts API"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro 6 config with React integration, Tailwind 4 vite plugin, DM Sans font"
      contains: "fontProviders"
    - path: "src/styles/global.css"
      provides: "Tailwind 4 entry with @import tailwindcss and placeholder theme"
      contains: "@import"
    - path: "src/layouts/Layout.astro"
      provides: "Base layout with Font component and global CSS import"
      contains: "Font"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
      contains: "Button"
    - path: "components.json"
      provides: "shadcn/ui configuration for Astro"
      contains: "astro"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "Vite plugin registration"
      pattern: "tailwindcss"
    - from: "src/layouts/Layout.astro"
      to: "src/styles/global.css"
      via: "CSS import"
      pattern: "import.*global\\.css"
---

<objective>
Scaffold the Astro 6 project with React islands, Tailwind CSS 4, shadcn/ui, and DM Sans font.

Purpose: Establish the working project foundation that all subsequent plans build on. This must produce a running dev server with all integrations wired correctly.
Output: A fully scaffolded Astro 6 project with React, Tailwind 4, shadcn/ui baseline components, and DM Sans font loading.
</objective>

<execution_context>
@/home/smana/.claude/get-shit-done/workflows/execute-plan.md
@/home/smana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 6 project with React + Tailwind 4 + DM Sans</name>
  <files>
    package.json
    pnpm-lock.yaml
    astro.config.mjs
    tsconfig.json
    src/styles/global.css
    src/layouts/Layout.astro
    src/pages/index.astro
    .prettierrc
    .node-version
  </files>
  <action>
    1. Initialize the Astro 6 project in the current repo root (which already has src/assets/logos). Use `pnpm create astro@latest . -- --template minimal --install` to scaffold in-place. If prompted about existing files, keep them.

    2. Add React integration: `pnpm astro add react`. This installs @astrojs/react, react, react-dom.

    3. Install Tailwind CSS 4 as a Vite plugin (NOT @astrojs/tailwind which is the legacy v3 integration):
       ```
       pnpm add tailwindcss @tailwindcss/vite
       ```

    4. Configure astro.config.mjs with React integration, Tailwind 4 vite plugin, and DM Sans font:
       ```javascript
       import { defineConfig, fontProviders } from "astro/config";
       import react from "@astrojs/react";
       import tailwindcss from "@tailwindcss/vite";

       export default defineConfig({
         integrations: [react()],
         vite: {
           plugins: [tailwindcss()],
         },
         fonts: [
           {
             provider: fontProviders.google(),
             name: "DM Sans",
             cssVariable: "--font-dm-sans",
             weights: [400, 500, 600, 700],
             styles: ["normal"],
             fallbacks: ["sans-serif"],
           },
         ],
       });
       ```

    5. Create src/styles/global.css with Tailwind 4 import and placeholder theme tokens. Use the shadcn/ui Tailwind v4 pattern with OKLCH colors. For now, use placeholder values clearly marked -- these will be replaced in Plan 03 after DESIGN.md is approved:
       ```css
       @import "tailwindcss";
       @import "tw-animate-css";

       /* Placeholder tokens -- will be replaced with DESIGN.md values in Plan 03 */
       :root {
         --background: oklch(0.145 0.015 285);
         --foreground: oklch(0.985 0.002 285);
         --primary: oklch(0.55 0.2 260);
         --primary-foreground: oklch(0.985 0.002 285);
         --secondary: oklch(0.25 0.015 285);
         --secondary-foreground: oklch(0.985 0.002 285);
         --accent: oklch(0.75 0.15 45);
         --accent-foreground: oklch(0.145 0.015 285);
         --muted: oklch(0.25 0.01 285);
         --muted-foreground: oklch(0.65 0.01 285);
         --card: oklch(0.185 0.015 285);
         --card-foreground: oklch(0.985 0.002 285);
         --popover: oklch(0.185 0.015 285);
         --popover-foreground: oklch(0.985 0.002 285);
         --border: oklch(0.3 0.01 285);
         --input: oklch(0.3 0.01 285);
         --ring: oklch(0.55 0.2 260);
         --destructive: oklch(0.55 0.2 25);
         --radius: 0.5rem;
         --sidebar-background: oklch(0.145 0.015 285);
         --sidebar-foreground: oklch(0.985 0.002 285);
         --sidebar-primary: oklch(0.55 0.2 260);
         --sidebar-primary-foreground: oklch(0.985 0.002 285);
         --sidebar-accent: oklch(0.25 0.015 285);
         --sidebar-accent-foreground: oklch(0.985 0.002 285);
         --sidebar-border: oklch(0.3 0.01 285);
         --sidebar-ring: oklch(0.55 0.2 260);
       }

       @theme inline {
         --color-background: var(--background);
         --color-foreground: var(--foreground);
         --color-primary: var(--primary);
         --color-primary-foreground: var(--primary-foreground);
         --color-secondary: var(--secondary);
         --color-secondary-foreground: var(--secondary-foreground);
         --color-accent: var(--accent);
         --color-accent-foreground: var(--accent-foreground);
         --color-muted: var(--muted);
         --color-muted-foreground: var(--muted-foreground);
         --color-card: var(--card);
         --color-card-foreground: var(--card-foreground);
         --color-popover: var(--popover);
         --color-popover-foreground: var(--popover-foreground);
         --color-border: var(--border);
         --color-input: var(--input);
         --color-ring: var(--ring);
         --color-destructive: var(--destructive);
         --radius-sm: calc(var(--radius) - 4px);
         --radius-md: calc(var(--radius) - 2px);
         --radius-lg: var(--radius);
         --radius-xl: calc(var(--radius) + 4px);
         --font-sans: var(--font-dm-sans), ui-sans-serif, system-ui, sans-serif;
       }
       ```

    6. Install tw-animate-css (replaces deprecated tailwindcss-animate): `pnpm add tw-animate-css`

    7. Update tsconfig.json to extend astro/tsconfigs/strict with path aliases:
       ```json
       {
         "extends": "astro/tsconfigs/strict",
         "include": [".astro/types.d.ts", "**/*"],
         "exclude": ["dist"],
         "compilerOptions": {
           "jsx": "react-jsx",
           "jsxImportSource": "react",
           "baseUrl": ".",
           "paths": {
             "@/*": ["./src/*"]
           }
         }
       }
       ```

    8. Create src/layouts/Layout.astro as the base layout:
       ```astro
       ---
       import { Font } from "astro:assets";
       import "../styles/global.css";

       interface Props {
         title?: string;
       }

       const { title = "Cloud Native Days France 2027" } = Astro.props;
       ---
       <html lang="fr" class="dark">
         <head>
           <meta charset="utf-8" />
           <meta name="viewport" content="width=device-width, initial-scale=1" />
           <title>{title}</title>
           <Font cssVariable="--font-dm-sans" preload />
         </head>
         <body class="bg-background text-foreground font-sans antialiased min-h-screen">
           <slot />
         </body>
       </html>
       ```

    9. Create a minimal src/pages/index.astro that uses the Layout and confirms all integrations work:
       ```astro
       ---
       import Layout from "../layouts/Layout.astro";
       ---
       <Layout>
         <main class="flex items-center justify-center min-h-screen">
           <h1 class="text-4xl font-bold text-primary">Cloud Native Days France</h1>
         </main>
       </Layout>
       ```

    10. Create .node-version with `22` to document the Node requirement.

    11. Install prettier with Astro and Tailwind plugins:
        ```
        pnpm add -D prettier prettier-plugin-astro prettier-plugin-tailwindcss
        ```
        Create .prettierrc:
        ```json
        {
          "plugins": ["prettier-plugin-astro", "prettier-plugin-tailwindcss"],
          "overrides": [{ "files": "*.astro", "options": { "parser": "astro" } }]
        }
        ```

    12. Update .gitignore to include standard Astro ignores: node_modules, dist, .astro, .env.

    ANTI-PATTERNS TO AVOID:
    - Do NOT create tailwind.config.mjs -- Tailwind 4 uses CSS @theme directive
    - Do NOT use @astrojs/tailwind -- that is the legacy Tailwind 3 integration
    - Do NOT use tailwindcss-animate -- use tw-animate-css instead
    - Do NOT put :root variables inside @layer base -- Tailwind v4 requires them outside layers
  </action>
  <verify>
    Run `pnpm dev` and confirm the server starts without errors.
    Run `pnpm build` and confirm production build succeeds.
    Verify node_modules contains astro, react, tailwindcss, @tailwindcss/vite.
    Verify no tailwind.config.mjs exists.
    Verify components.json does NOT exist yet (that comes in Task 2).
  </verify>
  <done>
    Astro 6 dev server starts cleanly, production build succeeds, React integration active, Tailwind 4 connected via Vite plugin, DM Sans font configured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add baseline components</name>
  <files>
    components.json
    src/lib/utils.ts
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/components/ui/separator.tsx
  </files>
  <action>
    1. Initialize shadcn/ui for Astro: `pnpm dlx shadcn@latest init -t astro`
       Follow the prompts or use flags to select:
       - Style: default
       - Base color: neutral (will be overridden by design tokens later)
       - CSS variables: yes
       If the CLI modifies global.css, review the changes and merge with the @theme inline block from Task 1, keeping the structure intact.

    2. Verify components.json was created and contains Astro-specific configuration. The file should reference the correct paths for components (@/components/ui), utils (@/lib/utils), and styles.

    3. Add baseline shadcn/ui components:
       ```
       pnpm dlx shadcn@latest add button card badge separator
       ```
       These provide the foundation components needed for sample page rendering and later phases.

    4. Verify src/lib/utils.ts exists with the cn() utility function (clsx + tailwind-merge).

    5. Verify each component file exists and uses the React 19 pattern (ComponentProps, data-slot) not the deprecated React.forwardRef pattern.

    6. Update the index.astro page to import and render a shadcn Button to confirm the full chain works:
       ```astro
       ---
       import Layout from "../layouts/Layout.astro";
       import { Button } from "@/components/ui/button";
       ---
       <Layout>
         <main class="flex flex-col items-center justify-center min-h-screen gap-6">
           <h1 class="text-4xl font-bold text-primary">Cloud Native Days France</h1>
           <p class="text-muted-foreground">June 3, 2027 — CENTQUATRE-PARIS</p>
           <Button size="lg">Scaffolding works!</Button>
         </main>
       </Layout>
       ```
  </action>
  <verify>
    Run `pnpm dev` -- page renders with styled button.
    Run `pnpm build` -- production build succeeds.
    Verify components.json exists with Astro type.
    Verify src/lib/utils.ts exists with cn() function.
    Verify src/components/ui/button.tsx, card.tsx, badge.tsx, separator.tsx exist.
  </verify>
  <done>
    shadcn/ui initialized for Astro, 4 baseline components installed (button, card, badge, separator), cn() utility available, sample page renders a styled Button confirming the full Astro -> React -> Tailwind 4 -> shadcn chain works end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `pnpm dev` starts without errors and serves the index page
2. The page displays "Cloud Native Days France" in DM Sans font with a styled shadcn Button
3. `pnpm build` completes successfully producing a dist/ folder
4. No tailwind.config.mjs exists (Tailwind 4 CSS-native config only)
5. components.json references Astro type
6. All 4 shadcn/ui components exist in src/components/ui/
</verification>

<success_criteria>
- Astro 6 project runs with `pnpm dev` and builds with `pnpm build` without errors
- React islands render (shadcn Button component visible)
- Tailwind 4 utility classes apply styling (visible in rendered page)
- DM Sans font configured in Astro config with Fonts API
- shadcn/ui initialized with 4 baseline components
- TypeScript configured with @/ path alias
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
