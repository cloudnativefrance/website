---
phase: 01-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/styles/global.css
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/pages/index.astro
  - src/components/patterns/GeoBackground.astro
autonomous: false
requirements:
  - DSGN-05
  - DSGN-06
  - FNDN-05

must_haves:
  truths:
    - "Tailwind theme tokens in global.css match DESIGN.md values exactly"
    - "shadcn/ui components render with the design system colors and typography"
    - "Sample page renders correctly on mobile viewport (375px)"
    - "Sample page renders correctly on tablet viewport (768px)"
    - "Sample page renders correctly on desktop viewport (1280px)"
    - "Geometric background pattern is visible on the sample page"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Design tokens from DESIGN.md wired as CSS custom properties and Tailwind @theme"
      contains: "@theme inline"
    - path: "src/pages/index.astro"
      provides: "Responsive sample page demonstrating all design system components"
      contains: "GeoBackground"
    - path: "src/components/patterns/GeoBackground.astro"
      provides: "Geometric background pattern component"
      contains: "svg"
  key_links:
    - from: "DESIGN.md"
      to: "src/styles/global.css"
      via: "OKLCH values copied from DESIGN.md into :root variables"
      pattern: "oklch"
    - from: "src/styles/global.css"
      to: "src/components/ui/button.tsx"
      via: "CSS custom properties consumed by Tailwind utility classes"
      pattern: "bg-primary|text-primary"
---

<objective>
Wire approved DESIGN.md tokens into Tailwind CSS 4, customize shadcn/ui components to match, create a geometric background pattern, and build a responsive sample page demonstrating the complete design system.

Purpose: Transform the approved visual identity from DESIGN.md into working code. This closes the design-to-code loop and validates that tokens render correctly across all viewport sizes.
Output: Design tokens in global.css matching DESIGN.md, customized shadcn components, geometric background component, responsive sample page.
</objective>

<execution_context>
@/home/smana/.claude/get-shit-done/workflows/execute-plan.md
@/home/smana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md
@DESIGN.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DESIGN.md tokens into Tailwind 4 and create responsive sample page</name>
  <files>
    src/styles/global.css
    src/components/patterns/GeoBackground.astro
    src/pages/index.astro
  </files>
  <action>
    1. Read DESIGN.md and extract all OKLCH color values, typography scale, spacing scale, and border radius values.

    2. Replace ALL placeholder token values in src/styles/global.css :root block with the exact OKLCH values from DESIGN.md. Every single color token must match DESIGN.md precisely -- no approximations.

    3. Update the @theme inline block if DESIGN.md adds any new semantic tokens beyond the placeholder set.

    4. Create src/components/patterns/GeoBackground.astro -- a geometric background pattern component:
       - Implement the pattern style described in DESIGN.md (hex mesh, polygons, circuit lines, or whatever was chosen)
       - Use inline SVG or CSS-generated patterns for zero JS overhead
       - Use design system colors (primary, accent) at low opacity so it serves as a subtle background texture
       - Accept a `class` prop for positioning (e.g., absolute, fixed)
       - The pattern must not compete with foreground content -- keep opacity low (5-15%)
       - Must work on all viewport sizes without visual artifacts

    5. Rewrite src/pages/index.astro as a responsive sample page that demonstrates the full design system:

       The page should showcase:
       - **Hero section**: Large heading "Cloud Native Days France" in font-bold, subheading with date/venue in muted-foreground, geometric background behind it
       - **CTA area**: Primary Button ("Get Your Ticket") and secondary/outline Button ("View Schedule")
       - **Cards section**: 3 cards in a responsive grid (1 col mobile, 3 cols desktop) with Card, CardHeader, CardTitle, CardDescription. Use sample content like "50+ Talks", "1700+ Attendees", "40+ Partners"
       - **Badges section**: A row of Badge components showing sample track names ("Kubernetes", "Platform Engineering", "Observability", "Security")
       - **Separator**: Between sections
       - **Typography showcase**: h1, h2, h3, body text, muted text all visible to verify the type scale

       Layout must be mobile-first:
       - Mobile (< 640px): Single column, full-width cards, centered content, adequate touch targets
       - Tablet (640-1024px): 2-column card grid, more horizontal breathing room
       - Desktop (> 1024px): 3-column card grid, max-width container centered, generous whitespace

       Use Tailwind responsive utilities (sm:, md:, lg:) throughout. Use the existing Layout.astro as the page wrapper.

    6. Customize shadcn/ui component files if DESIGN.md specifies component-level overrides (e.g., specific button border radius, card shadow, badge font weight). Only modify if DESIGN.md is explicit about component styling -- otherwise the CSS variable theming from global.css is sufficient.
  </action>
  <verify>
    Run `pnpm dev` and visually inspect at 375px, 768px, and 1280px viewport widths.
    Run `pnpm build` and confirm zero errors.
    Diff src/styles/global.css :root values against DESIGN.md -- every token must match.
    Verify geometric background renders as a subtle pattern behind the hero section.
    Verify cards stack on mobile, grid on desktop.
  </verify>
  <done>
    All DESIGN.md tokens are wired into global.css, shadcn components render with the approved design system colors, geometric background pattern displays behind the hero section, and the sample page is responsive across mobile/tablet/desktop viewports.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete design system wired into code:
    - DESIGN.md color tokens applied to Tailwind 4 theme
    - shadcn/ui components styled with design system colors
    - Geometric background pattern component
    - Responsive sample page with hero, CTAs, cards, badges, typography
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:4321
    2. Check desktop view: 3-column card grid, geometric background behind hero, correct colors
    3. Resize to tablet (768px): Cards should be in 2 columns
    4. Resize to mobile (375px): Single column, everything stacked
    5. Verify colors match the approved DESIGN.md palette
    6. Verify DM Sans font is rendering (check in browser DevTools > Computed > font-family)
    7. Verify the geometric background is subtle and doesn't interfere with content readability
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Every OKLCH value in global.css :root matches DESIGN.md exactly
2. Sample page renders at 375px, 768px, and 1280px without layout issues
3. Geometric background is visible but subtle
4. All shadcn components (Button, Card, Badge, Separator) render with design system colors
5. DM Sans font loads and renders
6. `pnpm build` succeeds
</verification>

<success_criteria>
- Tailwind theme tokens match DESIGN.md values exactly
- Responsive sample page works on mobile, tablet, and desktop
- Geometric background pattern renders correctly
- shadcn/ui components styled with design system
- User has visually approved the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-03-SUMMARY.md`
</output>
