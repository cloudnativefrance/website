---
phase: 18-venue-page-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/venue/index.astro
  - src/pages/en/venue/index.astro
  - tests/build/venue-cleanup.test.ts
autonomous: false
requirements: [VENUE-01, VENUE-02, VENUE-04]
must_haves:
  truths:
    - "The `<!-- 6. Previous edition replay -->` `<section>` block (lines ~218-285) is gone from src/pages/venue/index.astro."
    - "The equivalent `<section>` block is gone from src/pages/en/venue/index.astro (locale parity)."
    - "`grep -n \"EDITION_2026\\|ambiance03\\|ambiance06\\|ambiance10\\|YOUTUBE_ID\\|GALLERY_URL\\|previousStats\\|thumbnails\" src/pages/venue/index.astro` returns zero hits (SC2)."
    - "`grep -n \"EDITION_2026\\|ambiance03\\|ambiance06\\|ambiance10\\|YOUTUBE_ID\\|GALLERY_URL\\|previousStats\\|thumbnails\" src/pages/en/venue/index.astro` returns zero hits."
    - "`pnpm exec tsc --noEmit` passes with zero errors (SC2 second half)."
    - "`pnpm build` completes green; `dist/venue/index.html` and `dist/en/venue/index.html` contain zero matches for `1700+`, `youtube-nocookie`, `youtube.com/embed`, `CND France 2026` (D-03 dist-level assertions)."
    - "New file `tests/build/venue-cleanup.test.ts` exists and passes, encoding all D-03 source-level and dist-level assertions EXCEPT the `venue.prev` assertion (deferred to 18-02)."
    - "The venue page ends cleanly on the `venue.around.food_body` section — no filler link, no divider added after deletion (D-02)."
    - "The import `EDITION_2026` from `@/lib/editions-data` remains untouched in src/lib/editions-data.ts, src/pages/index.astro, and src/pages/en/index.astro (homepage still consumes it)."
    - "The image master files src/assets/photos/ambiance/ambiance-03.jpg, ambiance-06.jpg, ambiance-10.jpg remain on disk (used elsewhere per scouting)."
  artifacts:
    - path: "src/pages/venue/index.astro"
      provides: "FR venue page without the previous-edition block and without the four orphaned imports + four orphaned locals."
      contains: "venue.around.food_body"
    - path: "src/pages/en/venue/index.astro"
      provides: "EN venue page with identical cleanup (locale parity)."
      contains: "venue.around.food_body"
    - path: "tests/build/venue-cleanup.test.ts"
      provides: "Vitest build suite asserting source-level absence of 8 orphaned symbols on both venue pages + dist-level absence of the rendered block markers."
      min_lines: 40
      contains: "venue-cleanup"
  key_links:
    - from: "tests/build/venue-cleanup.test.ts"
      to: "dist/venue/index.html + dist/en/venue/index.html"
      via: "fs.readFileSync + regex assertions after `pnpm build`"
      pattern: "youtube-nocookie|1700\\+|CND France 2026"
    - from: "tests/build/venue-cleanup.test.ts"
      to: "src/pages/venue/index.astro + src/pages/en/venue/index.astro"
      via: "fs.readFileSync + negative-match regex on source"
      pattern: "EDITION_2026|ambiance0(3|6)|ambiance10|YOUTUBE_ID|GALLERY_URL|previousStats|thumbnails"
---

<objective>
Delete the "Previous edition 2026" block (the `<!-- 6. Previous edition replay -->` `<section>`) from both `src/pages/venue/index.astro` (FR) and `src/pages/en/venue/index.astro` (EN), now that Phase 17 shipped the 2026 homepage integration as the single source of truth for past-edition content. Remove the four orphaned imports (`EDITION_2026`, `ambiance03`, `ambiance06`, `ambiance10`) and the four orphaned locals (`YOUTUBE_ID`, `GALLERY_URL`, `previousStats`, `thumbnails`) from each venue page. Create a new Vitest build suite `tests/build/venue-cleanup.test.ts` that encodes the D-03 source-level and dist-level regression assertions (minus the `venue.prev` assertion, which lands in 18-02).

Wave 1: no dependencies on other Phase 18 plans. Depends implicitly on Phase 17 being complete (it is — 17-03 shipped the homepage mount, 17-04 shipped the 2023 minimal block).

Purpose: Remove the now-duplicated past-edition block from the venue page so content ownership is unambiguous: homepage owns past editions, venue owns venue. Honors D-01 (two commits, not three) and D-02 (no filler content).

Output: FR and EN venue pages trimmed to their venue-only concern; new regression test suite that will fail loudly if any orphaned symbol is re-introduced or if the block reappears in `dist/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-venue-page-cleanup/18-CONTEXT.md
@src/pages/venue/index.astro
@src/pages/en/venue/index.astro
@src/lib/editions-data.ts
@src/i18n/ui.ts
@tests/build/venue-block-unchanged.test.ts

<interfaces>
<!-- Block boundaries verified via grep on HEAD -->
<!-- src/pages/venue/index.astro (287 lines total): -->
<!--   Line 4:  import { EDITION_2026 } from "@/lib/editions-data"; -->
<!--   Lines 6-8: ambiance03 / ambiance06 / ambiance10 imports -->
<!--   Lines 25-26: const YOUTUBE_ID = EDITION_2026.youtubeId;  const GALLERY_URL = EDITION_2026.galleryUrl; -->
<!--   Lines 63-74: previousStats + thumbnails locals (plus the "Labels stay on..." comment that becomes dead text) -->
<!--   Lines 218-285: <!-- 6. Previous edition replay --> <section>...</section> -->

<!-- src/pages/en/venue/index.astro: mirror of FR (same structural layout per D-03 + Phase 17 UAT notes) -->

<!-- Dist markers to negate (from D-03): -->
<!--   dist/venue/index.html and dist/en/venue/index.html must NOT contain: -->
<!--     - "1700+" (the participants stat value) -->
<!--     - "youtube-nocookie" or "youtube.com/embed" (the iframe src) -->
<!--     - "CND France 2026" (rail/heading copy fragment) -->

<!-- Existing test patterns to mirror (Phase 17 style): -->
<!--   tests/build/venue-block-unchanged.test.ts — uses fs.readFileSync on dist/venue/index.html after `pnpm build` -->
<!--   tests/build/homepage-2026-section.test.ts — uses same pattern for dist assertions -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the previous-edition block and orphaned symbols from src/pages/venue/index.astro (FR)</name>
  <read_first>
    - src/pages/venue/index.astro (full file, 287 lines)
    - .planning/phases/18-venue-page-cleanup/18-CONTEXT.md (D-01, D-02 for scope)
  </read_first>
  <files>src/pages/venue/index.astro</files>
  <action>
In `src/pages/venue/index.astro`, apply **four deletions**, nothing else:

1. **Delete line 4:** `import { EDITION_2026 } from "@/lib/editions-data";`
2. **Delete lines 6-8** (the three ambiance imports):
   ```astro
   import ambiance03 from "@/assets/photos/ambiance/ambiance-03.jpg";
   import ambiance06 from "@/assets/photos/ambiance/ambiance-06.jpg";
   import ambiance10 from "@/assets/photos/ambiance/ambiance-10.jpg";
   ```
   (Leave the blank line above the `const lang = getLangFromUrl(...)` line clean — one blank line max between frontmatter imports and the first `const`.)
3. **Delete lines 25-26:**
   ```astro
   const YOUTUBE_ID = EDITION_2026.youtubeId;
   const GALLERY_URL = EDITION_2026.galleryUrl;
   ```
   (Also collapse any now-orphan blank lines.)
4. **Delete lines 63-74** (the comment + `previousStats` + `thumbnails` locals). After this delete, the frontmatter must proceed cleanly from the last pre-existing local to the `---` closer. Scrub the stale comment `// Labels stay on the venue-specific "venue.prev.*" keys (untouched in Phase 17 — removed Phase 18).` — it is dead text after this plan.
5. **Delete lines 218-285** (the entire `<!-- 6. Previous edition replay -->` `<section>...</section>` block, up to and including the closing `</section>`). The `</main>` on line 286 and `</Layout>` on line 287 become the new lines immediately after the preceding "around" `</section>`.

**Do not** add a filler link, a divider, or a "see homepage for past editions" CTA. Per D-02 the venue page ends on the final existing section. **Do not** modify any other content: transport, accessibility, map, around — all untouched.

Per D-01: one commit for all block-deletion + orphan-cleanup work.
  </action>
  <verify>
    <automated>grep -nE "EDITION_2026|ambiance03|ambiance06|ambiance10|YOUTUBE_ID|GALLERY_URL|previousStats|thumbnails|venue\.prev\.rail_label" src/pages/venue/index.astro ; test $? -eq 1</automated>
  </verify>
  <done>
    - FR venue page has zero occurrences of any of the 8 orphaned symbols.
    - File ends at the same `</Layout>` tag (closing brace parity preserved).
    - No new content added in place of the deleted block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror the deletion in src/pages/en/venue/index.astro (EN parity)</name>
  <read_first>
    - src/pages/en/venue/index.astro (full file)
  </read_first>
  <files>src/pages/en/venue/index.astro</files>
  <action>
Apply the **same four deletions** as Task 1 to `src/pages/en/venue/index.astro`. The EN file mirrors the FR file's structure; identify the equivalent imports (`EDITION_2026`, `ambiance03`, `ambiance06`, `ambiance10`), the equivalent frontmatter locals (`YOUTUBE_ID`, `GALLERY_URL`, `previousStats`, `thumbnails`, plus the "Labels stay on..." stale comment if present), and the equivalent `<!-- 6. Previous edition replay -->` `<section>` block. Delete them all. No filler content added. Locale parity is the top priority — do not diverge FR from EN.
  </action>
  <verify>
    <automated>grep -nE "EDITION_2026|ambiance03|ambiance06|ambiance10|YOUTUBE_ID|GALLERY_URL|previousStats|thumbnails|venue\.prev\.rail_label" src/pages/en/venue/index.astro ; test $? -eq 1</automated>
  </verify>
  <done>
    - EN venue page has zero occurrences of any of the 8 orphaned symbols.
    - EN venue page structurally matches FR venue page post-deletion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/build/venue-cleanup.test.ts regression suite + green the whole pipeline</name>
  <read_first>
    - tests/build/venue-block-unchanged.test.ts (Phase 17 pattern — dist + readFileSync + describe/it)
    - tests/build/homepage-2026-section.test.ts (Phase 17 pattern — helper functions, dist path resolution)
    - .planning/phases/18-venue-page-cleanup/18-CONTEXT.md (D-03 assertion list)
  </read_first>
  <files>tests/build/venue-cleanup.test.ts</files>
  <action>
Create a new Vitest file `tests/build/venue-cleanup.test.ts` that mirrors the pattern used by `tests/build/venue-block-unchanged.test.ts` (read dist HTML after `pnpm build` has run; read source files with `fs.readFileSync`). The suite MUST encode all D-03 assertions from 18-CONTEXT.md **except** the `venue.prev` i18n-key assertion (that one lands in 18-02 — leave a TODO comment pointing to it).

Required assertions (group into sensible `describe` blocks):

**Source-level (both FR and EN venue pages) — "venue source is free of orphaned previous-edition symbols":**
  - `src/pages/venue/index.astro` contains zero matches for the regex `/EDITION_2026|ambiance0(3|6)|ambiance10|YOUTUBE_ID|GALLERY_URL|previousStats|thumbnails/`
  - `src/pages/en/venue/index.astro` contains zero matches for the same regex

**Dist-level (both FR and EN rendered venue pages) — "rendered venue is free of the previous-edition block":**
  - `dist/venue/index.html` contains zero matches for `/1700\+/`, `/youtube-nocookie/`, `/youtube\.com\/embed/`, `/CND France 2026/`
  - `dist/en/venue/index.html` contains zero matches for the same four patterns

**Deferred (document only, do not assert):**
  - Add a TODO comment: `// TODO(18-02): assert grep venue.prev src/i18n/ui.ts -> 0 matches once the i18n sweep commit lands.`

Follow the Phase 17 convention: if `dist/` is missing at test-run time, the suite should call `execSync("pnpm build")` in a `beforeAll` (or skip dist-level assertions with a clear message and fail source-level only). Mirror exactly what `tests/build/venue-block-unchanged.test.ts` does — do not invent a new pattern.

Per D-03 and D-05: tests ship in the same commit as the deletion (no RED-first phase for pure deletion work).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build && pnpm exec vitest run tests/build/venue-cleanup.test.ts</automated>
  </verify>
  <done>
    - `tests/build/venue-cleanup.test.ts` exists and all assertions pass.
    - `pnpm exec tsc --noEmit` passes with zero errors (SC2 half #2).
    - `pnpm build` completes green; no orphaned asset warnings, no broken `t()` calls (SC5).
    - Roadmap grep assertion returns zero hits: `grep -n "ambiance03\|ambiance06\|ambiance10\|YOUTUBE_ID\|GALLERY_URL\|previousStats\|thumbnails" src/pages/venue/index.astro` (SC2 half #1).
    - Commit (via separate git command, single commit covering Tasks 1-3): `refactor(18-01): delete previous-edition 2026 block from venue pages + orphan cleanup`
  </done>
</task>

</tasks>

<verification>
- Roadmap SC1: "Previous edition 2026" block removed from `src/pages/venue/index.astro` in a commit separate from any homepage change → satisfied (no homepage file in `files_modified`).
- Roadmap SC2: grep returns zero hits + `pnpm exec tsc --noEmit` passes → asserted in Task 3 verify.
- Roadmap SC5: `pnpm build` succeeds with no orphaned `dist/` assets and no broken `t()` calls → asserted in Task 3 verify.
- Roadmap SC3 (`venue.prev` sweep) is deferred to Plan 18-02 per D-01.
- Roadmap SC4 (anchor audit) lands in 18-VERIFICATION.md per D-01 — not a plan concern.
</verification>

<success_criteria>
1. Both venue pages compile, build, and render cleanly with the block and all 8 orphan symbols removed.
2. `tests/build/venue-cleanup.test.ts` exists and is green.
3. One atomic commit lands on `main` with message `refactor(18-01): delete previous-edition 2026 block from venue pages + orphan cleanup`.
4. Homepage, editions-data, and i18n remain untouched by this plan (ownership scope is venue only).
</success_criteria>

<output>
After completion, create `.planning/phases/18-venue-page-cleanup/18-01-SUMMARY.md`.
</output>
