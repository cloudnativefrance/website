---
phase: 15-stitch-full-homepage-mock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [EDIT-06]
tags: [design, stitch, homepage]
must_haves:
  truths:
    - "A single Stitch screen renders the full homepage with section order Hero -> KeyNumbers -> CFP -> 2026 -> 2023 -> Testimonials -> Footer (D-12, EDIT-06)"
    - "CFP section top edge sits within ~1688px on a 390x844 mobile mock (D-11, ~2 viewport heights)"
    - "Mock uses only DS token roles (Background, Card, Primary, Accent, Foreground, Muted, Border) — no raw hex that contradicts DS asset 3926684191749761173"
    - "User explicitly approves the full-homepage Stitch mock in chat"
  artifacts:
    - path: ".planning/phases/15-stitch-full-homepage-mock/15-DISCUSSION-LOG.md"
      provides: "Record of Stitch screen ID(s) generated + user approval signal"
      contains: "15-01 full-homepage mock"
  key_links:
    - from: "Stitch generate_screen_from_text prompt"
      to: "Stitch project 14858529831105057917 + DS asset 3926684191749761173"
      via: "mcp__stitch__generate_screen_from_text with projectId and designSystemId parameters"
      pattern: "14858529831105057917.*3926684191749761173"
---

<objective>
Generate the single full-homepage Stitch mock that serves as the visual contract for v1.1. Lock section order (D-12), shared-shell rhythm (D-01, D-02), gap rhythm (D-03, D-11), CFP compact band (D-10), placeholder treatment (D-06), and the overall composition. Iterate in Stitch until the user explicitly approves.

Purpose: Stitch-first mandate (CLAUDE.md) — no code can land in Phases 16-20 without an approved visual contract. This plan produces that contract.

Output: Approved Stitch screen (full-homepage mock) + discussion-log entry with screen ID and approval signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-stitch-full-homepage-mock/15-CONTEXT.md
@.planning/ROADMAP.md
@DESIGN.md
@CLAUDE.md
@.claude/projects/-home-smana-Sources-cndfrance-website/memory/reference_stitch.md
@.claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_ds_tokens.md
@.claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_first.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Stitch project + DS asset, then generate full-homepage mock</name>
  <read_first>
    - .planning/phases/15-stitch-full-homepage-mock/15-CONTEXT.md (decisions D-01..D-12 verbatim)
    - .claude/projects/-home-smana-Sources-cndfrance-website/memory/reference_stitch.md (project 14858529831105057917, DS asset 3926684191749761173)
    - .claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_ds_tokens.md (token roles only, never raw hex)
    - DESIGN.md (token role names: Background, Card, Primary, Accent, Foreground, Muted, Border)
  </read_first>
  <files></files>
  <action>
    Step 1 — Verify state:
    - Call `mcp__stitch__get_project` with projectId `14858529831105057917` to confirm the project exists.
    - Call `mcp__stitch__list_design_systems` and confirm DS asset `3926684191749761173` is present; read its `designMd` field to verify token roles (Background, Card, Primary, Accent, Foreground, Muted, Border).

    Step 2 — Generate full-homepage mock with `mcp__stitch__generate_screen_from_text`:
    - projectId: `14858529831105057917`
    - designSystemId: `3926684191749761173`
    - Device: mobile 390x844 (primary target for D-11 CFP constraint)
    - Prompt body (locked decisions in verbatim form — DO NOT paraphrase):

    """
    Full homepage mock for Cloud Native Days France 2027. Dark theme using the project design system — reference tokens by role only (Background, Card, Primary Blue, Accent Pink, Foreground, Muted Foreground, Border). Never introduce raw hex values.

    Section order, top to bottom (locked by D-12, EDIT-06, reverse-chronological past editions):
    1. Hero — event name "Cloud Native Days France 2027", date, location, two CTAs (Primary "Register" + Accent "Submit a Talk")
    2. KeyNumbers — 3-up or 4-up stat tiles (attendees, speakers, sessions, sponsors)
    3. CFP — COMPACT BAND (D-10): one tight row with headline + dates + single CTA. No track list, no countdown timer.
    4. Edition 2026 — shared shell per D-01 order: vertical left rail label "EDITION 2026" (rotated text running down the left edge per D-02), then h2 heading, then stats row, then video embed placeholder (youtube-nocookie style thumbnail), then optional brand callout. Mark visible "PLACEHOLDER" badge on 2026 content per D-06.
    5. Edition 2023 — shared shell per D-01: vertical left rail label "EDITION 2023" (D-02), h2, stats row, 10-photo mosaic grid (asymmetric: 2 hero tiles + 3 medium + 5 small bottom row per D-04), then full-width brand-history callout band with KCD logo placeholder + short copy per D-05.
    6. Testimonials — single-row marquee strip of quote-led cards (large quote + small "Name — Role" attribution, no avatars), right-to-left slow drift per D-07/D-08. Indicate pause-on-hover visually via a "hover pauses" hint per D-09.
    7. Footer — standard footer (logo, links, social, legal).

    Vertical gap rhythm (D-03 default + D-11 exception):
    - Hero -> KeyNumbers -> CFP: 48px mobile gaps (tightened per D-11 to satisfy ~2 viewport heights constraint). Target: CFP top edge within ~1688px on 390x844.
    - CFP -> 2026 -> 2023 -> Testimonials -> Footer: 64px mobile / 96px desktop gaps (generous, D-03).

    Rail label treatment (D-02): rotated -90deg text on the left edge of each PastEditionSection, uppercase, tracking 0.05em, Muted Foreground color.

    Typography: DM Sans throughout. Hero event name at the scale of the type-6xl token (64px-equivalent), h2 at type-4xl (36px), body at type-base (16px). Tight letter-spacing on headings (-0.02em).

    Colors: dark theme using Background token (deep purple-tinted) as page background, Card token for stat tiles and quote cards, Primary Blue for buttons and links, Accent Pink sparingly (max one per section — use on "Submit a Talk" CTA and placeholder badge). No raw hex values in the render.

    Make CFP visually compact so its top edge lands within ~1688px from page top on 390x844 mobile (D-11).
    """

    Step 3 — Capture the returned screen ID. Share the preview URL with the user in chat.
  </action>
  <verify>
    <automated>`mcp__stitch__list_screens` with projectId `14858529831105057917` returns a screen with the ID captured in Step 3 and the description contains "full homepage" or equivalent.</automated>
  </verify>
  <acceptance_criteria>
    - Stitch screen exists in project 14858529831105057917 (verifiable via `mcp__stitch__get_screen` with the captured screenId)
    - Screen used designSystemId 3926684191749761173 (token roles, not raw hex)
    - Generated prompt contained verbatim D-02, D-04, D-05, D-06, D-09, D-10, D-11, D-12 language as written in 15-CONTEXT.md
    - Preview URL shared with user in chat
  </acceptance_criteria>
  <done>Full-homepage Stitch screen generated at mobile 390x844, screen ID recorded, preview shared with user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approves full-homepage mock (revise in Stitch until approved)</name>
  <read_first>
    - .planning/phases/15-stitch-full-homepage-mock/15-CONTEXT.md (D-01..D-12 are the acceptance bar)
    - .claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_first.md (design iteration happens in Stitch, not in code or description — re-generate/edit there first)
  </read_first>
  <files></files>
  <what-built>Full-homepage Stitch mock (mobile 390x844) covering Hero -> KeyNumbers -> CFP -> 2026 -> 2023 -> Testimonials -> Footer in one screen, using DS tokens only, with the CFP section top edge targeted within ~1688px.</what-built>
  <how-to-verify>
    1. Open the Stitch preview URL shared in Task 1.
    2. Verify section order matches D-12: Hero, KeyNumbers, CFP, 2026, 2023, Testimonials, Footer (top to bottom).
    3. Verify CFP section is compact (headline + dates + single CTA in one tight row — D-10) and its top edge sits within ~2 viewport heights on 390x844 (~1688px — D-11).
    4. Verify colors match the DS: deep purple-tinted Background, Card surfaces, Primary Blue CTAs, Accent Pink used sparingly. No foreign hex.
    5. Verify vertical gap rhythm feels tight Hero->KeyNumbers->CFP (48px mobile) and generous below (64px mobile / 96px desktop).
    6. Verify each PastEditionSection (2026, 2023) has a vertical left rail label reading "EDITION 2026" / "EDITION 2023" (rotated -90deg).
    7. Verify the 2026 block carries a visible "PLACEHOLDER" badge.
    8. Verify the 2023 photo grid is a mosaic (asymmetric tile sizes, not a uniform 2x5).
    9. Verify the 2023 brand-history callout sits below the photo grid as a full-width band.
    10. Verify the testimonials strip is a single row of quote-led cards with attribution (no avatars), and the direction reads right-to-left.

    If ANY check fails:
    - Use `mcp__stitch__edit_screens` with the screen ID to revise in Stitch FIRST (never revise in description or code — feedback_stitch_first.md rule).
    - Re-share preview with user, repeat verification.
    - Record each revision iteration in `.planning/phases/15-stitch-full-homepage-mock/15-DISCUSSION-LOG.md`.

    When all 10 checks pass, request explicit approval from user.
  </how-to-verify>
  <resume-signal>User types "approved" (or equivalent) in chat. Record approval in `.planning/phases/15-stitch-full-homepage-mock/15-DISCUSSION-LOG.md` with the approved screen ID and timestamp.</resume-signal>
  <acceptance_criteria>
    - 15-DISCUSSION-LOG.md contains a line matching pattern `15-01.*approved.*screen.*[0-9]+` with the approved screen ID
    - User approval explicit in chat transcript
    - No code files modified (design-only phase)
  </acceptance_criteria>
  <action>This is a human-verification checkpoint. See <what-built>, <how-to-verify>, and <resume-signal> above. Executor must pause and wait for the user's approval signal; no autonomous action is taken beyond presenting the preview and (if needed) invoking Stitch edit tools in response to user feedback.</action>
  <verify>
    <automated>User approval explicit in chat; `.planning/phases/15-stitch-full-homepage-mock/15-DISCUSSION-LOG.md` updated with approval entry.</automated>
  </verify>
  <done>- 15-DISCUSSION-LOG.md contains a line matching pattern `15-01.*approved.*screen.*[0-9]+` with the approved screen ID
    - User approval explicit in chat transcript
    - No code files modified (design-only phase)</done>
</task>

</tasks>

<verification>
- `mcp__stitch__get_screen` returns the approved screen with DS asset 3926684191749761173
- 15-DISCUSSION-LOG.md has a "15-01 approved" entry
- No files outside `.planning/` were modified
</verification>

<success_criteria>
- Full-homepage Stitch mock exists, uses DS tokens only, covers Hero -> KeyNumbers -> CFP -> 2026 -> 2023 -> Testimonials -> Footer
- CFP top edge visually within ~2vh on mobile (ROADMAP success criterion 2)
- User explicitly approved
- Approval recorded in 15-DISCUSSION-LOG.md
</success_criteria>

<output>
After completion, create `.planning/phases/15-stitch-full-homepage-mock/15-01-SUMMARY.md` including:
- Approved Stitch screen ID
- Iteration count (how many edit cycles before approval)
- Any D-XX decisions the user amended during review (should be zero — locked decisions)
- Link to DISCUSSION-LOG entry
</output>
