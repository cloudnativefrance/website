---
phase: 3
slug: hero-landing
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-11
---

# Phase 03 — UI Design Contract

> Visual and interaction contract for the hero and landing page. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | radix (via shadcn) |
| Icon library | lucide |
| Font | DM Sans (Google Fonts via Astro Font API) |

Source: `components.json`, DESIGN.md

---

## Spacing Scale

Declared values (all multiples of 4, from DESIGN.md):

| Token | Value | Usage in This Phase |
|-------|-------|---------------------|
| spacing-1 | 4px | Icon gaps within countdown digit groups |
| spacing-2 | 8px | Gap between countdown digit and label |
| spacing-3 | 12px | CTA button vertical padding |
| spacing-4 | 16px | Horizontal page padding (mobile), card internal padding |
| spacing-6 | 24px | CTA button horizontal padding, key-numbers card padding |
| spacing-8 | 32px | Gap between key-number cards |
| spacing-10 | 40px | Gap between hero subsections (title block, countdown, CTAs) |
| spacing-12 | 48px | Section padding (key numbers top/bottom) |
| spacing-16 | 64px | Hero section vertical padding on mobile |
| spacing-24 | 96px | Hero section vertical padding on desktop |

Exceptions: Countdown digit cards use 44px minimum touch target height (WCAG, per DESIGN.md accessibility notes).

---

## Typography

All values from DESIGN.md type scale. This phase uses **4 size roles** and **2 weights**.

Responsive variants of the same role count as one size role, not two.

| Role | Size (desktop) | Size (mobile) | Weight | Line Height | Letter Spacing |
|------|----------------|---------------|--------|-------------|----------------|
| Display (h1, hero title) | 64px (text-6xl) | 36px (text-4xl) | 700 (Bold) | 1.05 | -0.02em |
| Feature number (key-number value, countdown digit) | 48px (text-5xl) | 20px (text-xl) | 700 (Bold) | 1.1 | -0.02em |
| Subtitle (hero date/venue, CTA button) | 20px (text-xl) | 20px (text-xl) | 700 (Bold) | 1.4 | 0 |
| Body (key-number label, countdown label, tagline) | 16px (text-base) | 16px (text-base) | 400 (Regular) | 1.6 | 0 |

### Size Role Mapping

| Component Element | Size Role | Notes |
|-------------------|-----------|-------|
| Hero title (h1) | Display | 64px desktop, 36px mobile |
| Key-number value | Feature number | 48px desktop, 20px mobile |
| Countdown digit | Feature number | 48px desktop, 20px mobile |
| Hero subtitle (date/venue) | Subtitle | 20px at all breakpoints |
| CTA button text | Subtitle | 20px at all breakpoints |
| Key-number label | Body | 16px at all breakpoints |
| Countdown label | Body | Uppercase, letter-spacing 0.05em |
| Event tagline | Body | 16px at all breakpoints |

### Weight Rules

Only 2 weights are permitted in this phase:

| Weight | Token | Usage |
|--------|-------|-------|
| 400 | Regular | Body text, labels, countdown labels, key-number labels |
| 700 | Bold | Headings, hero title, CTA buttons, countdown digits, key-number values, emphasized text |

Source: DESIGN.md Typography section, aligned with Phase 02 weight consolidation (commit d2b4c1a)

---

## Color

All values from DESIGN.md Semantic Token Map.

| Role | Token | Hex Approx | Usage in This Phase |
|------|-------|------------|---------------------|
| Dominant (60%) | --color-background | #0e0a24 | Page background, hero background |
| Secondary (30%) | --color-card / --color-secondary | #1a1240 / #251c50 | Countdown digit cards, key-number cards |
| Accent (10%) | --color-accent | #ff8a9e | Event date badge only |
| Primary interactive | --color-primary | #4985e8 | "Register" CTA button background, countdown active state |
| Destructive | --color-destructive | #cf0822 | Not used in this phase |

Accent reserved for:
- Event date badge (the "3 juin 2027" / "June 3, 2027" pill)
- One accent per visual section maximum (per DESIGN.md guidelines)

Primary blue reserved for:
- "Register" CTA button (primary action)
- Countdown digit card border highlight on hover
- Focus rings on interactive elements

Background gradient: Hero section uses a subtle radial gradient from `--color-background` center to `--color-secondary` edges, overlaid with GeoBackground hex mesh pattern.

Source: DESIGN.md Color Usage Guidelines, Hero Section pattern

---

## Component Inventory

### 1. HeroSection

Full-viewport landing section. Astro component (no client JS except countdown).

**Visual Hierarchy (focal point order):**

1. **Primary focal point:** h1 event name -- largest text (Display size role, 64px desktop), bold 700, highest contrast (`--color-foreground` on `--color-background`)
2. **Secondary focal point:** Primary CTA "Register" button -- `--color-primary` background with glow shadow, positioned after the title block, size="lg"
3. **Tertiary focal point:** Date badge -- `--color-accent` pill at 15% opacity background, positioned on the subtitle line, draws the eye after the CTA

| Element | Spec |
|---------|------|
| Container | `min-h-screen` on desktop (`lg:`), `min-h-[80vh]` on mobile, `relative`, `overflow-hidden` |
| Background | GeoBackground.astro (existing) positioned `absolute inset-0` + radial gradient overlay |
| Content wrapper | `relative z-10`, `max-w-4xl`, centered flex column, `gap-10` (40px) |
| Event name (h1) | Display size role: text-6xl bold on lg, text-4xl on mobile, `--color-foreground` |
| Date/venue line | Subtitle size role: text-xl, weight 400 (exception: this line uses Regular for contrast against the bold h1), `--color-muted-foreground` |
| Date badge | Accent pill badge: `--color-accent` background at 15% opacity, `--color-accent` text, radius-full, Body size role uppercase tracking-widest |
| CTA group | Flex row on sm+, column on mobile, gap-4 (16px) |
| Primary CTA | shadcn Button size="lg", `--color-primary` bg, white text, glow shadow, hover translateY(-1px), Subtitle size role bold |
| Secondary CTA | shadcn Button variant="outline" size="lg", transparent bg, border-border, Subtitle size role bold |

### 2. CountdownTimer

React island (`client:load`). Counts down to June 3, 2027 09:00 CEST.

| Element | Spec |
|---------|------|
| Container | Flex row, gap-4 (16px) between digit groups, centered |
| Digit card | `--color-card` background, `--color-border` border (1px), radius-lg (8px), min-width 72px, padding 12px 8px, text-center |
| Digit value | Feature number size role: text-5xl bold desktop, text-xl bold mobile, `--color-foreground` |
| Digit label | Body size role: text-base regular uppercase, `--color-muted-foreground`, tracking-widest |
| Separator | Colon character in `--color-muted-foreground` at Feature number size between groups, or thin border |
| Update | setInterval at 1000ms, no visible flicker (use state batching) |
| Mobile | 4 digit groups in a row, reduce card min-width to 60px |
| a11y | `aria-live="polite"` on the container, `aria-label` with full time remaining text, update aria every 60s not every second |

Target date: `2027-06-03T09:00:00+02:00` (CEST)

### 3. KeyNumbers

Static Astro component. Three stat blocks in a row.

| Element | Spec |
|---------|------|
| Container | `max-w-5xl` centered, grid `grid-cols-1 sm:grid-cols-3`, gap-8 (32px), padding-y spacing-12 (48px) |
| Stat block | `--color-card` background, `--color-border` border (1px), radius-lg (8px), padding spacing-6 (24px), text-center |
| Number value | Feature number size role: text-5xl bold on desktop, text-xl bold on mobile, `--color-primary` text |
| Number suffix | Inline with value, same size, `--color-accent` text (the "+" character) |
| Label | Body size role: text-base regular, `--color-muted-foreground` |
| Hover | Border transitions to `--color-primary` at 50% opacity, 200ms ease |

Data:

| Value | FR Label | EN Label |
|-------|----------|----------|
| 1700+ | Participants | Attendees |
| 50+ | Talks | Talks |
| 40+ | Partenaires | Partners |

### 4. GeoBackground (existing)

Already implemented at `src/components/patterns/GeoBackground.astro`. No changes needed. Used as-is with `class="absolute inset-0"` in the hero section.

---

## Copywriting Contract

All copy must exist in both FR and EN in `src/i18n/ui.ts`.

| Element | FR | EN |
|---------|----|----|
| Hero title | Cloud Native Days France | Cloud Native Days France |
| Hero subtitle | 3 juin 2027 -- CENTQUATRE-PARIS | June 3, 2027 -- CENTQUATRE-PARIS |
| Primary CTA | Reservez votre place | Get Your Ticket |
| Secondary CTA | Voir le programme | View Schedule |
| Countdown: days | jours | days |
| Countdown: hours | heures | hours |
| Countdown: minutes | minutes | minutes |
| Countdown: seconds | secondes | seconds |
| Key numbers heading | L'evenement en chiffres | The Event in Numbers |
| Key number: attendees | Participants | Attendees |
| Key number: talks | Talks | Talks |
| Key number: partners | Partenaires | Partners |
| Empty state (post-event) | L'evenement est termine ! | The event has ended! |
| Post-event CTA | Voir les replays | Watch Replays |

Translation keys to add to `src/i18n/ui.ts`:

```
hero.title, hero.subtitle, hero.cta.register, hero.cta.schedule,
countdown.days, countdown.hours, countdown.minutes, countdown.seconds,
keynumbers.heading, keynumbers.attendees, keynumbers.talks, keynumbers.partners,
hero.post_event, hero.cta.replays
```

No destructive actions in this phase.
No error states in this phase (all content is static/hardcoded).

---

## Interaction Contract

### Countdown Timer
- Updates every 1 second via `setInterval`
- When countdown reaches zero: hide countdown, show post-event message with replay CTA
- No animation on digit change (keep it simple, avoid layout shift)
- Timer uses `client:load` directive (visible above the fold, must hydrate immediately)

### CTA Buttons
- Primary "Register" button: `hover:translateY(-1px)` lift, glow shadow `0 0 20px oklch(62.5% 0.162 259.9 / 0.3)`
- Secondary "View Schedule" button: `hover:bg-secondary` background transition
- Both buttons: `transition-all duration-150`
- Primary CTA links to external ticketing URL (placeholder `#register` until real URL provided)
- Secondary CTA links to `/programme` (FR) or `/en/schedule` (EN) -- will be placeholder until Phase 7

### Key Number Cards
- Hover: border-color transitions from `--color-border` to `--color-primary/50` over 200ms
- No click action (purely informational)

### Responsive Behavior
- Mobile (<640px): Single column layout, hero Display role at 36px, key numbers stacked, countdown digits at text-xl
- Tablet (640-1023px): Key numbers in 3-column grid, hero Display role at 48px
- Desktop (1024px+): Full viewport hero, Display role at 64px, all elements at full size

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Card, Badge, Separator | not required |

No third-party registries declared.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
