---
phase: 09
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - astro.config.mjs
  - public/robots.txt
  - package.json
autonomous: true
requirements: [META-02]
tags: [seo, sitemap, robots, indexing]
must_haves:
  truths:
    - "A sitemap-index.xml file is generated at build time"
    - "A robots.txt file is served at the site root and references the sitemap"
    - "Sitemap includes all public routes in both FR and EN"
    - "@astrojs/sitemap integration is installed and configured with the site's i18n config"
  artifacts:
    - path: "public/robots.txt"
      provides: "Robots directives + sitemap URL"
      contains: "Sitemap:"
    - path: "astro.config.mjs"
      provides: "@astrojs/sitemap integration with i18n config"
      contains: "sitemap"
    - path: "package.json"
      provides: "@astrojs/sitemap dependency"
      contains: "@astrojs/sitemap"
  key_links:
    - from: "public/robots.txt"
      to: "sitemap-index.xml"
      via: "Sitemap: directive with absolute URL"
      pattern: "Sitemap: https://cloudnativedays.fr/sitemap-index.xml"
    - from: "astro.config.mjs sitemap()"
      to: "Astro.site"
      via: "sitemap integration reads `site:` field for absolute URLs"
      pattern: "site: \"https://cloudnativedays.fr\""
---

<objective>
Install `@astrojs/sitemap`, wire it into `astro.config.mjs`, and author `public/robots.txt`. Closes META-02.

Purpose: Search-engine discoverability. `@astrojs/sitemap` auto-generates `sitemap-index.xml` + `sitemap-0.xml` at build time, picking up every route from Astro's file-based router. It also honors the i18n config (FR + EN alternates). `robots.txt` is hand-authored (only 3 lines) and references the generated sitemap via absolute URL.

Output: `@astrojs/sitemap` installed and integrated; `public/robots.txt` committed; build produces `dist/sitemap-index.xml` + `dist/sitemap-0.xml`.

Note on wave parallelism: This plan is independent of 09-01 — both modify `astro.config.mjs` but at different property locations (09-01 adds `site:`, this plan adds `sitemap()` to `integrations:`). **However**, to avoid merge conflicts on `astro.config.mjs`, executor MUST read the current file state before edits (single-file shared surface). If this plan runs before 09-01, it ADDS `site: "https://cloudnativedays.fr"` as well (09-01 will no-op on a re-add via conditional check). If this plan runs after, leave `site:` alone and only add the sitemap integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-seo-legal-polish/09-CONTEXT.md
@astro.config.mjs

<interfaces>
@astrojs/sitemap official docs: https://docs.astro.build/en/guides/integrations-guide/sitemap/

Expected integration shape:
```javascript
import sitemap from "@astrojs/sitemap";

export default defineConfig({
  site: "https://cloudnativedays.fr",
  integrations: [
    react(),
    sitemap({
      i18n: {
        defaultLocale: "fr",
        locales: { fr: "fr-FR", en: "en-US" },
      },
    }),
  ],
  // ...
});
```

The sitemap integration emits:
- `dist/sitemap-index.xml` — references sitemap-0.xml
- `dist/sitemap-0.xml` — one `<url>` per route with `<xhtml:link rel="alternate" hreflang="...">` for i18n
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install @astrojs/sitemap + wire into astro.config.mjs</name>
  <read_first>
    - astro.config.mjs (current state — after 09-01 runs it has `site: "https://cloudnativedays.fr"`, before 09-01 it does not)
    - package.json (confirm current deps; check for existing `@astrojs/*` versions to match)
    - .planning/phases/09-seo-legal-polish/09-CONTEXT.md D-05
  </read_first>
  <action>
  1. Install: `pnpm add -D @astrojs/sitemap` (dev dependency — integration runs at build time only).
  2. Edit `astro.config.mjs`:
     - Add `import sitemap from "@astrojs/sitemap";` at the top alongside other integration imports.
     - If `site:` is NOT already set (09-01 may have already added it), add `site: "https://cloudnativedays.fr",` as the first property of `defineConfig({...})`.
     - Add `sitemap({ i18n: { defaultLocale: "fr", locales: { fr: "fr-FR", en: "en-US" } } })` to the `integrations:` array (after `react()`).
  3. Do NOT add custom `filter` or `customPages` — default behavior (index every route) is correct for v1.0. D-05 explicitly says "Exclude paths: none for v1.0".
  </action>
  <verify>
    <automated>rg '"@astrojs/sitemap"' package.json && rg 'import sitemap from "@astrojs/sitemap"' astro.config.mjs && rg 'sitemap\(' astro.config.mjs && rg 'site: "https://cloudnativedays.fr"' astro.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"@astrojs/sitemap"' package.json` returns a match (dependency added)
    - `rg 'import sitemap' astro.config.mjs` returns 1 match
    - `rg 'sitemap\(\{' astro.config.mjs` returns 1 match (integration invoked)
    - `rg 'site: "https://cloudnativedays.fr"' astro.config.mjs` returns 1 match
    - `pnpm install` completes without errors
  </acceptance_criteria>
  <done>@astrojs/sitemap installed and wired with i18n config; astro.config.mjs still type-checks and module-loads.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author public/robots.txt + verify build emits sitemap</name>
  <read_first>
    - .planning/phases/09-seo-legal-polish/09-CONTEXT.md D-05 (robots.txt content spec)
  </read_first>
  <action>
  1. Create `public/robots.txt` with exactly these three directives (one trailing newline):
  ```
  User-agent: *
  Allow: /
  Sitemap: https://cloudnativedays.fr/sitemap-index.xml
  ```

  2. Run `pnpm build` and verify:
     - `dist/robots.txt` exists (Astro copies `public/` verbatim)
     - `dist/sitemap-index.xml` exists
     - `dist/sitemap-0.xml` exists and contains at least the homepage + a few known routes

  If the sitemap files are missing after build, re-verify the integration import + config in Task 1; do not ship without the generated files.
  </action>
  <verify>
    <automated>pnpm build && test -f dist/robots.txt && rg -F "Sitemap: https://cloudnativedays.fr/sitemap-index.xml" dist/robots.txt && test -f dist/sitemap-index.xml && test -f dist/sitemap-0.xml && rg -F "https://cloudnativedays.fr/" dist/sitemap-0.xml && rg -F "https://cloudnativedays.fr/en/" dist/sitemap-0.xml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f public/robots.txt` succeeds
    - `rg -c "Sitemap: https://cloudnativedays.fr/sitemap-index.xml" public/robots.txt` returns 1
    - `pnpm build` exits 0
    - `test -f dist/robots.txt && test -f dist/sitemap-index.xml && test -f dist/sitemap-0.xml` all succeed
    - `rg -F "https://cloudnativedays.fr/" dist/sitemap-0.xml` returns at least 5 matches (home, speakers, sponsors, venue, programme in FR)
    - `rg -F "https://cloudnativedays.fr/en/" dist/sitemap-0.xml` returns at least 3 matches (EN variants)
    - `rg 'hreflang="fr-FR"' dist/sitemap-0.xml` returns matches (xhtml:link alternates present — per @astrojs/sitemap i18n behavior)
  </acceptance_criteria>
  <done>robots.txt committed, sitemap-index.xml + sitemap-0.xml emit at build, both FR and EN URLs indexed, each route carries i18n alternates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public/robots.txt` → search crawlers | Static declaration; no trust boundary crossed inbound |
| `sitemap-0.xml` (build output) → search crawlers | Static declaration of all routes; information disclosure of route structure is by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | `sitemap-0.xml` exposes all routes | accept | No draft/admin/private routes exist in v1.0 per D-05 ("Exclude paths: none"). If such routes appear in future phases, they must be added to `@astrojs/sitemap` `filter` option. Documented as accepted. |
| T-09-06 | Tampering | `robots.txt` Sitemap directive | accept | Static file; no untrusted input reaches it. Path hardcoded to production domain. |
| T-09-07 | Denial of Service | Crawler over-fetching | accept | Single-page event site; crawl budget is minimal. No rate limiting required at infra layer for a static site on nginx. |
</threat_model>

<verification>
- `pnpm build` emits `dist/robots.txt`, `dist/sitemap-index.xml`, `dist/sitemap-0.xml`
- robots.txt references the correct production sitemap URL
- Sitemap contains FR and EN variants of every route with proper hreflang alternates
</verification>

<success_criteria>
- META-02: sitemap.xml + robots.txt generated and accessible at build ✓
- No manual maintenance: every new page added in future phases auto-appears in sitemap
- i18n alternates correctly declared in sitemap (xhtml:link entries)
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-legal-polish/09-02-SUMMARY.md`.
</output>
