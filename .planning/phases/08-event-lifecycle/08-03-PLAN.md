---
phase: 08-event-lifecycle
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/components/cfp/CfpSection.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
autonomous: true
requirements:
  - EVNT-04
must_haves:
  truths:
    - "Homepage (FR + EN) shows a CFP section between Key Numbers and Footer whose visual state reflects getCfpState() at render time"
    - "CFP 'open' state uses accent-pink pill + accent button; 'coming-soon' uses muted pill + ghost/hidden CTA; 'closed' renders the muted no-CTA note per D-04"
    - "All copy in the section comes from i18n keys — no hardcoded FR/EN strings"
    - "Accent color is used only on the pill and primary CTA when state === 'open' (per UI-SPEC Accent Reserved For)"
  artifacts:
    - path: "src/components/cfp/CfpSection.astro"
      provides: "Homepage CFP status section, three-state visual component"
      contains: "getCfpState"
    - path: "src/pages/index.astro"
      provides: "FR homepage with CfpSection slotted between KeyNumbers and Footer"
      contains: "CfpSection"
    - path: "src/pages/en/index.astro"
      provides: "EN homepage with CfpSection slotted between KeyNumbers and Footer"
      contains: "CfpSection"
  key_links:
    - from: "src/components/cfp/CfpSection.astro"
      to: "src/lib/cfp.ts"
      via: "import { getCfpState, CFP_CLOSES, CONFERENCE_HALL_URL } from '@/lib/cfp'"
      pattern: "from ['\"]@/lib/cfp['\"]"
    - from: "src/components/cfp/CfpSection.astro"
      to: "src/i18n/ui.ts"
      via: "useTranslations(lang) with cfp.* keys"
      pattern: "cfp\\."
---

<objective>
Implement the CFP homepage section (EVNT-04). Reads state from `src/lib/cfp.ts` (build-time — the static page is re-deployed when staff edit placeholder dates). Wires it into both homepages between Key Numbers and Footer.

Depends on 08-01 (cfp.ts + i18n keys) and 08-02 (Stitch approval).

Output: Single `CfpSection.astro` component + two homepage edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-event-lifecycle/08-UI-SPEC.md
@.planning/phases/08-event-lifecycle/08-CONTEXT.md
@DESIGN.md
@src/lib/cfp.ts
@src/i18n/ui.ts
@src/i18n/utils.ts
@src/pages/index.astro
@src/pages/en/index.astro
@src/components/hero/KeyNumbers.tsx

<interfaces>
From src/lib/cfp.ts (created in 08-01):
```typescript
export function getCfpState(now?: Date): "coming-soon" | "open" | "closed";
export const CFP_OPENS: Date;
export const CFP_CLOSES: Date;
export const CONFERENCE_HALL_URL: string;
```

From src/i18n/utils.ts:
```typescript
export function getLangFromUrl(url: URL): Locale;
export function useTranslations(lang: Locale): (key) => string;
```

Existing homepage shape (src/pages/index.astro):
```astro
<main>
  <HeroSection />
  <KeyNumbers client:idle lang={lang} />
</main>
```
Insert CfpSection AFTER KeyNumbers and BEFORE `</main>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/components/cfp/CfpSection.astro with three-state rendering</name>
  <files>src/components/cfp/CfpSection.astro</files>
  <read_first>
    - src/lib/cfp.ts (the exports this component consumes)
    - .planning/phases/08-event-lifecycle/08-UI-SPEC.md §1. CFP Status Section (structure, states, responsive, motion)
    - DESIGN.md §Component Patterns §Buttons (accent button spec)
    - src/components/hero/KeyNumbers.tsx (for matching section rhythm and container width)
    - .planning/phases/08-event-lifecycle/stitch/CFP-coming-soon.png, CFP-open.png, CFP-closed.png (approved Stitch reference)
  </read_first>
  <behavior>
    - When getCfpState() === "open": pill has `bg-accent text-accent-foreground`, description uses cfp.description.open, CTA is an `<a>` with `bg-accent shadow-[...]` linking to CONFERENCE_HALL_URL with label `cfp.cta.submit`, meta row shows `cfp.deadline` with formatted CFP_CLOSES date via `Intl.DateTimeFormat(lang === "fr" ? "fr-FR" : "en-US", { year: "numeric", month: "long", day: "numeric" })`.
    - When getCfpState() === "coming-soon": pill has `bg-secondary text-muted-foreground`, description uses cfp.description.coming_soon, CTA is a ghost-style `<a>` with label `cfp.cta.notify` linking to CONFERENCE_HALL_URL — OR hidden when URL is the TODO placeholder (implementer picks per D-04).
    - When getCfpState() === "closed": render a single centered muted line with copy `cfp.closed.note`, no CTA, no pill. (Implementer picks between full-hide and muted-note per D-04 — UI-SPEC allows either; pick muted-note so the rhythm is preserved.)
    - All strings come from `useTranslations(lang)`, never hardcoded.
    - aria-label on the pill follows UI-SPEC: `{cfp.status.*} — {cfp.description.*}`.
  </behavior>
  <action>
    Create `src/components/cfp/CfpSection.astro`.

    Structure (Astro frontmatter + markup):

    ```astro
    ---
    import { getLangFromUrl, useTranslations } from "@/i18n/utils";
    import { getCfpState, CFP_CLOSES, CONFERENCE_HALL_URL } from "@/lib/cfp";

    const lang = getLangFromUrl(Astro.url);
    const t = useTranslations(lang);
    const state = getCfpState();
    const localeTag = lang === "fr" ? "fr-FR" : "en-US";
    const deadline = new Intl.DateTimeFormat(localeTag, { year: "numeric", month: "long", day: "numeric" }).format(CFP_CLOSES);
    ---
    ```

    Then render three branches via `{state === "open" && (...)} {state === "coming-soon" && (...)} {state === "closed" && (...)}` or a switch pattern.

    Classes (Tailwind 4 + DS tokens — no raw hex):

    - Outer `<section>`: `relative w-full py-12 md:py-16 bg-background` (section rhythm matches Key Numbers). Section id: `id="cfp"` so anchor links work.
    - Inner container: `mx-auto max-w-[1280px] px-4 md:px-6`
    - Content stack: `flex flex-col items-center text-center md:items-start md:text-left gap-6 max-w-2xl md:mx-0 mx-auto`
    - Pill (open state): `inline-flex items-center gap-2 rounded-full bg-accent text-accent-foreground px-[10px] py-1 text-xs font-normal uppercase tracking-[0.05em]` + lucide `Send` icon from `lucide-astro` (if installed) OR inline SVG (check dependencies — if lucide-astro not present, use inline svg matching Send glyph).
    - Pill (coming-soon): same shape, `bg-secondary text-muted-foreground`, lucide `Megaphone` icon.
    - Pill (closed) is not rendered — closed state shows a single muted line.
    - Headline: `<h2 class="text-2xl font-bold leading-[1.3] tracking-[-0.02em] text-foreground">{t("cfp.heading")}</h2>`
    - Description paragraph: `<p class="text-base text-muted-foreground leading-[1.6] max-w-[60ch]">{t("cfp.description." + state)}</p>` (only for open/coming-soon)
    - CTA (open): `<a href={CONFERENCE_HALL_URL} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 h-[52px] rounded-md bg-accent text-accent-foreground px-8 text-lg font-bold shadow-[0_0_20px_oklch(76.6%_0.142_10.1_/_0.25)] transition-all hover:translate-y-[-1px]">{t("cfp.cta.submit")}<ArrowRightIcon /></a>`
    - CTA (coming-soon): `<a href={CONFERENCE_HALL_URL} class="inline-flex items-center gap-2 h-[44px] rounded-md border border-border text-foreground px-6 text-base font-medium hover:bg-secondary transition-colors">{t("cfp.cta.notify")}</a>` — gated on `!CONFERENCE_HALL_URL.includes("TODO_EVENT_ID")` (hide CTA when URL is still placeholder).
    - Meta row (open only): `<p class="text-xs text-muted-foreground">{t("cfp.deadline").replace("{date}", deadline)}</p>`
    - Closed branch: `<p class="text-base text-muted-foreground text-center">{t("cfp.closed.note")}</p>` inside the same section container.

    For the lucide icons: check if `lucide-astro` is a dependency (`grep lucide package.json`). If yes, `import { Send, Megaphone, ArrowRight } from "lucide-astro"` and use `<Send size={16} />` etc. If not, inline SVG paths copied from lucide.dev using `stroke="currentColor"` and `stroke-width="2"`.

    Accent reserved-for rule: the accent CTA and accent pill only appear when state === "open". The coming-soon pill uses secondary/muted tokens. Closed state shows no accent at all. Verify the `grep` checks in acceptance criteria prove this.

    No client-side JavaScript needed — state is computed at build time. When staff edit `CFP_OPENS`/`CFP_CLOSES` and redeploy, the section updates. (This is intentionally simpler than the countdown — staff rebuild cadence is fine for CFP state per D-01.)
  </action>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website && test -f src/components/cfp/CfpSection.astro && grep -q 'from "@/lib/cfp"' src/components/cfp/CfpSection.astro && grep -q 'getCfpState()' src/components/cfp/CfpSection.astro && grep -q 'cfp.heading' src/components/cfp/CfpSection.astro && grep -q 'cfp.description' src/components/cfp/CfpSection.astro && grep -q 'cfp.cta.submit' src/components/cfp/CfpSection.astro && grep -q 'cfp.closed.note' src/components/cfp/CfpSection.astro && grep -q 'CONFERENCE_HALL_URL' src/components/cfp/CfpSection.astro && grep -q 'Intl.DateTimeFormat' src/components/cfp/CfpSection.astro && ! grep -E "#[0-9a-fA-F]{6}" src/components/cfp/CfpSection.astro && pnpm astro check 2>&1 | grep -i "CfpSection" | grep -i error && exit 1; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from "@/lib/cfp"' src/components/cfp/CfpSection.astro` succeeds (imports from central config)
    - `grep -q 'getCfpState()' src/components/cfp/CfpSection.astro` succeeds (state is computed, not hardcoded)
    - All five cfp.* keys referenced: `grep -c "cfp\\.\\(heading\\|description\\|cta\\|closed\\|status\\|deadline\\)" src/components/cfp/CfpSection.astro` returns >= 5
    - `grep -q "Intl.DateTimeFormat" src/components/cfp/CfpSection.astro` succeeds (localized date formatting)
    - No raw hex in the component (except the documented shadow-[...] oklch expression from UI-SPEC): `grep -vE 'shadow-\\[0_0_20px' src/components/cfp/CfpSection.astro | grep -E "#[0-9a-fA-F]{6}"` returns no matches
    - `pnpm astro check` reports zero errors involving CfpSection.astro
    - `bg-accent` appears only in branches gated on state === "open" (manual visual check OR `grep -A2 'state === "open"' src/components/cfp/CfpSection.astro | grep -q 'bg-accent'` succeeds AND coming-soon/closed branches don't reference bg-accent)
  </acceptance_criteria>
  <done>Component exists, builds clean, renders state-dependent markup using DS tokens, uses i18n for all copy, no hex literals.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CfpSection into both homepages (FR + EN)</name>
  <files>src/pages/index.astro, src/pages/en/index.astro</files>
  <read_first>
    - src/pages/index.astro (current state)
    - src/pages/en/index.astro (current state)
    - src/components/cfp/CfpSection.astro (created in Task 1)
  </read_first>
  <action>
    In both `src/pages/index.astro` and `src/pages/en/index.astro`:

    1. Add import after the existing KeyNumbers import:
       `import CfpSection from "@/components/cfp/CfpSection.astro";`

    2. Inside `<main>`, after `<KeyNumbers client:idle lang={lang} />` and before `</main>`, add:
       `<CfpSection />`

    Both files get the same two-line edit. CfpSection reads lang from Astro.url itself, so no prop is needed (mirror how Navigation.astro works).

    Do NOT touch HeroSection, KeyNumbers, or the JSON-LD script tag. Only add the import and the component slot.
  </action>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website && grep -q 'import CfpSection from "@/components/cfp/CfpSection.astro"' src/pages/index.astro && grep -q "<CfpSection" src/pages/index.astro && grep -q 'import CfpSection from "@/components/cfp/CfpSection.astro"' src/pages/en/index.astro && grep -q "<CfpSection" src/pages/en/index.astro && grep -B1 "<CfpSection" src/pages/index.astro | grep -q "KeyNumbers" && grep -B1 "<CfpSection" src/pages/en/index.astro | grep -q "KeyNumbers" && pnpm astro build 2>&1 | tail -20 | grep -iE "error|fail" && exit 1; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both `src/pages/index.astro` and `src/pages/en/index.astro` contain `import CfpSection from "@/components/cfp/CfpSection.astro"`
    - Both contain `<CfpSection` (self-closing or with children)
    - In both files, `<CfpSection` appears on a line after `<KeyNumbers` (verified by `grep -n`)
    - `pnpm astro build` succeeds with no errors
    - Visit `http://localhost:4321/` (after `pnpm dev`) — CFP section visible below Key Numbers
    - Visit `http://localhost:4321/en/` — CFP section visible below Key Numbers in English
  </acceptance_criteria>
  <done>Both homepages wire in CfpSection between KeyNumbers and closing main; build is green.</done>
</task>

</tasks>

<verification>
- CfpSection.astro renders correctly in all three states (test by temporarily editing CFP_OPENS / CFP_CLOSES in src/lib/cfp.ts to force each state, then reverting)
- Homepages (FR + EN) show the section below Key Numbers
- No raw hex, no hardcoded strings
- Astro build passes
</verification>

<success_criteria>
EVNT-04 satisfied for the open/coming-soon states. Staff flip the section to a different state by editing two dates in `src/lib/cfp.ts` and redeploying — single point of edit per D-02.
</success_criteria>

<output>
Create `.planning/phases/08-event-lifecycle/08-03-SUMMARY.md`: component path, state-to-CSS mapping recap, which state the site currently renders with the placeholder dates, how staff flip states.
</output>
