---
phase: 08-event-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-event-lifecycle/stitch/CFP-coming-soon.png
  - .planning/phases/08-event-lifecycle/stitch/CFP-open.png
  - .planning/phases/08-event-lifecycle/stitch/CFP-closed.png
  - .planning/phases/08-event-lifecycle/stitch/Replays-populated-desktop.png
  - .planning/phases/08-event-lifecycle/stitch/Replays-empty.png
  - .planning/phases/08-event-lifecycle/stitch/Replays-mobile.png
  - .planning/phases/08-event-lifecycle/08-02-SUMMARY.md
autonomous: false
requirements:
  - EVNT-01
  - EVNT-04
must_haves:
  truths:
    - "User has reviewed Stitch output for CFP section (all three states) before any component code is written"
    - "User has reviewed Stitch output for /replays page (populated + empty + mobile) before any component code is written"
    - "Stitch prompts referenced only DS token names — no raw hex values"
  artifacts:
    - path: ".planning/phases/08-event-lifecycle/stitch/"
      provides: "Stitch PNG exports or screenshots for CFP states + /replays layouts + mobile"
      contains: "CFP-open, CFP-closed, CFP-coming-soon, Replays-populated, Replays-empty, Replays-mobile"
  key_links:
    - from: "Stitch design validation"
      to: "08-03 (CFP section component) and 08-04 (/replays page)"
      via: "user approval recorded in 08-02-SUMMARY.md"
      pattern: "approved"
---

<objective>
Stitch-first gate. Per CLAUDE.md §Design Rules and CONTEXT D-13, all new significant UI in this phase (CFP homepage section + /replays page) must be designed in Google Stitch and validated by the user before any component code is written.

This plan has ONE task: a human-action checkpoint that produces six Stitch screens, the user reviews them, and implementation is blocked until approval. It gates 08-03 and 08-04.

Output: Stitch exports committed to `.planning/phases/08-event-lifecycle/stitch/` + a SUMMARY recording approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DESIGN.md
@.planning/phases/08-event-lifecycle/08-UI-SPEC.md
@.planning/phases/08-event-lifecycle/08-CONTEXT.md
@/home/smana/.claude/projects/-home-smana-Sources-cndfrance-website/memory/reference_stitch.md
@/home/smana/.claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_ds_tokens.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Produce Stitch screens for CFP states + /replays (populated/empty/mobile) and obtain user approval</name>
  <files>.planning/phases/08-event-lifecycle/stitch/ (six PNGs)</files>
  <read_first>
    - .planning/phases/08-event-lifecycle/08-UI-SPEC.md (full — this is the token contract the Stitch prompt must honor)
    - /home/smana/.claude/projects/-home-smana-Sources-cndfrance-website/memory/reference_stitch.md (Stitch project and DS IDs)
    - /home/smana/.claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_ds_tokens.md (token-only rule)
    - CLAUDE.md (Stitch-first rule)
  </read_first>
  <what-built>
    The UI-SPEC (08-UI-SPEC.md) is the design contract. The Stitch pass translates that spec into visual designs the user can react to before code is written. Per D-13 this is mandatory — Phase 7's retroactive verification was a one-off exception, not a precedent.
  </what-built>
  <how-to-verify>
    Run the Stitch design flow for these six screens, using the CND France 2027 Stitch project + DS referenced in `reference_stitch.md`. Every Stitch prompt MUST reference DS token role names only (bg-card, text-muted-foreground, border-primary, accent, primary, etc.) — NEVER raw hex. DESIGN.md is the source of truth; Stitch must not invent colors.

    Required screens:

    1. **CFP section — coming-soon state** (homepage, desktop viewport 1440×900): full-width section between Key Numbers and Footer. Muted pill ("Coming soon" / "Bientôt ouvert"), section headline `cfp.heading`, description paragraph `cfp.description.coming_soon`, ghost-or-hidden CTA. Tokens: bg-background, text-muted-foreground for pill, --text-2xl weight 700 for headline.
    2. **CFP section — open state** (desktop 1440×900): accent-pink pill ("CFP open" / "CFP ouvert"), description `cfp.description.open`, accent button "Submit a talk" / "Soumettre une conférence" with `--shadow-glow-accent`, meta row "Closes April 30, 2027". Tokens: bg-accent pill, accent button with glow.
    3. **CFP section — closed state** (desktop 1440×900): two variants on one canvas — (a) fully hidden (represent as crossed-out placeholder showing Key Numbers → Footer with no gap), (b) muted-only no-CTA note "CFP closed — see you June 3, 2027" / "CFP clôturé — Rendez-vous le 3 juin 2027". Tokens: bg-muted pill, muted-foreground text.
    4. **/replays page — populated state** (desktop 1440×900): page header with H1 "Replays" (--text-4xl weight 700), lead paragraph, "Back to schedule" ghost link. Three track groups with sub-headings (--text-2xl weight 700) and 4px left border in trackColor() hue. Each group: 3-column grid of recording cards. Card: track badge pill, title (--text-base weight 700, 2-line clamp), speakers line (--text-xs muted), Play icon indicator in a 32px circle (bg-primary @ 15%), whole-card clickable link. Hover state visible on one card.
    5. **/replays page — empty state** (desktop 1440×900): centered block with `replays.empty.heading` + `replays.empty.body`, muted colors, optional ghost "Back to home" link.
    6. **/replays page — mobile viewport** (390×844): single-column card grid, 16px gap, track group sub-headings stack above their respective card columns, "Back to schedule" link visible at top.

    For all six: dark theme (bg-background #0e0a24), DM Sans, DS tokens only.

    Export each screen as PNG to `.planning/phases/08-event-lifecycle/stitch/` with filenames matching `files_modified` above.

    Then present the six screens to the user and ask: "Do these match the spec? Any visual changes before I code?" Capture the response in `08-02-SUMMARY.md` with status `approved` or `revisions-requested`.
  </how-to-verify>
  <resume-signal>Type "approved" to unblock 08-03 and 08-04, or describe requested changes.</resume-signal>
  <acceptance_criteria>
    - Six PNG files exist in `.planning/phases/08-event-lifecycle/stitch/` with the exact names listed in files_modified
    - `.planning/phases/08-event-lifecycle/08-02-SUMMARY.md` contains the word "approved" on a line documenting user sign-off
    - No raw hex values appear in the Stitch prompts (if prompts are logged to SUMMARY, verify via `grep -E "#[0-9a-fA-F]{6}" 08-02-SUMMARY.md` returning nothing)
  </acceptance_criteria>
  <done>All six Stitch screens exported, user has typed "approved", SUMMARY records approval. 08-03 and 08-04 are unblocked.</done>
</task>

</tasks>

<verification>
- Six PNGs committed under `.planning/phases/08-event-lifecycle/stitch/`
- 08-02-SUMMARY.md records user approval verbatim
- No raw hex colors in Stitch prompts (token names only)
</verification>

<success_criteria>
User has seen the CFP section in all three lifecycle states and the /replays page in three variants (populated/empty/mobile) before a single line of component code is written. This honors the Stitch-first rule in CLAUDE.md and D-13.
</success_criteria>

<output>
Create `.planning/phases/08-event-lifecycle/08-02-SUMMARY.md` with: Stitch project link, list of exported screens, user feedback verbatim, approval status, and any callouts for 08-03 / 08-04 executors.
</output>
