---
phase: 11-security-i18n-hardcode-fixes
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/speakers/SpeakerCard.astro
  - src/components/hero/CountdownTimer.tsx
  - src/components/speakers/TalkCard.astro
  - "src/pages/speakers/[slug].astro"
  - "src/pages/en/speakers/[slug].astro"
autonomous: true
requirements: [IN-01, IN-02, WR-01, keynote_badge, schedule_link]
tags: [i18n, components, strings]
must_haves:
  truths:
    - "SpeakerCard renders the localized keynote badge (FR speaker page shows 'Keynote' sourced from t(), not a literal)"
    - "CountdownTimer aria-label contains no hardcoded English word outside the EN dictionary — FR page aria-label starts with 'Plus que'"
    - "TalkCard emits t(`talks.track.${track}`) and t(`schedule.format.${format}`) — no trackNames map, no capitalizedFormat local"
    - "[slug].astro files derive lang from getLangFromUrl(Astro.url); no `const lang: \"fr\" = \"fr\"` or `: \"en\" = \"en\"` literal remains"
    - "[slug].astro talk row emits t(`schedule.format.${talk.format}`) — no .charAt(0).toUpperCase() format capitalization"
    - "[slug].astro talk link anchor text uses t('speakers.schedule_link') — the real link label, not the placeholder"
  artifacts:
    - path: "src/components/speakers/SpeakerCard.astro"
      provides: "Keynote badge rendered via t('speakers.keynote_badge')"
    - path: "src/components/hero/CountdownTimer.tsx"
      provides: "Locale-safe aria-label built from countdown.aria_template + placeholder replacement"
    - path: "src/components/speakers/TalkCard.astro"
      provides: "Track + format badges sourced from i18n; no inline English maps"
    - path: "src/pages/speakers/[slug].astro"
      provides: "Dynamic speaker page with getLangFromUrl-derived lang + localized talk format"
    - path: "src/pages/en/speakers/[slug].astro"
      provides: "EN mirror with same getLangFromUrl pattern"
  key_links:
    - from: "src/components/speakers/SpeakerCard.astro"
      to: "src/i18n/ui.ts speakers.keynote_badge"
      via: "useTranslations(lang)"
      pattern: "t\\(\"speakers.keynote_badge\"\\)"
    - from: "src/components/hero/CountdownTimer.tsx"
      to: "src/i18n/ui.ts countdown.aria_template"
      via: "direct ui[lang] dictionary access + String.replace chain"
      pattern: "countdown\\.aria_template"
    - from: "src/components/speakers/TalkCard.astro"
      to: "src/i18n/ui.ts talks.track.* and schedule.format.*"
      via: "useTranslations(lang) with templated key"
      pattern: "t\\(`talks\\.track\\."
    - from: "src/pages/speakers/[slug].astro"
      to: "src/i18n/utils.ts getLangFromUrl"
      via: "Astro.url → Locale"
      pattern: "getLangFromUrl\\(Astro\\.url\\)"
---

<objective>
Replace every remaining hardcoded locale-dependent string in Speaker/Countdown/Talk components and the dynamic speaker pages with calls through the extended i18n dictionary from Plan 01. Also fix the residual SC #3 violation: `const lang: "fr" = "fr"` in `[slug].astro` (both FR and EN variants) is replaced with `getLangFromUrl(Astro.url)` per user D-02.

Finally, upgrade the inline talk row in `[slug].astro` from `talk.format.charAt(0).toUpperCase() + talk.format.slice(1)` to `t(`schedule.format.${talk.format}`)`, and replace the internal "→" arrow hint anchor with a text label sourced from `t("speakers.schedule_link")` so visitors see "Voir dans le programme" / "View in schedule" on hover/screen readers (per user D-01 — the link already deep-links to `#session-${talk.id}`, the existing code just lacks the localized label).

Purpose: Close SC #2, IN-01 (residual), IN-02, WR-01, keynote_badge, schedule_link. Closes the integration-gap cluster from v1.0 audit.
Output: Five files updated; zero hardcoded English strings remain in these components/pages outside their EN dictionary counterparts.

**Stitch-first exemption** (re-stated): String swaps, no layout change. Per CLAUDE.md the rule applies to "significant UI change" — not applicable here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-security-i18n-hardcode-fixes/11-RESEARCH.md
@.planning/phases/11-security-i18n-hardcode-fixes/11-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes. All keys below are created by Plan 01. -->

From src/i18n/ui.ts (after Plan 01 merge):
```
speakers.keynote_badge          (existing — "Keynote" in both locales)
countdown.days / hours / minutes / seconds   (existing)
countdown.aria_template         (NEW, from Plan 01 — has {days}{daysLabel}{hours}{hoursLabel}{minutes}{minutesLabel} placeholders)
talks.track.cloud-infra         (NEW — English verbatim both locales per D-03)
talks.track.devops-platform     (NEW — English verbatim)
talks.track.security            (NEW — English verbatim)
talks.track.community           (NEW — English verbatim)
schedule.format.keynote / talk / lightning / workshop   (existing)
speakers.schedule_link          (existing — "Voir dans le programme" / "View in schedule")
```

From src/i18n/utils.ts:
```typescript
export function getLangFromUrl(url: URL): Locale;                // extracts locale from first pathname segment; defaults to "fr"
export function useTranslations(lang: Locale): (key) => string;  // typed against FR branch, falls back to FR if missing in target locale
export function getLocalePath(lang: Locale, path: string): string;
```

Existing consumer pattern (already correct in several files — mimic this): in the Astro frontmatter fence (the leading `---` and trailing `---` lines of any .astro file) include these three imports/assignments:

    import { useTranslations, getLangFromUrl } from "@/i18n/utils";
    const lang = getLangFromUrl(Astro.url);
    const t = useTranslations(lang);

Existing talk→session deep-link pattern in `[slug].astro` (already in place, lines 69 of both files):
```astro
href={`${getLocalePath(lang, "/programme")}#session-${talk.id}`}
```
This confirms `talk.id === sessionId` — same string, no mapping layer needed. Per user D-01 we just need to make the link's accessible label/hover use `t("speakers.schedule_link")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire speakers.keynote_badge into SpeakerCard.astro</name>
  <files>src/components/speakers/SpeakerCard.astro</files>
  <read_first>
    - src/components/speakers/SpeakerCard.astro (current file — 40 lines; hardcoded "Keynote" on line 35)
    - src/i18n/utils.ts (useTranslations signature)
    - src/components/speakers/TalkCard.astro lines 1-16 (reference pattern: import + `const t = useTranslations(lang)` in frontmatter)
  </read_first>
  <behavior>
    - Given `isKeynote=true` and `lang="fr"`, the rendered span's text content MUST equal `ui.fr["speakers.keynote_badge"]` ("Keynote").
    - Given `isKeynote=true` and `lang="en"`, the span's text content MUST equal `ui.en["speakers.keynote_badge"]` ("Keynote" — coincidentally identical value).
    - Given `isKeynote=false`, no badge span is emitted (existing behavior preserved).
  </behavior>
  <action>
  1. Add `useTranslations` to the existing named import from `@/i18n/utils` on line 3:
     Change: `import { getLocalePath } from "@/i18n/utils";`
     To:     `import { getLocalePath, useTranslations } from "@/i18n/utils";`
  2. After the existing destructuring on line 16 (`const { name, company, slug, photo, isKeynote, lang } = Astro.props;`), add a new line:
     `const t = useTranslations(lang);`
  3. On line 35, replace the literal text `Keynote` with `{t("speakers.keynote_badge")}`:
     - Before: `<span class="...">Keynote</span>` (literal text child)
     - After:  `<span class="...">{t("speakers.keynote_badge")}</span>`
  4. Do not change any class names, any other markup, or the `isKeynote &&` conditional.

  No new props; `lang: Locale` is already in the Props interface (line 13).
  </action>
  <acceptance_criteria>
    - `grep -n 'import { getLocalePath, useTranslations } from "@/i18n/utils"' src/components/speakers/SpeakerCard.astro` returns exactly 1 match
    - `grep -n 'const t = useTranslations(lang);' src/components/speakers/SpeakerCard.astro` returns exactly 1 match
    - `grep -n 't("speakers.keynote_badge")' src/components/speakers/SpeakerCard.astro` returns exactly 1 match
    - `grep -Pn '>\s*Keynote\s*<' src/components/speakers/SpeakerCard.astro` returns zero matches (no literal "Keynote" between tags)
    - `pnpm astro check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website &amp;&amp; grep -q 'import { getLocalePath, useTranslations } from "@/i18n/utils"' src/components/speakers/SpeakerCard.astro &amp;&amp; grep -q 'const t = useTranslations(lang);' src/components/speakers/SpeakerCard.astro &amp;&amp; grep -q 't("speakers.keynote_badge")' src/components/speakers/SpeakerCard.astro &amp;&amp; ! grep -Pq '>\s*Keynote\s*<' src/components/speakers/SpeakerCard.astro &amp;&amp; pnpm astro check</automated>
  </verify>
  <done>Keynote badge in SpeakerCard sourced from i18n; hardcoded English literal removed; astro check green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace CountdownTimer hardcoded "remaining" with locale-safe template</name>
  <files>src/components/hero/CountdownTimer.tsx</files>
  <read_first>
    - src/components/hero/CountdownTimer.tsx (full file — template literal on line 46 contains hardcoded English "remaining")
    - src/i18n/ui.ts lines corresponding to countdown.aria_template (both locales — added by Plan 01)
    - .planning/phases/11-security-i18n-hardcode-fixes/11-RESEARCH.md Pattern 2 (templated aria-label)
  </read_first>
  <behavior>
    - For lang="en" with 45 days / 3 hours / 10 minutes: aria-label MUST equal `"45 days, 3 hours, 10 minutes remaining"`.
    - For lang="fr" with 45 jours / 3 heures / 10 minutes: aria-label MUST equal `"Plus que 45 jours, 3 heures et 10 minutes"`.
    - No hardcoded word `remaining` MUST exist in the source file anymore.
  </behavior>
  <action>
  Inside the `updateAriaLabel` closure (currently lines 40-49), replace the single `setAriaLabel(...)` call that uses string concatenation with a template-based approach:

  Current code to replace (lines 44-47):
  ```ts
  setAriaLabel(
    `${tl.days} ${t("countdown.days")}, ${tl.hours} ${t("countdown.hours")}, ${tl.minutes} ${t("countdown.minutes")} remaining`
  );
  ```

  Replace with:
  ```ts
  const tpl = t("countdown.aria_template");
  const label = tpl
    .replace("{days}", String(tl.days))
    .replace("{daysLabel}", t("countdown.days"))
    .replace("{hours}", String(tl.hours))
    .replace("{hoursLabel}", t("countdown.hours"))
    .replace("{minutes}", String(tl.minutes))
    .replace("{minutesLabel}", t("countdown.minutes"));
  setAriaLabel(label);
  ```

  Note: the local `t` function (defined on line 36-37) is typed loosely as `(key: string) => string` — so calling `t("countdown.aria_template")` needs no type widening. Do not touch the rest of the component.
  </action>
  <acceptance_criteria>
    - `grep -q '" remaining"' src/components/hero/CountdownTimer.tsx` returns nothing (exit 1)
    - `grep -q 'countdown.aria_template' src/components/hero/CountdownTimer.tsx` returns a match
    - `grep -q '{daysLabel}' src/components/hero/CountdownTimer.tsx` returns a match (string literal placeholder for .replace)
    - `pnpm astro check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website &amp;&amp; ! grep -q '" remaining"' src/components/hero/CountdownTimer.tsx &amp;&amp; grep -q 'countdown.aria_template' src/components/hero/CountdownTimer.tsx &amp;&amp; grep -q '{daysLabel}' src/components/hero/CountdownTimer.tsx &amp;&amp; pnpm astro check</automated>
  </verify>
  <done>CountdownTimer aria-label composed via template replacement from countdown.aria_template; no hardcoded English glue word remains in the source.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Localize track and format in TalkCard.astro via i18n keys</name>
  <files>src/components/speakers/TalkCard.astro</files>
  <read_first>
    - src/components/speakers/TalkCard.astro (full file — `trackNames` map lines 25-30, `capitalizedFormat` line 32, usages lines 49 and 52)
    - src/i18n/ui.ts talks.track.* keys (added by Plan 01) and existing schedule.format.* keys
    - src/components/schedule/ScheduleGrid.astro line 168 (reference pattern: `t(`schedule.format.${fmt}` as any)`)
  </read_first>
  <behavior>
    - For `track="security"` / `lang="fr"`: badge text equals `ui.fr["talks.track.security"]` (= "Security" per D-03 — English verbatim).
    - For `format="keynote"` / `lang="en"`: format label equals `ui.en["schedule.format.keynote"]` (= "Keynote").
    - The file no longer contains a `trackNames` identifier or a `capitalizedFormat` identifier.
    - `trackColors` map stays (styling only, not a translation).
  </behavior>
  <action>
  In `src/components/speakers/TalkCard.astro`:

  1. Delete the `trackNames` const declaration entirely (lines 25-30 of current file).
  2. Delete the `capitalizedFormat` const declaration (line 32 of current file).
  3. In the markup at line 49, change `{trackNames[track]}` to `{t(`talks.track.${track}` as any)}`.
  4. In the markup at line 52, change `{capitalizedFormat} - {duration}min` to `{t(`schedule.format.${format}` as any)} - {duration}min`.

  Leave `trackColors` (lines 18-23), the Props interface, imports, and all other markup untouched. `t` is already in scope (line 16: `const t = useTranslations(lang);`).

  Per D-03: track values in ui.ts are English verbatim in both locales; this task only changes the data flow (map → i18n), producing no visible change on EN pages and only namespace consistency on FR pages.
  </action>
  <acceptance_criteria>
    - `grep -q 'trackNames' src/components/speakers/TalkCard.astro` returns nothing (exit 1)
    - `grep -q 'capitalizedFormat' src/components/speakers/TalkCard.astro` returns nothing (exit 1)
    - `grep -q 't(`talks.track.' src/components/speakers/TalkCard.astro` returns a match
    - `grep -q 't(`schedule.format.' src/components/speakers/TalkCard.astro` returns a match
    - `grep -q 'trackColors' src/components/speakers/TalkCard.astro` still returns a match (styling preserved)
    - `pnpm astro check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website &amp;&amp; ! grep -q 'trackNames' src/components/speakers/TalkCard.astro &amp;&amp; ! grep -q 'capitalizedFormat' src/components/speakers/TalkCard.astro &amp;&amp; grep -q 't(`talks.track.' src/components/speakers/TalkCard.astro &amp;&amp; grep -q 't(`schedule.format.' src/components/speakers/TalkCard.astro &amp;&amp; grep -q 'trackColors' src/components/speakers/TalkCard.astro &amp;&amp; pnpm astro check</automated>
  </verify>
  <done>TalkCard uses typed i18n lookups for both track and format labels; inline English maps removed; trackColors styling preserved; astro check green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix FR [slug].astro — derive lang, localize talk format, add schedule link aria/label</name>
  <files>src/pages/speakers/[slug].astro</files>
  <read_first>
    - "src/pages/speakers/[slug].astro" (full file — hardcoded `const lang: "fr" = "fr"` on line 21; `getTalksForSpeaker("fr", slug)` on line 24; inline `talk.format.charAt(0).toUpperCase() + talk.format.slice(1)` on line 77; existing deep-link already on line 69)
    - src/i18n/utils.ts (getLangFromUrl signature — returns Locale)
    - .planning/phases/11-security-i18n-hardcode-fixes/11-RESEARCH.md Open Question 2 resolution (D-02: fix it)
  </read_first>
  <behavior>
    - The `lang` const is derived from `Astro.url`, typed as `Locale`.
    - `getTalksForSpeaker` is called with the derived `lang`, not the literal "fr".
    - Talk row format label comes from `t(`schedule.format.${talk.format}`)` — no `.charAt(0).toUpperCase()` capitalization.
    - The existing `<a href={`${getLocalePath(lang, "/programme")}#session-${talk.id}`}>` gains an `aria-label` with the value `t("speakers.schedule_link")` so the screen-reader-announced purpose is localized and explicit (per D-01, the real href was already in place).
    - Page still builds for every speaker and renders correctly for FR routes.
  </behavior>
  <action>
  In `src/pages/speakers/[slug].astro`:

  1. Update the import line 10 to add `getLangFromUrl`:
     - Before: `import { useTranslations, getLocalePath } from "@/i18n/utils";`
     - After:  `import { useTranslations, getLocalePath, getLangFromUrl } from "@/i18n/utils";`

  2. Replace line 21:
     - Before: `const lang: "fr" = "fr";`
     - After:  `const lang = getLangFromUrl(Astro.url);`
     (Drop the literal-type assertion; `getLangFromUrl` returns `Locale`.)

  3. On line 24, swap the literal argument for the variable:
     - Before: `const talks = await getTalksForSpeaker("fr", slug);`
     - After:  `const talks = await getTalksForSpeaker(lang, slug);`

  4. On line 77 (inside the inline talk row `<p>`), replace the capitalized format with the i18n lookup:
     - Before: `{talk.format.charAt(0).toUpperCase() + talk.format.slice(1)} · {talk.durationMin} min`
     - After:  `{t(`schedule.format.${talk.format}` as any)} · {talk.durationMin} min`

  5. On the existing `<a>` element (line 68-71) that deep-links to `/programme#session-${talk.id}`, add an `aria-label={t("speakers.schedule_link")}` attribute between the `href` and `class` attributes. Keep the arrow "→" decoration as-is (it's `aria-hidden="true"` already on line 74).
     - Before: `<a\n  href={`${getLocalePath(lang, "/programme")}#session-${talk.id}`}\n  class="group block ..."\n>`
     - After:  `<a\n  href={`${getLocalePath(lang, "/programme")}#session-${talk.id}`}\n  aria-label={t("speakers.schedule_link")}\n  class="group block ..."\n>`

  Per D-01: the href was already wired; we're only adding the localized aria label that surfaces `t("speakers.schedule_link")` ("Voir dans le programme" / "View in schedule"), satisfying the `schedule_link` requirement.
  Per D-02: the literal lang constant is removed in favor of `getLangFromUrl`, satisfying SC #3 literally.

  Do not modify any other line, any class names, or the `talks.length > 0 ? ... : ...` conditional.
  </action>
  <acceptance_criteria>
    - `grep -E 'const lang(: "fr")? = "fr"' src/pages/speakers/\[slug\].astro` returns nothing (exit 1)
    - `grep -q 'const lang = getLangFromUrl(Astro.url);' src/pages/speakers/\[slug\].astro` returns a match
    - `grep -q 'getTalksForSpeaker(lang, slug)' src/pages/speakers/\[slug\].astro` returns a match
    - `grep -q 'getTalksForSpeaker("fr"' src/pages/speakers/\[slug\].astro` returns nothing (exit 1)
    - `grep -q 't(`schedule.format.' src/pages/speakers/\[slug\].astro` returns a match
    - `grep -q 'charAt(0).toUpperCase()' src/pages/speakers/\[slug\].astro` returns nothing (exit 1)
    - `grep -q 'aria-label={t("speakers.schedule_link")}' src/pages/speakers/\[slug\].astro` returns a match
    - `grep -q '#session-' src/pages/speakers/\[slug\].astro` still returns a match (deep-link preserved)
    - `pnpm astro check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website &amp;&amp; ! grep -E 'const lang(: "fr")? = "fr"' 'src/pages/speakers/[slug].astro' &amp;&amp; grep -q 'const lang = getLangFromUrl(Astro.url);' 'src/pages/speakers/[slug].astro' &amp;&amp; grep -q 'getTalksForSpeaker(lang, slug)' 'src/pages/speakers/[slug].astro' &amp;&amp; ! grep -q 'getTalksForSpeaker("fr"' 'src/pages/speakers/[slug].astro' &amp;&amp; grep -q 't(`schedule.format.' 'src/pages/speakers/[slug].astro' &amp;&amp; ! grep -q 'charAt(0).toUpperCase()' 'src/pages/speakers/[slug].astro' &amp;&amp; grep -q 'aria-label={t("speakers.schedule_link")}' 'src/pages/speakers/[slug].astro' &amp;&amp; grep -q '#session-' 'src/pages/speakers/[slug].astro' &amp;&amp; pnpm astro check</automated>
  </verify>
  <done>FR speaker [slug].astro: lang derived from URL, format label localized, schedule link labeled with i18n key, deep-link preserved; astro check green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Mirror fixes to EN /en/speakers/[slug].astro</name>
  <files>src/pages/en/speakers/[slug].astro</files>
  <read_first>
    - "src/pages/en/speakers/[slug].astro" (current file — structure is identical to FR variant but hardcodes `const lang: "en" = "en"` on line 21 and `getTalksForSpeaker("en", slug)` on line 24; same format capitalization on line 77)
    - "src/pages/speakers/[slug].astro" AFTER Task 4 (apply the identical diff, only differing by the literal that was there before)
  </read_first>
  <behavior>
    - Same five behaviors as Task 4 but on the EN-mirror page.
    - Importantly: after the change, the two files are structurally identical in their language-handling code (both derive `lang` from URL), simplifying future maintenance.
  </behavior>
  <action>
  Apply the same five edits from Task 4 to `src/pages/en/speakers/[slug].astro`:

  1. Add `getLangFromUrl` to the import from `@/i18n/utils` on line 10.
  2. Line 21: `const lang: "en" = "en";` → `const lang = getLangFromUrl(Astro.url);`
  3. Line 24: `getTalksForSpeaker("en", slug)` → `getTalksForSpeaker(lang, slug)`
  4. Line 77: `{talk.format.charAt(0).toUpperCase() + talk.format.slice(1)} · {talk.durationMin} min` → `{t(`schedule.format.${talk.format}` as any)} · {talk.durationMin} min`
  5. Add `aria-label={t("speakers.schedule_link")}` attribute to the `<a>` element (lines 68-71).

  Do not touch anything else.
  </action>
  <acceptance_criteria>
    - `grep -E 'const lang(: "en")? = "en"' src/pages/en/speakers/\[slug\].astro` returns nothing (exit 1)
    - `grep -q 'const lang = getLangFromUrl(Astro.url);' src/pages/en/speakers/\[slug\].astro` returns a match
    - `grep -q 'getTalksForSpeaker(lang, slug)' src/pages/en/speakers/\[slug\].astro` returns a match
    - `grep -q 'getTalksForSpeaker("en"' src/pages/en/speakers/\[slug\].astro` returns nothing (exit 1)
    - `grep -q 't(`schedule.format.' src/pages/en/speakers/\[slug\].astro` returns a match
    - `grep -q 'charAt(0).toUpperCase()' src/pages/en/speakers/\[slug\].astro` returns nothing (exit 1)
    - `grep -q 'aria-label={t("speakers.schedule_link")}' src/pages/en/speakers/\[slug\].astro` returns a match
    - Cross-file: `diff <(grep -E 'lang|getTalksForSpeaker' src/pages/speakers/\[slug\].astro) <(grep -E 'lang|getTalksForSpeaker' src/pages/en/speakers/\[slug\].astro)` produces no differences (both files now handle locale identically)
    - `rg -n "const lang: \"(fr|en)\" = \"(fr|en)\"" src/pages/` returns zero matches (SC #3 literal compliance)
    - `pnpm astro check` exits 0
    - `pnpm build` exits 0 (full build validates getStaticPaths still works for every speaker in both FR and EN)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website &amp;&amp; ! grep -E 'const lang(: "en")? = "en"' 'src/pages/en/speakers/[slug].astro' &amp;&amp; grep -q 'const lang = getLangFromUrl(Astro.url);' 'src/pages/en/speakers/[slug].astro' &amp;&amp; grep -q 'getTalksForSpeaker(lang, slug)' 'src/pages/en/speakers/[slug].astro' &amp;&amp; ! grep -q 'getTalksForSpeaker("en"' 'src/pages/en/speakers/[slug].astro' &amp;&amp; grep -q 't(`schedule.format.' 'src/pages/en/speakers/[slug].astro' &amp;&amp; ! grep -q 'charAt(0).toUpperCase()' 'src/pages/en/speakers/[slug].astro' &amp;&amp; grep -q 'aria-label={t("speakers.schedule_link")}' 'src/pages/en/speakers/[slug].astro' &amp;&amp; [ -z "$(rg -n 'const lang: \"(fr|en)\" = \"(fr|en)\"' src/pages/ 2>/dev/null)" ] &amp;&amp; pnpm astro check &amp;&amp; pnpm build</automated>
  </verify>
  <done>EN [slug].astro mirrors FR changes; SC #3 is literally satisfied across all pages under src/pages/; full build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content collection → Astro frontmatter | `talk.id`, `talk.format`, `talk.track` originate from YAML/markdown content collections validated by Zod (src/content.config.ts). No runtime user input crosses into the rendered output. |
| Astro frontmatter → HTML | `{expr}` interpolation auto-escapes. aria-label strings go through React/Astro attribute serialization which escapes quotes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering (XSS via deep-link anchor) | `[slug].astro` `href={`...#session-${talk.id}`}` | accept | `talk.id` is content-sourced, Zod-validated at build time. Anchor fragment is passed to Astro `{expr}` which URL-encodes reserved chars. No runtime user input. |
| T-11-04 | Tampering (XSS via i18n value containing HTML) | aria-label output of t("speakers.schedule_link") | mitigate | Astro auto-escapes attribute values; the i18n dict contains plain text only. Any future translator who pastes HTML into ui.ts would have it rendered as literal text, not parsed. |
| T-11-05 | Elevation (open redirect via schedule link) | `#session-${talk.id}` fragment on same-origin href | accept | Anchor is a fragment on an already-built-in internal route (`/programme`). No external URL construction. |
</threat_model>

<verification>
After this plan merges:
- Visual manual smoke (1 min): load `/`, `/en/`, `/speakers/<any>`, `/en/speakers/<any>` in browser; confirm:
  - Countdown aria-label reads naturally in both locales (devtools inspect `[role="timer"]`)
  - A keynote speaker's card shows the Keynote pill
  - A speaker profile talk row says `Keynote · 30 min` / `Keynote · 30 min` (same EN text both locales per D-03 on track; format is also "Keynote" in both for this format)
  - Hovering the talk link shows `aria-label="Voir dans le programme"` (FR) / `"View in schedule"` (EN) in devtools
- Automated: `pnpm astro check && pnpm build` exit 0
- Grep audit: `rg -n "const lang: \"(fr|en)\" = \"(fr|en)\"" src/` returns zero matches
</verification>

<success_criteria>
- SC #2 for these surfaces: all targeted hardcoded strings replaced with i18n lookups
- SC #3 literal: `rg -n "const lang: \"(fr|en)\" = \"(fr|en)\"" src/pages/` returns zero (D-02 satisfied)
- `schedule_link` requirement: aria-label sourced from `t("speakers.schedule_link")` on the real deep-link anchor (D-01 satisfied)
- `keynote_badge` requirement: SpeakerCard renders via `t("speakers.keynote_badge")`
- WR-01: CountdownTimer contains no hardcoded word `remaining` outside the EN dictionary
- IN-02: TalkCard uses typed i18n key lookups for track and format
- Full `pnpm build` green
</success_criteria>

<output>
After completion, create `.planning/phases/11-security-i18n-hardcode-fixes/11-02-SUMMARY.md`
</output>
