---
phase: 13-speaker-schema-drift-cleanup
plan: 02
type: tdd
wave: 2
depends_on: ["13-01"]
files_modified:
  - tests/build/speaker-profile.test.ts
  - tests/build/speaker-talks.test.ts
  - src/lib/__tests__/speakers.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "tests/build/speaker-profile.test.ts passes against real speakers from src/content/schedule/speakers.csv (Phase 13 success criterion #2)"
    - "tests/build/speaker-talks.test.ts passes against the real co-speaker pair arthur-outhenin-chalandre + quentin-swiech from session S3SPP8 (Phase 13 success criterion #3)"
    - "src/lib/__tests__/speakers.test.ts is replaced wholesale by ~30 lines of minimal coverage targeting the actual exports of src/lib/speakers.ts (no getLocale, no astro:content mock for non-existent 'talks' collection)"
    - "All three test files anchor on a documented set of real speakers — header comment lists chosen anchors so future data-ops can spot-check before removing rows (D-10)"
    - "Bidirectional co-speaker assertions (SPKR-03) still hold: each anchor's profile page references the other anchor by name, and each links to the other's profile URL"
    - "Profile-page assertion (SPKR-02) still holds: every anchor speaker has a built FR + EN profile page containing their name and back-navigation"
    - "`pnpm vitest run` exits 0 across all three files after `pnpm build`"
  artifacts:
    - path: "tests/build/speaker-profile.test.ts"
      provides: "SPKR-02 build-output coverage anchored on real CFP speakers"
      contains: "SPKR-02"
    - path: "tests/build/speaker-talks.test.ts"
      provides: "SPKR-03 bidirectional co-speaker coverage anchored on session S3SPP8"
      contains: "SPKR-03"
    - path: "src/lib/__tests__/speakers.test.ts"
      provides: "Minimal unit coverage of getSlug, getCoSpeakersForTalk, getPrimaryTalk (~30 lines, no stale fixture mocks)"
      contains: "getPrimaryTalk"
  key_links:
    - from: "tests/build/speaker-profile.test.ts"
      to: "dist/speakers/{anchor}/index.html"
      via: "readFileSync after pnpm build"
      pattern: "readFileSync.*dist/speakers"
    - from: "tests/build/speaker-talks.test.ts"
      to: "dist/speakers/arthur-outhenin-chalandre/index.html and dist/speakers/quentin-swiech/index.html"
      via: "readFileSync of both pages, assert each contains the other's name and a link to the other's URL"
      pattern: "Quentin Swiech|Arthur Outhenin"
    - from: "src/lib/__tests__/speakers.test.ts"
      to: "src/lib/speakers.ts (getSlug, getCoSpeakersForTalk, getPrimaryTalk)"
      via: "direct import — no astro:content mock needed because these helpers are pure"
      pattern: "from \"\\.\\./speakers\""
---

<objective>
Rewrite the two failing build-output test suites and the stale unit-test file so that all three pass against the current 65-speaker CFP roster (per D-08 REVISED Option B + D-09 + D-10). No production code changes — pure test rewrite.

Purpose: Closes Phase 13 success criteria #2 and #3 (build tests must pass). Eliminates the fixture-identity drift that caused both build-output suites to crash before reaching their real assertions, and the stale `getLocale` / non-existent `talks` collection mocks in the unit-test file.

Output: Three test files anchored on real, documented speakers. Each file's header comment lists the chosen anchor speakers so future data-ops can spot-check before removing CSV rows. Wholesale replacement of `src/lib/__tests__/speakers.test.ts` with a focused ~30-line coverage of the helpers actually exported by `src/lib/speakers.ts` (which Plan 13-01 now includes `getPrimaryTalk` + the hardened `getCoSpeakersForTalk`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-speaker-schema-drift-cleanup/13-CONTEXT.md
@.planning/phases/13-speaker-schema-drift-cleanup/13-RESEARCH.md
@.planning/phases/13-speaker-schema-drift-cleanup/13-01-SUMMARY.md
@CLAUDE.md
@tests/build/speaker-profile.test.ts
@tests/build/speaker-talks.test.ts
@src/lib/__tests__/speakers.test.ts
@src/lib/speakers.ts
@src/content/schedule/speakers.csv
@src/content/schedule/sessions.csv
@src/i18n/ui.ts

<interfaces>
<!-- Plan 13-01 added these. The unit test file rewrite must target them, not the deleted exports. -->

From src/lib/speakers.ts (POST 13-01):
```ts
export function getSlug(entryId: string): string;             // returns entryId verbatim
export async function getAllSpeakers();
export async function getSpeakersByLocale(_locale: Locale);   // locale-agnostic
export async function getTalksByLocale(_locale: Locale): Promise<SessionRow[]>;
export async function getTalksForSpeaker(_locale: Locale, speakerSlug: string): Promise<SessionRow[]>;
export async function getSortedSpeakers(_locale: Locale);
export function getCoSpeakersForTalk(session: SessionRow, currentSpeakerSlug: string): string[];   // hardened (D-06)
export function getPrimaryTalk(speakerSlug: string, sessions: SessionRow[]): SessionRow | undefined;   // NEW (D-04)
```
There is NO `getLocale` export. There is NO `talks` astro content collection (sessions live in sessions.csv via loadSessions, not getCollection).

Anchor speakers chosen at planning time (verified against current CSVs):
- **petazzoni** (Jérôme Petazzoni) — KEYNOTE per speakers.csv; primary on session GJ89TV (format=keynote). Most stable anchor (event MC, will not be dropped).
- **arthur-outhenin-chalandre** (Arthur Outhenin-Chalandre) — co-speaker on session S3SPP8 with quentin-swiech ("REX Ledger - Le réseau multi-cluster…").
- **quentin-swiech** (Quentin Swiech) — co-speaker on session S3SPP8 with arthur-outhenin-chalandre. Bidirectional anchor pair for SPKR-03.
- **vache** (Aurélie Vache) — non-keynote regular speaker, present in CSV. Reserve as a "speaker exists, has a profile" anchor.

i18n keys (verified in src/i18n/ui.ts):
- FR `speakers.back` = "Retour aux speakers"
- EN `speakers.back` = "Back to speakers"
Rendered by `src/components/speakers/SpeakerProfile.astro`, so any built profile page contains the locale-appropriate string.
</interfaces>

<!-- Locked decisions referenced: D-08 REVISED Option B, D-09, D-10 (per 13-CONTEXT.md). -->
<!-- Per RESEARCH § Runtime State Inventory: `pnpm build` MUST run before the build-output tests so dist/ is fresh. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pick + document anchor speakers (D-10) and write the shared header block</name>
  <files>tests/build/_anchors.md</files>
  <action>
    Create a small reference doc `tests/build/_anchors.md` (Markdown, not a test file — vitest will ignore it). Content:

    ```markdown
    # Test Anchor Speakers (Phase 13 / D-10)

    These speakers are referenced by `tests/build/speaker-profile.test.ts` and `tests/build/speaker-talks.test.ts`.
    Before removing any of these rows from `src/content/schedule/speakers.csv` or `src/content/schedule/sessions.csv`,
    update the corresponding test assertions.

    | Slug | Name | Why anchored | Source row |
    |------|------|--------------|------------|
    | petazzoni | Jérôme Petazzoni | Keynote MC, lowest churn risk; sole speaker on keynote session GJ89TV | speakers.csv:2 |
    | arthur-outhenin-chalandre | Arthur Outhenin-Chalandre | Co-speaker pair anchor (with quentin-swiech) on session S3SPP8 | speakers.csv:9 |
    | quentin-swiech | Quentin Swiech | Co-speaker pair anchor (with arthur-outhenin-chalandre) on session S3SPP8 | speakers.csv:~ |
    | vache | Aurélie Vache | Non-keynote regular speaker; profile-existence assertion only | speakers.csv:3 |

    Selection rationale per D-10 (CONTEXT.md): keynotes are rarely dropped; canonical
    co-presented talks survive CFP churn better than solo talks. petazzoni is the
    strongest anchor because removing him would mean replanning the opening keynote.
    ```

    Each test file in Tasks 2 + 3 will reference this doc in its header comment so the link is bidirectional.

    Conventional Commits hint: `docs(13-02): document Phase 13 test anchor speakers`. No co-author line.
  </action>
  <verify>
    <automated>test -f tests/build/_anchors.md &amp;&amp; grep -q "petazzoni" tests/build/_anchors.md &amp;&amp; grep -q "arthur-outhenin-chalandre" tests/build/_anchors.md &amp;&amp; grep -q "quentin-swiech" tests/build/_anchors.md</automated>
  </verify>
  <done>tests/build/_anchors.md exists, lists the four anchor speakers with rationale per D-10, and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite tests/build/speaker-profile.test.ts against real anchors (SPKR-02)</name>
  <files>tests/build/speaker-profile.test.ts</files>
  <behavior>
    - For each anchor in {petazzoni, arthur-outhenin-chalandre, quentin-swiech, vache}: the FR profile at `dist/speakers/{slug}/index.html` exists, contains the speaker's display name, and contains the FR back-nav text "Retour aux speakers".
    - For each anchor: the EN profile at `dist/en/speakers/{slug}/index.html` exists, contains the speaker's display name, and contains the EN back-nav text "Back to speakers".
    - No assertions on talk content here — that lives in Task 3 (speaker-talks.test.ts) where it's the actual subject.
    - Maintains SPKR-02 coverage: "Individual speaker page with bio, company, photo, social links, and talk abstract" is reduced to the testable load-bearing parts (name + back-nav as proof of generation + i18n wiring). Bio/company/photo/social are validated by other phases' UI tests; here we only need to prove the page exists per speaker.
  </behavior>
  <action>
    REPLACE the entire contents of `tests/build/speaker-profile.test.ts` with a new file structured as follows:

    1. Header docblock (~12 lines): purpose, references `tests/build/_anchors.md` and CONTEXT.md D-08/D-09/D-10, lists the anchor slugs, includes the standard "Run `pnpm build` before this test" warning.
    2. Standard imports (vitest, fs, path) and a `readPage(relativePath)` helper IDENTICAL to the existing one (it works fine — it's the data that was wrong, not the helper).
    3. An `ANCHORS` const array of `{ slug, name }` objects matching the four anchors. Use the EXACT name strings from speakers.csv (NB: `Jérôme Petazzoni` and `Aurélie Vache` carry diacritics — copy verbatim).
    4. A `describe("SPKR-02: FR speaker profile pages exist for each anchor", ...)` block iterating the anchors, with two `it(...)` per anchor: name appears, back-nav "Retour aux speakers" appears.
    5. A mirrored `describe("SPKR-02: EN speaker profile pages exist for each anchor", ...)` block with EN paths and "Back to speakers" back-nav.
    6. Drop the talk-title assertions entirely — they belong in Task 3 and per D-09 we keep coverage minimal.

    Do NOT add new assertions beyond what's needed to prove SPKR-02. Per D-09: "No new test cases added beyond what's needed."

    Per CONTEXT.md anti-pattern list: do NOT use `it.skip()`. If an anchor doesn't generate a page, that's a real regression — the test SHOULD fail.

    Conventional Commits hint: `test(13-02): rewrite speaker-profile build test against real anchors`.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm vitest run tests/build/speaker-profile.test.ts</automated>
  </verify>
  <done>`pnpm vitest run tests/build/speaker-profile.test.ts` reports all tests PASS after `pnpm build`. The file no longer references `speaker-1`..`speaker-6` or fictional names ("Marie Laurent", "Thomas Nguyen", etc.). The header docblock cross-references `tests/build/_anchors.md`. Phase 13 success criterion #2 is met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite tests/build/speaker-talks.test.ts for the real co-speaker pair (SPKR-03)</name>
  <files>tests/build/speaker-talks.test.ts</files>
  <behavior>
    - FR + EN bidirectional co-speaker linking on session S3SPP8:
      - `dist/speakers/arthur-outhenin-chalandre/index.html` contains the string `"Quentin Swiech"` AND a substring matching `/speakers/quentin-swiech` (the link href to the co-speaker's profile).
      - `dist/speakers/quentin-swiech/index.html` contains `"Arthur Outhenin-Chalandre"` AND `/speakers/arthur-outhenin-chalandre`.
      - Mirror both in EN at `dist/en/speakers/{slug}/index.html` with paths `/en/speakers/{co-slug}`.
    - Single-talk display: `dist/speakers/petazzoni/index.html` contains the keynote session's title fragment ("Keynote d'ouverture") — proves the profile-page talks list is wired to sessions.csv via getTalksForSpeaker.
    - Drop the multi-talk speaker-4 assertions entirely (no equivalent multi-talk anchor exists yet in the current roster, and chasing one churns badly per D-10). This is a documented narrowing of scope vs. the original test file — log it in the SUMMARY.
    - Drop the "Programme a venir" / "Schedule coming soon" placeholder assertions — that placeholder logic was removed when the profile page migrated to live talks. Capture this in the SUMMARY.
  </behavior>
  <action>
    REPLACE the entire contents of `tests/build/speaker-talks.test.ts` with a new file:

    1. Header docblock (~15 lines): purpose, references `tests/build/_anchors.md`, explains anchor pair (S3SPP8 → arthur-outhenin-chalandre + quentin-swiech), lists what was REMOVED vs the old file (the multi-talk + schedule-placeholder describes) and why (no current-roster equivalent + placeholder removed in Phase 4 migration). Per D-10: this header is the data-ops spot-check surface.
    2. Imports + `readPage` helper IDENTICAL to existing.
    3. Constants: `const A = { slug: "arthur-outhenin-chalandre", name: "Arthur Outhenin-Chalandre" };` and `const B = { slug: "quentin-swiech", name: "Quentin Swiech" };`. Computed paths via template literals.
    4. `describe("SPKR-03: FR co-speaker cross-references are bidirectional (session S3SPP8)", ...)` with four `it`:
       - A's page references B's name
       - B's page references A's name
       - A's page contains the link `/speakers/{B.slug}`
       - B's page contains the link `/speakers/{A.slug}`
    5. Mirrored EN block with `/en/speakers/...` paths.
    6. `describe("SPKR-03: Speaker profile lists their session from sessions.csv (single-talk anchor)", ...)` with one `it`: `dist/speakers/petazzoni/index.html` contains `"Keynote d'ouverture"` (proves the session-driven talk list is wired).

    Per CONTEXT.md and 13-01-SUMMARY: the Plan 13-01 D-06 fix means `getCoSpeakersForTalk` no longer crashes on undefined speakers, so `pnpm build` will complete without errors against the current sessions.csv (which has no undefined-speakers rows in the happy path, but the guard is the safety net). DO NOT add a build-time assertion about that — it's a unit-test concern, covered in Task 4.

    Conventional Commits hint: `test(13-02): rewrite speaker-talks build test against real co-speaker pair`.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm vitest run tests/build/speaker-talks.test.ts</automated>
  </verify>
  <done>`pnpm vitest run tests/build/speaker-talks.test.ts` reports all tests PASS after `pnpm build`. File no longer references speaker-1..speaker-6 or any fictional name. Header docblock documents the dropped scope (multi-talk + placeholder describes) so the choice is auditable. Phase 13 success criterion #3 is met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Replace src/lib/__tests__/speakers.test.ts wholesale (~30 lines, no stale mocks)</name>
  <files>src/lib/__tests__/speakers.test.ts</files>
  <behavior>
    - Coverage MUST include:
      - `getSlug` returns its argument verbatim (post-CSV-migration behavior).
      - `getCoSpeakersForTalk` happy path: filters out the current speaker.
      - `getCoSpeakersForTalk` D-06 edge case: returns [] for undefined and null `session.speakers` (preserves the guard from Plan 13-01).
      - `getPrimaryTalk` D-04 ordering: keynote beats non-keynote, earliest startTime breaks ties; returns `undefined` for zero-session speaker (D-05).
    - Coverage MUST NOT include:
      - Any reference to `getLocale` (deleted export).
      - Any `vi.mock("astro:content", ...)` for a `talks` collection (no such collection exists — sessions live in sessions.csv).
      - Any fixture using slugs `speaker-1`..`speaker-6` (deleted from CSV).
    - Target file size: ~30 lines of test code (excluding header comment + imports). Per D-09: "broader coverage is out of scope."
  </behavior>
  <action>
    DELETE the entire current contents of `src/lib/__tests__/speakers.test.ts` (including the temporary D-06 block appended in Plan 13-01 Task 1) and write the replacement from scratch:

    1. Header docblock (~8 lines): purpose, lists which exports are covered and why others (`getAllSpeakers`, `getSpeakersByLocale`, `getTalksByLocale`, `getTalksForSpeaker`, `getSortedSpeakers`) are not — they require the live Astro content collection and are exercised end-to-end by the build-output tests in tests/build/.
    2. Imports: `describe, it, expect` from vitest; `getSlug, getCoSpeakersForTalk, getPrimaryTalk` from `../speakers`. No `vi.mock` calls — the three tested helpers are pure.
    3. SessionRow-shaped fixtures inline (3-4 lines each): a keynote session with one speaker, a regular session with two speakers, a regular session with three speakers, and an "early morning" regular session for the startTime tiebreak.
    4. `describe("getSlug", ...)`: one `it` — returns argument verbatim.
    5. `describe("getCoSpeakersForTalk", ...)`: three `it` — happy path filter, undefined-speakers returns [], null-speakers returns [].
    6. `describe("getPrimaryTalk", ...)`: three `it` — keynote ordering wins, earliest startTime wins among non-keynotes, returns undefined when speaker has no sessions.

    No mocks, no `vi.clearAllMocks`, no async setup — the three covered helpers are pure functions.

    Conventional Commits hint: `test(13-02): replace stale speakers.test.ts with focused helper coverage`.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/__tests__/speakers.test.ts &amp;&amp; ! grep -E "getLocale|astro:content|speaker-[1-6]\b" src/lib/__tests__/speakers.test.ts</automated>
  </verify>
  <done>`pnpm vitest run src/lib/__tests__/speakers.test.ts` reports all tests PASS. The file is ~30 lines of test code (header + imports excluded), references no `getLocale`, no `astro:content`, and no `speaker-N` fixture slugs. The D-06 cases from Plan 13-01 Task 1 live here now in their final form (Plan 13-01's append was a temporary RED scaffold, by design).</done>
</task>

</tasks>

<verification>
**Plan-level acceptance gate (run after Task 4):**

1. `pnpm build` succeeds end-to-end — proves the Plan 13-01 grid migration didn't regress.
2. `pnpm vitest run` — all three rewritten files PASS, AND no other test file regresses.
3. `pnpm astro check` — still 0 errors (Plan 13-01's gain is preserved).
4. `! grep -rE "speaker-[1-6]\b|Marie Laurent|Thomas Nguyen|Sarah Chen|Lucas Martin|Amina Diallo|David Moreau" tests/ src/lib/__tests__/` — zero references to deleted fixture identities.
5. `! grep -rE "getLocale\b" src/lib/__tests__/` — confirms the deleted export is no longer imported in tests.
6. `tests/build/_anchors.md` exists and lists petazzoni + arthur-outhenin-chalandre + quentin-swiech + vache (D-10 traceability surface).
</verification>

<success_criteria>
- `tests/build/speaker-profile.test.ts` passes against real anchor speakers — Phase 13 success criterion #2 met.
- `tests/build/speaker-talks.test.ts` passes against the real co-speaker pair (S3SPP8) — Phase 13 success criterion #3 met.
- `src/lib/__tests__/speakers.test.ts` is ~30 lines, covers `getSlug` + `getCoSpeakersForTalk` (incl. D-06 undefined/null cases) + `getPrimaryTalk` (D-04 ordering + D-05 zero-session), and references neither `getLocale` nor a non-existent `talks` collection.
- All test files reference `tests/build/_anchors.md` in their header for the data-ops spot-check trail (D-10).
- No production code modified in this plan — `git diff src/` (excluding `src/lib/__tests__/`) is empty after Task 4.
</success_criteria>

<output>
After completion, create `.planning/phases/13-speaker-schema-drift-cleanup/13-02-SUMMARY.md` capturing:
- The four anchor speakers selected and the rationale (link to tests/build/_anchors.md).
- Documented scope narrowing in speaker-talks.test.ts: dropped multi-talk and "schedule placeholder" describes, with reasons (no current-roster multi-talk anchor; placeholder UI removed in Phase 4).
- Final line counts for each rewritten file vs. the originals.
- Confirmation that all three Phase 13 success criteria from ROADMAP are met (counts + commands).
- The total astro-check error count (should be 0 — preserved from 13-01).
</output>
</content>
</invoke>