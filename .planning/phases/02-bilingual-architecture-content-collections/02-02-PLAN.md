---
phase: 02-bilingual-architecture-content-collections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/LanguageToggle.astro
  - src/components/TranslationNotice.astro
  - src/layouts/Layout.astro
autonomous: true
requirements: [FNDN-03]

must_haves:
  truths:
    - "Language toggle is visible on all pages"
    - "Clicking toggle switches between FR and EN preserving the current page"
    - "Active locale is visually highlighted in the toggle"
    - "Missing translation shows a fallback notice with link to original locale"
  artifacts:
    - path: "src/components/LanguageToggle.astro"
      provides: "FR/EN segmented toggle component"
      contains: "aria-label"
    - path: "src/components/TranslationNotice.astro"
      provides: "Missing translation fallback banner"
      contains: "fallback"
    - path: "src/layouts/Layout.astro"
      provides: "Layout with dynamic lang attribute and language toggle"
      contains: "LanguageToggle"
  key_links:
    - from: "src/components/LanguageToggle.astro"
      to: "src/i18n/utils.ts"
      via: "import { getLangFromUrl, getLocalePath }"
      pattern: "import.*getLocalePath"
    - from: "src/layouts/Layout.astro"
      to: "src/components/LanguageToggle.astro"
      via: "import LanguageToggle"
      pattern: "import LanguageToggle"
---

<objective>
Create the language toggle component and translation notice, then wire them into the site-wide Layout.

Purpose: Visitors need a visible, accessible way to switch languages on every page (FNDN-03). The toggle must preserve the current page path.
Output: LanguageToggle.astro and TranslationNotice.astro components, updated Layout.astro with dynamic lang and toggle in header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bilingual-architecture-content-collections/02-RESEARCH.md
@.planning/phases/02-bilingual-architecture-content-collections/02-UI-SPEC.md
@.planning/phases/02-bilingual-architecture-content-collections/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/i18n/ui.ts:
```typescript
export const languages = { fr: "FR", en: "EN" } as const;
export type Locale = keyof typeof languages;
export const defaultLang: Locale = "fr";
export const ui = { fr: { ... }, en: { ... } } as const;
```

From src/i18n/utils.ts:
```typescript
export function getLangFromUrl(url: URL): Locale;
export function useTranslations(lang: Locale): (key: string) => string;
export function getLocalePath(lang: Locale, path: string): string;
```

Current src/layouts/Layout.astro:
```astro
<html lang="fr" class="dark">
  <!-- hardcoded lang="fr", no header, no toggle -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageToggle and TranslationNotice components</name>
  <files>src/components/LanguageToggle.astro, src/components/TranslationNotice.astro</files>
  <read_first>src/styles/global.css, .planning/phases/02-bilingual-architecture-content-collections/02-UI-SPEC.md, src/i18n/ui.ts, src/i18n/utils.ts</read_first>
  <action>
1. Create `src/components/LanguageToggle.astro`:
   - Import `{ languages, type Locale }` from `@/i18n/ui`
   - Import `{ getLangFromUrl, getLocalePath }` from `@/i18n/utils`
   - Get current language: `const currentLang = getLangFromUrl(Astro.url);`
   - Get current path: `const currentPath = Astro.url.pathname;`
   - Render a `<nav>` element with `aria-label` set to the translated "toggle.aria" string (use `useTranslations`)
   - Container: `class="inline-flex items-center gap-0 rounded-md border border-border bg-secondary p-1"` (uses --color-secondary background, --color-border, --radius-md 6px via rounded-md)
   - For each language in `languages`, render an `<a>` tag:
     - `href` = `getLocalePath(lang, currentPath)`
     - If active: `aria-current="true"`, classes: `bg-primary text-primary-foreground rounded-sm` (uses --color-primary background)
     - If inactive: classes: `text-muted-foreground hover:bg-secondary/80`
     - Common classes: `inline-flex min-w-[40px] items-center justify-center rounded-sm px-3 py-1.5 text-sm font-semibold uppercase tracking-widest transition-colors duration-150`
     - Text content: the language label ("FR" or "EN")
   - Use `class:list` for conditional class application

2. Create `src/components/TranslationNotice.astro`:
   - Import `{ getLangFromUrl, useTranslations, getLocalePath }` from `@/i18n/utils`
   - Import `{ defaultLang }` from `@/i18n/ui`
   - Get lang and translations
   - Props interface: `{ originalLocale?: Locale }` with default to opposite of current lang
   - Render a full-width banner:
     - Container: `class="w-full border-y border-border bg-card px-4 py-4"` (uses --color-card background, --color-border)
     - Info icon: Use inline SVG circle-info icon at 16px, colored with `text-primary` (--color-primary blue)
     - Heading: `class="text-sm font-semibold text-foreground"` -- text from `t("fallback.heading")`
     - Body: `class="text-sm text-muted-foreground"` -- text from `t("fallback.body")`
     - CTA link: `class="text-sm font-semibold text-primary underline-offset-2 hover:underline"` -- text from `t("fallback.cta")`, href links to the same page in the original locale
  </action>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website && [ -f src/components/LanguageToggle.astro ] && [ -f src/components/TranslationNotice.astro ] && grep -q 'aria-label' src/components/LanguageToggle.astro && grep -q 'aria-current' src/components/LanguageToggle.astro && grep -q 'getLocalePath' src/components/LanguageToggle.astro && grep -q 'fallback' src/components/TranslationNotice.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/LanguageToggle.astro exists and contains `aria-label` attribute on nav element
    - src/components/LanguageToggle.astro contains `aria-current` for active locale
    - src/components/LanguageToggle.astro imports from `@/i18n/ui` and `@/i18n/utils`
    - src/components/LanguageToggle.astro renders `<a>` tags (not buttons) for each language
    - src/components/TranslationNotice.astro exists and imports `useTranslations`
    - src/components/TranslationNotice.astro uses `t("fallback.heading")` and `t("fallback.body")` for text
  </acceptance_criteria>
  <done>LanguageToggle renders a segmented FR/EN control with proper accessibility attributes. TranslationNotice renders a fallback banner with info icon and CTA link.</done>
</task>

<task type="auto">
  <name>Task 2: Wire toggle into Layout and verify on all pages</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>src/layouts/Layout.astro, src/components/LanguageToggle.astro, src/pages/index.astro, src/pages/en/index.astro</read_first>
  <action>
1. Update `src/layouts/Layout.astro`:
   - Add import: `import LanguageToggle from "@/components/LanguageToggle.astro";`
   - Change `<html lang="fr" class="dark">` to `<html lang={Astro.currentLocale ?? "fr"} class="dark">`
   - Add a header element between `<body>` and `<slot />`:
     ```html
     <header class="sticky top-0 z-50 flex items-center justify-end px-4 py-3 backdrop-blur-sm bg-background/90">
       <LanguageToggle />
     </header>
     ```
   - Keep all existing content (Font import, global.css import, Props interface, body classes)

2. Run `npx astro build` and verify:
   - dist/index.html contains `LanguageToggle` rendered HTML (look for `aria-label`)
   - dist/index.html contains `href="/en/"` (FR page linking to EN)
   - dist/en/index.html contains `href="/"` (EN page linking to FR)
   - Both pages have the toggle in a header element
  </action>
  <verify>
    <automated>cd /home/smana/Sources/cndfrance-website && npx astro build 2>&1 | tail -3 && grep -q 'LanguageToggle' src/layouts/Layout.astro && grep -q 'Astro.currentLocale' src/layouts/Layout.astro && grep -q 'href="/en/' dist/index.html && grep -q 'href="/"' dist/en/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/Layout.astro contains `import LanguageToggle from`
    - src/layouts/Layout.astro contains `Astro.currentLocale` (dynamic lang attribute)
    - src/layouts/Layout.astro does NOT contain hardcoded `lang="fr"` on html element
    - `npx astro build` exits 0
    - dist/index.html contains `href="/en/"` (toggle link to EN)
    - dist/en/index.html contains `href="/"` (toggle link to FR root)
    - Both dist/index.html and dist/en/index.html contain `aria-label` from the toggle
  </acceptance_criteria>
  <done>Language toggle appears on all pages via Layout.astro. html lang attribute is dynamic. Toggle links correctly between / and /en/ preserving page path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL pathname in toggle | Toggle generates locale-switching URLs from current path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | LanguageToggle href | accept | SSG: all hrefs are computed at build time, not from user input. No open redirect risk. |
| T-02-04 | Information Disclosure | TranslationNotice | accept | Notice only reveals that a translation is missing -- no sensitive data. |
</threat_model>

<verification>
- Language toggle renders on both FR and EN pages
- Toggle correctly links between / and /en/
- html lang attribute matches page locale
- Build passes
</verification>

<success_criteria>
- LanguageToggle is visible on every page (included in Layout.astro)
- Clicking FR/EN in toggle navigates to correct locale path
- Active locale is visually distinct (primary background)
- TranslationNotice component exists for future use
</success_criteria>

<output>
After completion, create `.planning/phases/02-bilingual-architecture-content-collections/02-02-SUMMARY.md`
</output>
