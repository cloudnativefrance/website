---
phase: 4
slug: speakers
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-11
revised: 2026-04-11
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for the Speakers phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | Radix (via shadcn) |
| Icon library | lucide (+ inline SVG for Bluesky) |
| Font | DM Sans (Google Fonts via Astro Fonts API) |

---

## Spacing Scale

Declared values (all multiples of 4, inherited from DESIGN.md):

| Token | Value | Phase 4 Usage |
|-------|-------|---------------|
| xs | 4px | Icon gaps in social link row, badge internal padding-top/bottom |
| sm | 8px | Gap between social icons, compact card internal spacing |
| md | 16px | Card internal padding (mobile), grid gap (mobile) |
| lg | 24px | Card internal padding (desktop), speaker profile section gaps |
| xl | 32px | Grid gap (desktop), space between keynote and regular sections |
| 2xl | 48px | Page section top/bottom padding (mobile) |
| 3xl | 64px | Page section top/bottom padding (desktop) |

Exceptions: 44px minimum touch target for social link icons on mobile (accessibility requirement from DESIGN.md).

---

## Typography

All values from DESIGN.md. Phase 4 uses exactly 4 sizes and 2 weights:

| Role | Size | Weight | Line Height | Phase 4 Usage |
|------|------|--------|-------------|---------------|
| Label / Badge | 14px (0.875rem) | 400 | 1.5 | Company name on grid card, talk duration/format text, track badge text (uppercase, letter-spacing 0.05em for badges) |
| Body / Card title | 16px (1rem) | 400 | 1.6 | Speaker bio text, talk abstract body, speaker name on grid card, talk title on talk card, role + company on profile page |
| Sub-heading | 24px (1.5rem) | 700 | 1.3 | "Keynotes" section label, "Talks" section label on profile |
| Section heading | 36px (2.25rem) | 700 | 1.2 | Page heading "Speakers" / "Nos Speakers" |

**Weight rules:** 400 (regular) for body text, labels, badges, and card content. 700 (bold) for all headings and sub-headings. No intermediate weights.

**Badge differentiation:** Track badges use 14px at weight 400 with `text-transform: uppercase` and `letter-spacing: 0.05em` to achieve visual distinction without a separate size or weight.

**Card title differentiation:** Speaker name on grid card uses 16px at weight 700 to stand out from the 16px/400 body text within the same card context.

---

## Color

All colors from DESIGN.md semantic tokens. Phase 4 color allocation:

| Role | Token | Hex Approx | Phase 4 Usage |
|------|-------|------------|---------------|
| Dominant (60%) | `--color-background` | #0e0a24 | Page background |
| Secondary (30%) | `--color-card` | #1a1240 | Speaker grid cards, talk cards, profile card surface |
| Accent (10%) | `--color-primary` | #4985e8 | Speaker name hover, social link hover, "View talks" link, card hover border |
| Warm accent | `--color-accent` | #ff8a9e | Keynote badge only |
| Muted text | `--color-muted-foreground` | #9490b0 | Company name, role, talk duration, format label |
| Destructive | `--color-destructive` | #cf0822 | Security track badge background (15% opacity) |

Accent reserved for:
- `--color-primary` (#4985e8): Speaker name on hover, social icon on hover, card border on hover, "View talk" links, active navigation
- `--color-accent` (#ff8a9e): Keynote badge pill only. Maximum one accent element per card.

### Track Badge Color Map

| Track | Background | Text |
|-------|-----------|------|
| cloud-infra | `bg-primary/15` | `text-primary` |
| devops-platform | `bg-accent/15` | `text-accent` |
| security | `bg-destructive/15` | `text-destructive` |
| community | `bg-chart-4/15` | `text-chart-4` |

Source: DESIGN.md Track Badges section + RESEARCH.md Pattern 5.

---

## Component Inventory

### SpeakerCard (grid item)

| Property | Specification |
|----------|---------------|
| Container | shadcn `Card` with `--color-card` background, `--radius-lg` (8px), 1px `--color-border` |
| Layout | Centered column: avatar, name, company. Padding 24px. |
| Avatar | 64px circle (`rounded-full`). Initials fallback on `--color-secondary` with `--color-muted-foreground` text. |
| Name | 16px, weight 700, `--color-foreground` |
| Company | 14px, weight 400, `--color-muted-foreground` |
| Keynote badge | Pill badge below name: "Keynote" in `--color-accent` text on `--color-accent/15` background, 14px uppercase, letter-spacing 0.05em |
| Hover | Border color transitions to `--color-primary/50` over 200ms. Card lifts `translateY(-2px)` over 200ms. |
| Click | Entire card is a link to `/speakers/{slug}` (or `/en/speakers/{slug}`) |
| Grid | 4 columns at xl (1280px+), 3 at lg (1024px+), 2 at md (768px+), 1 at mobile. Gap: 24px desktop, 16px mobile. |

### SpeakerProfile (individual page)

| Property | Specification |
|----------|---------------|
| Layout | Single column, centered, max-width 768px. |
| Avatar | 128px circle (`rounded-full`) on grid, 128px `rounded-xl` on profile page. Initials fallback at 30px font size. |
| Name | 36px, weight 700, `--color-foreground`, letter-spacing -0.02em |
| Role + Company | 16px, weight 400, `--color-muted-foreground` |
| Social links | Horizontal icon row below name. 20px icons. Gap 8px. Color: `--color-muted-foreground`, hover: `--color-primary`. Transition 150ms. Min touch target 44px. |
| Bio | Rendered Markdown in `prose prose-invert` with `max-w-none`. Body at 16px/1.6. |
| Talks section | Heading "Talks" at 24px/700. Talk cards stacked vertically with 16px gap. |

### TalkCard (on speaker profile)

| Property | Specification |
|----------|---------------|
| Container | shadcn `Card`, padding 16px, `--radius-lg` border |
| Title | 16px, weight 700, `--color-foreground` |
| Track badge | Pill badge using track color map above. 14px, weight 400, uppercase, 0.05em tracking. Padding 4px 8px. |
| Format + Duration | 14px, weight 400, `--color-muted-foreground`. Format capitalized (e.g., "Talk - 45min"). |
| Co-speakers | 14px, `--color-muted-foreground`. Names as `--color-primary` links to their profile pages. |
| Schedule link | Text link "View in schedule" in `--color-primary`, 14px. Disabled/muted until Phase 7 (render as `--color-muted-foreground` with `cursor-default`). |
| Hover | Border color to `--color-primary/50` over 200ms (same as SpeakerCard). |

### SpeakerAvatar (shared)

| Property | Specification |
|----------|---------------|
| Size `sm` | 64px width/height. Font 16px for initials. |
| Size `lg` | 128px width/height. Font 30px for initials. |
| Photo present | `<img>` with `object-cover`, matching size and border-radius. |
| Photo absent | `--color-secondary` circle/rounded-square with initials in `--color-muted-foreground`, weight 700. |
| Border radius | `rounded-full` on grid cards. `rounded-xl` on profile page (size `lg`). |

### SocialLinks (icon row)

| Property | Specification |
|----------|---------------|
| Icons | GitHub, LinkedIn, Bluesky (inline SVG), Globe (website). Each 20px. |
| Layout | Horizontal flex, gap 8px, vertically centered. |
| Color | `--color-muted-foreground` default, `--color-primary` on hover. |
| Transition | `color 150ms ease` |
| Accessibility | Each link has `aria-label` (e.g., "GitHub profile"), `target="_blank"`, `rel="noopener noreferrer"`. |
| Touch target | Minimum 44px tap area via padding on the anchor element. |

### PageHeader (speakers grid page)

| Property | Specification |
|----------|---------------|
| Heading | "Nos Speakers" (FR) / "Our Speakers" (EN). 36px, weight 700, `--color-foreground`, letter-spacing -0.02em. Centered. |
| Subtext | "Decouvrez les intervenants de Cloud Native Days France 2027" (FR) / "Meet the speakers of Cloud Native Days France 2027" (EN). 16px, weight 400, `--color-muted-foreground`. Centered. |
| Spacing | Heading to subtext: 8px. Section to grid: 48px. Page top padding: 64px. |

---

## Copywriting Contract

### French

| Element | Copy |
|---------|------|
| Page heading | Nos Speakers |
| Page subtext | Decouvrez les intervenants de Cloud Native Days France 2027 |
| Keynote badge | Keynote |
| Talks section heading | Ses talks |
| Co-speaker prefix | Avec |
| Schedule link (active) | Voir dans le programme |
| Schedule link (placeholder) | Programme a venir |
| Empty state heading | Speakers a venir |
| Empty state body | La liste des speakers sera annoncee prochainement. Revenez bientot ! |
| Back to speakers | Retour aux speakers |

### English

| Element | Copy |
|---------|------|
| Page heading | Our Speakers |
| Page subtext | Meet the speakers of Cloud Native Days France 2027 |
| Keynote badge | Keynote |
| Talks section heading | Their Talks |
| Co-speaker prefix | With |
| Schedule link (active) | View in schedule |
| Schedule link (placeholder) | Schedule coming soon |
| Empty state heading | Speakers Coming Soon |
| Empty state body | The speaker lineup will be announced shortly. Check back soon! |
| Back to speakers | Back to speakers |

### Error States

| Scenario | Copy (FR) | Copy (EN) |
|----------|-----------|-----------|
| Speaker not found (404) | Speaker introuvable. Retournez a la liste des speakers. | Speaker not found. Return to the speakers page. |
| No talks for speaker | Aucun talk annonce pour le moment. | No talks announced yet. |

### Destructive Actions

None in this phase. Speaker pages are read-only.

---

## Interaction Contract

### Speaker Grid Page

| Interaction | Behavior |
|-------------|----------|
| Page load | Speakers load statically (SSG). Keynote speakers render first, then alphabetical. |
| Card hover | Border transitions to `--color-primary/50`, card lifts 2px. 200ms ease. |
| Card click | Navigate to `/speakers/{slug}` (FR) or `/en/speakers/{slug}` (EN). Full card is clickable. |
| Responsive | Grid reflows: 4 cols > 3 cols > 2 cols > 1 col. |

### Speaker Profile Page

| Interaction | Behavior |
|-------------|----------|
| Social icon hover | Icon color transitions from muted to primary. 150ms. |
| Social icon click | Opens external URL in new tab. |
| Co-speaker name click | Navigate to co-speaker's profile page (same locale). |
| Back link click | Navigate to speakers grid (same locale). |
| Schedule link (placeholder) | Muted, non-interactive. Becomes a real link when Phase 7 ships. |

### States

| State | Visual Treatment |
|-------|-----------------|
| Speaker with photo | Photo rendered as circle (grid) or rounded square (profile). |
| Speaker without photo | Initials avatar on `--color-secondary` background. |
| Speaker with no talks | "No talks announced yet" message in `--color-muted-foreground`, 14px. |
| Speaker with keynote talk | Keynote badge visible below name on grid card. |
| Co-speaker talk | Co-speaker names listed as primary-colored links. |
| Empty speakers collection | Empty state with heading + body copy, centered on page. |

---

## Responsive Breakpoints

| Breakpoint | Grid Columns | Card Padding | Page Side Padding | Avatar Size (grid) |
|------------|-------------|-------------|-------------------|-------------------|
| < 768px (mobile) | 1 | 16px | 16px | 64px |
| 768px-1023px (tablet) | 2 | 24px | 24px | 64px |
| 1024px-1279px (desktop) | 3 | 24px | 32px | 64px |
| 1280px+ (wide) | 4 | 24px | 32px | 64px |

Profile page max-width: 768px centered at all breakpoints. Side padding: 16px mobile, 0 above (centered by max-width).

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| Color contrast | All text/background pairs exceed WCAG AA (verified in DESIGN.md) |
| Touch targets | Social links: 44px minimum tap area. Card: entire surface is clickable. |
| Alt text | Avatar images: `alt="{speaker name}"`. Missing photo: initials are decorative, avatar div has `aria-label="{speaker name}"`. |
| Keyboard navigation | Cards are `<a>` elements, focusable with visible focus ring (`--color-ring`, 2px offset). Social links individually focusable. |
| Screen reader | Keynote badge includes visually hidden "Keynote speaker" context. Track badges include track name as accessible text. |
| Reduced motion | Hover lift and border transitions respect `prefers-reduced-motion: reduce` (disable transform, keep instant color change). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Badge, Button, Separator | not required |

No third-party registries declared.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
