---
phase: 20-animated-testimonials-strip
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/testimonials/TestimonialsStrip.astro
  - tests/build/testimonials-strip-source.test.ts
autonomous: true
requirements: [TEST-02, A11Y-01, A11Y-02]
must_haves:
  truths:
    - "Component contains @keyframes scroll-x translating 0% to -50%"
    - "Track uses animation: scroll-x 40s linear infinite"
    - "Animation pauses on :hover and :focus-within"
    - "@media (prefers-reduced-motion: reduce) block sets animation: none and enables overflow-x: auto with scroll-snap"
    - "Duplicate track carries aria-hidden=\"true\" AND inert"
    - "Source contract Vitest passes"
  artifacts:
    - path: "src/components/testimonials/TestimonialsStrip.astro"
      provides: "Marquee CSS + a11y contract completed"
      contains: "@keyframes scroll-x"
    - path: "tests/build/testimonials-strip-source.test.ts"
      provides: "A11Y-01/02 + TEST-02 byte-level source assertions"
      contains: "prefers-reduced-motion"
---

<objective>
Land the marquee CSS + a11y media query + source-level Vitest contract in a single atomic pair of commits. After Plan 20-01 the component renders dual tracks with no animation; after this plan it drifts right→left, pauses on hover/focus, respects reduced motion locally, and the duplicate track is fully inert.
</objective>

<context>
@.planning/workstreams/past-editions-showcase/phases/20-animated-testimonials-strip/20-CONTEXT.md
@.planning/workstreams/past-editions-showcase/phases/20-animated-testimonials-strip/20-01-PLAN.md
@src/components/testimonials/TestimonialsStrip.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TestimonialsStrip.astro &lt;style&gt; with marquee keyframes + pause + reduced-motion</name>
  <read_first>
    - src/components/testimonials/TestimonialsStrip.astro (from Plan 20-01)
    - src/styles/global.css lines 101-115 (global Phase 16 reset — local defense complements it)
    - .planning/workstreams/past-editions-showcase/phases/20-animated-testimonials-strip/20-CONTEXT.md (D-07, D-08)
  </read_first>
  <files>src/components/testimonials/TestimonialsStrip.astro</files>
  <action>
Append the following CSS rules inside the existing `<style>` block (before the closing `</style>`). DO NOT remove any existing rules — this is additive.

```css
  /* D-07 marquee keyframes: right → left drift, 40s linear infinite. */
  @keyframes scroll-x {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* The dual-track flex row means translateX(-50%) of the combined width
     aligns the start of track 2 with the start of track 1 — seamless loop. */
  .marquee {
    /* width-contained already via overflow: hidden on .marquee */
    mask-image: linear-gradient(
      to right,
      transparent 0,
      black 4rem,
      black calc(100% - 4rem),
      transparent 100%
    );
    -webkit-mask-image: linear-gradient(
      to right,
      transparent 0,
      black 4rem,
      black calc(100% - 4rem),
      transparent 100%
    );
  }

  .marquee-track {
    animation: scroll-x 40s linear infinite;
    will-change: transform;
  }

  /* D-08 pause on hover and keyboard focus — no play/pause button. */
  .marquee:hover .marquee-track,
  .marquee:focus-within .marquee-track {
    animation-play-state: paused;
  }

  /* A11Y-01 defensive reduced-motion reset: stops animation + enables
     horizontal-scroll fallback with snap points. The global Phase 16 reset
     already neutralizes animation-duration; this block is authoritative. */
  @media (prefers-reduced-motion: reduce) {
    .marquee {
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      mask-image: none;
      -webkit-mask-image: none;
    }
    .marquee-track {
      animation: none;
    }
    .marquee-track > .card {
      scroll-snap-align: start;
    }
  }
```
  </action>
  <acceptance_criteria>
    - grep -qF '@keyframes scroll-x' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF 'translateX(-50%)' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF 'animation: scroll-x 40s linear infinite' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF 'animation-play-state: paused' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF ':focus-within' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF '@media (prefers-reduced-motion: reduce)' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF 'animation: none' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF 'overflow-x: auto' src/components/testimonials/TestimonialsStrip.astro
    - grep -qF 'scroll-snap-type: x mandatory' src/components/testimonials/TestimonialsStrip.astro
    - pnpm build exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -qF '@keyframes scroll-x' src/components/testimonials/TestimonialsStrip.astro && grep -qF 'translateX(-50%)' src/components/testimonials/TestimonialsStrip.astro && grep -qF 'animation: scroll-x 40s linear infinite' src/components/testimonials/TestimonialsStrip.astro && grep -qF 'animation-play-state: paused' src/components/testimonials/TestimonialsStrip.astro && grep -qF ':focus-within' src/components/testimonials/TestimonialsStrip.astro && grep -qF '@media (prefers-reduced-motion: reduce)' src/components/testimonials/TestimonialsStrip.astro && grep -qF 'animation: none' src/components/testimonials/TestimonialsStrip.astro && grep -qF 'overflow-x: auto' src/components/testimonials/TestimonialsStrip.astro && grep -qF 'scroll-snap-type: x mandatory' src/components/testimonials/TestimonialsStrip.astro && pnpm build</automated>
  </verify>
  <done>Marquee CSS + pause + reduced-motion landed; build passes.</done>
  <commit>feat(20-02): add marquee animation + pause + reduced-motion CSS (TEST-02, A11Y-01)</commit>
</task>

<task type="auto">
  <name>Task 2: Add tests/build/testimonials-strip-source.test.ts — A11Y-01/02 + TEST-02 source contract</name>
  <read_first>
    - src/components/testimonials/TestimonialsStrip.astro
    - tests/build/past-edition-shell.test.ts (source-read pattern reference)
  </read_first>
  <files>tests/build/testimonials-strip-source.test.ts</files>
  <action>
Create `tests/build/testimonials-strip-source.test.ts`:

```ts
/**
 * TEST-02 + A11Y-01 + A11Y-02 (Phase 20).
 *
 * Byte-level contract on the component source file. We read as text rather
 * than importing because Vitest cannot parse .astro SFCs without extra config
 * (same pattern as past-edition-shell.test.ts).
 */
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const COMPONENT_PATH = resolve(
  import.meta.dirname,
  "../../src/components/testimonials/TestimonialsStrip.astro",
);

const src = readFileSync(COMPONENT_PATH, "utf-8");

describe("TEST-02 marquee animation contract", () => {
  it("defines @keyframes scroll-x from 0 to -50%", () => {
    expect(src).toMatch(/@keyframes\s+scroll-x/);
    expect(src).toContain("translateX(-50%)");
  });

  it("applies the animation to .marquee-track with 40s linear infinite", () => {
    expect(src).toMatch(/animation:\s*scroll-x\s+40s\s+linear\s+infinite/);
  });

  it("pauses the animation on :hover AND :focus-within", () => {
    expect(src).toMatch(/\.marquee:hover\s+\.marquee-track/);
    expect(src).toMatch(/\.marquee:focus-within\s+\.marquee-track/);
    expect(src).toContain("animation-play-state: paused");
  });
});

describe("A11Y-01 reduced-motion fallback", () => {
  it("contains a prefers-reduced-motion media query that disables animation", () => {
    expect(src).toMatch(/@media\s*\(prefers-reduced-motion:\s*reduce\)/);
    // inside that block, animation must resolve to none
    const idx = src.indexOf("prefers-reduced-motion");
    const block = src.slice(idx, idx + 600);
    expect(block).toContain("animation: none");
  });

  it("enables an overflow-x: auto horizontal-scroll fallback under reduced motion", () => {
    const idx = src.indexOf("prefers-reduced-motion");
    const block = src.slice(idx, idx + 600);
    expect(block).toContain("overflow-x: auto");
    expect(block).toContain("scroll-snap-type: x mandatory");
  });
});

describe("A11Y-02 duplicate-track hidden from assistive tech", () => {
  it("renders exactly two <ul> elements with class marquee-track", () => {
    const matches = src.match(/class="marquee-track"/g) ?? [];
    expect(matches.length, `Expected 2 marquee-track lists, got ${matches.length}`).toBe(2);
  });

  it("marks the duplicate track with aria-hidden=\"true\" AND inert", () => {
    // The duplicate track's <ul ...> tag must carry both attributes.
    const dupMatch = src.match(
      /<ul[^>]*data-track="duplicate"[^>]*>/,
    );
    expect(dupMatch, "Duplicate track tag not found").toBeTruthy();
    expect(dupMatch![0]).toContain('aria-hidden="true"');
    expect(dupMatch![0]).toContain("inert");
  });

  it("canonical track does NOT carry aria-hidden or inert", () => {
    const canonMatch = src.match(
      /<ul[^>]*data-track="canonical"[^>]*>/,
    );
    expect(canonMatch, "Canonical track tag not found").toBeTruthy();
    expect(canonMatch![0]).not.toContain("aria-hidden");
    expect(canonMatch![0]).not.toMatch(/\binert\b/);
  });
});
```
  </action>
  <acceptance_criteria>
    - test -f tests/build/testimonials-strip-source.test.ts
    - pnpm vitest run tests/build/testimonials-strip-source.test.ts exits 0
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/build/testimonials-strip-source.test.ts && pnpm vitest run tests/build/testimonials-strip-source.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Source-level a11y + animation contract test exists and passes.</done>
  <commit>test(20-02): assert marquee CSS + reduced-motion + inert dup-track (A11Y-01/02, TEST-02)</commit>
</task>

</tasks>

<success_criteria>
Plan complete when:
- Component renders a fully-animated marquee in normal motion, stops animating on hover/focus, and falls back to a scrollable snap row under `prefers-reduced-motion: reduce`
- Duplicate track is `inert` + `aria-hidden="true"`
- `tests/build/testimonials-strip-source.test.ts` passes
- 2 atomic commits landed (feat, test)
</success_criteria>

<output>
After completion, create `.planning/workstreams/past-editions-showcase/phases/20-animated-testimonials-strip/20-02-SUMMARY.md`
</output>
