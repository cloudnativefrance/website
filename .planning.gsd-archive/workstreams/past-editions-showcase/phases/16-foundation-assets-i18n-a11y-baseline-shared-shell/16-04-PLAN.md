---
phase: 16
plan: 04
type: execute
wave: 2
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md
autonomous: true
requirements: [EDIT-04, A11Y-05, I18N-01, I18N-02]
tags: [validation, build-gate]
user_setup: []

must_haves:
  truths:
    - "pnpm build exits 0 with zero new warnings compared to the pre-Phase-16 baseline"
    - "pnpm test exits 0 with all suites green"
    - "VALIDATION.md is filled in with real commands and marked nyquist_compliant: true"
  artifacts:
    - path: ".planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md"
      provides: "phase validation strategy sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "VALIDATION.md"
      to: "all Phase 16 test files"
      via: "Per-Task Verification Map"
      pattern: "tests/build/(i18n-parity|past-edition-shell|a11y-motion-reset|kcd2023-assets)"
---

<objective>
Final phase gate: prove success criterion 5 (zero-new-warnings `pnpm build` + green Vitest suite) and fill VALIDATION.md with the real commands and sign-off. This plan is deliberately small so it fits one context window and runs after the three work plans (01, 02, 03) all land.

Purpose: flip Phase 16 from "code written" to "baseline certified". Blocks Phase 17 from starting until baseline is known clean.
Output: validated build + test run, populated VALIDATION.md with full Per-Task Verification Map covering every task across plans 16-01..16-03 and the gate task in 16-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-CONTEXT.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-RESEARCH.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-01-PLAN.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-02-PLAN.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-03-PLAN.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-01-SUMMARY.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-02-SUMMARY.md
@.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-03-SUMMARY.md
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Baseline green-build gate + fill VALIDATION.md (D-11, success criterion 5)</name>
  <files>.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md</files>
  <read_first>
    - .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md (current template — fill in, don't overwrite the frontmatter structure)
    - .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-CONTEXT.md §D-11 (zero new warnings)
    - .planning/STATE.md §"Carry-Over from v1.0 (not in v1.1)" (pre-existing v1.0 warnings are documented out-of-scope — do NOT regress them, do NOT fix them either)
    - package.json (scripts section — confirm `build`, `test`, `optimize:photos`)
  </read_first>
  <action>
    Step 1 — Run the zero-new-warnings baseline check:
    ```
    pnpm build 2>&1 | tee /tmp/phase16-build.log
    pnpm test  2>&1 | tee /tmp/phase16-test.log
    ```

    Both commands MUST exit 0. Inspect `/tmp/phase16-build.log` and `/tmp/phase16-test.log` for warnings.

    A "new warning" (D-11) means a warning that (a) does NOT appear in the STATE.md carry-over list (SPKR-01 fixture drift, Zod 13 `LoaderConstraint` errors, organizer content placeholders) and (b) is attributable to a file touched by Phase 16 (ui.ts, global.css, PastEditionSection.astro, optimize-photos.ts, the four new test files, kcd2023 logo/photo assets).

    If any NEW warning is found: identify the root cause and fix it in the responsible file before proceeding. Common candidates:
    - Astro `astro check` errors in `PastEditionSection.astro` → fix the shell (typically `ImageMetadata` import path per RESEARCH.md Pitfall 4)
    - TypeScript errors in `ui.ts` → check for duplicated keys or missing trailing commas
    - Vitest flakes on parity test → check byte-identical FR/EN values (RESEARCH.md Pitfall 6)
    - Vitest fail on motion-reset test → verify the @media block sits OUTSIDE any @layer (Pitfall 3)

    Document the specific warning + fix in the VALIDATION.md "Validation Sign-Off" section if one was encountered.

    Step 2 — Rewrite `.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md` with real values, preserving the frontmatter schema. Use this structure (fill every placeholder). The Per-Task Verification Map MUST list one row per concrete task across plans 16-01, 16-02, 16-03, 16-04 (10 task rows total — see the canonical task IDs below):

    Canonical task IDs covered by Phase 16 (each MUST appear as a row):
    - 16-01-01 — global.css A11Y-05 reset block
    - 16-01-02 — ui.ts editions.* + testimonials.* keys
    - 16-01-03 — i18n-parity.test.ts
    - 16-01-04 — KCD 2023 logo SVGs
    - 16-01-05 — a11y-motion-reset.test.ts
    - 16-02-01 — Human gate: photo originals dropped (manual)
    - 16-02-02 — sharp pipeline + 10 JPG masters
    - 16-02-03 — kcd2023-assets.test.ts (asset-budget gate)
    - 16-03-01 — PastEditionSection.astro shell
    - 16-03-02 — past-edition-shell.test.ts (shell + D-09 safeguard)
    - 16-04-01 — this gate task (build + test + VALIDATION sign-off)

    Phase-16 Vitest test file inventory (each row's "Automated Command" must reference one of these or a shell command):
    - tests/build/i18n-parity.test.ts (I18N-01, I18N-02; covers D-04/D-05/D-06)
    - tests/build/a11y-motion-reset.test.ts (A11Y-05; covers D-07)
    - tests/build/kcd2023-assets.test.ts (EDIT-04 asset portion; covers D-01/D-02/D-03)
    - tests/build/past-edition-shell.test.ts (EDIT-04 shell portion; covers D-08/D-09/D-10)

    Use this template structure verbatim:

    ```markdown
    ---
    phase: 16
    slug: foundation-assets-i18n-a11y-baseline-shared-shell
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-13
    approved: {YYYY-MM-DD of today's execution}
    ---

    # Phase 16 — Validation Strategy

    ## Test Infrastructure

    | Property | Value |
    |----------|-------|
    | Framework | vitest 4.1.x |
    | Config file | vitest.config.ts |
    | Quick run command | `pnpm exec vitest run tests/build/i18n-parity.test.ts tests/build/a11y-motion-reset.test.ts tests/build/kcd2023-assets.test.ts tests/build/past-edition-shell.test.ts` |
    | Full suite command | `pnpm test` |
    | Estimated runtime | ~{measured seconds from the run} s |

    ## Sampling Rate

    - After every task commit: quick run command above
    - After every plan wave: `pnpm test`
    - Before `/gsd-verify-work`: full suite must be green AND `pnpm build` must exit 0 with no new warnings
    - Max feedback latency: < 10 s (all 4 Phase-16 tests import source directly, no dist/ dep)

    ## Per-Task Verification Map

    | Task ID | Plan | Wave | Requirement | Decision Refs | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|---------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 16-01-01 | 01 | 1 | A11Y-05 | D-07 | T-16-03 | reduced-motion disables animations site-wide | CSS-grep | `grep -q "prefers-reduced-motion: reduce" src/styles/global.css` | ✅ | {status} |
    | 16-01-02 | 01 | 1 | I18N-01 | D-04, D-06 | T-16-01 | bilingual key parity in ui.ts | TS-check | `pnpm exec tsc --noEmit` | ✅ | {status} |
    | 16-01-03 | 01 | 1 | I18N-02 | D-05 | T-16-01 | drift detected at CI time | unit (Vitest) | `pnpm exec vitest run tests/build/i18n-parity.test.ts` | ✅ | {status} |
    | 16-01-04 | 01 | 1 | EDIT-04 (logo asset) | D-03 | T-16-02 | logo assets present | fs | `test -f src/assets/logos/kcd2023/logo.svg && test -f src/assets/logos/kcd2023/logo-dark.svg` | ✅ | {status} |
    | 16-01-05 | 01 | 1 | A11Y-05 (guard) | D-07 | T-16-03 | A11Y reset stays at top level (Pitfall 3) | unit (Vitest) | `pnpm exec vitest run tests/build/a11y-motion-reset.test.ts` | ✅ | {status} |
    | 16-02-01 | 02 | 1 | EDIT-04 (input) | D-02 | — | originals staged locally, never committed | manual gate | `ls ./_photo-drop/kcd2023/ \| grep -ciE "\\.(jpe?g\|png\|webp\|heic\|avif)$"` (≥ 10) | n/a | {status} |
    | 16-02-02 | 02 | 1 | EDIT-04 (asset) | D-01, D-02, D-11 | T-16-04, T-16-05, T-16-06 | photo budget enforced at write time | fs/size | `test "$(ls src/assets/photos/kcd2023/*.jpg \| wc -l)" -eq 10 && test -z "$(find src/assets/photos/kcd2023 -name '*.jpg' -size +1024k)" && test "$(du -cb src/assets/photos/kcd2023/*.jpg \| tail -1 \| awk '{print $1}')" -le 7340032` | ✅ | {status} |
    | 16-02-03 | 02 | 1 | EDIT-04 (asset guard) | D-01, D-02, D-03 | T-16-06 | photo + logo budget enforced at CI time | unit (Vitest) | `pnpm exec vitest run tests/build/kcd2023-assets.test.ts` | ✅ | {status} |
    | 16-03-01 | 03 | 1 | EDIT-04 (shell) | D-08, D-10 | T-16-07, T-16-08, T-16-09 | shell contract + no-hydration + DS-tokens-only | astro check | `pnpm exec astro check 2>&1 \| tee /tmp/astro-check.log && ! grep -iE "error.*past-editions\|past-editions.*error" /tmp/astro-check.log` | ✅ | {status} |
    | 16-03-02 | 03 | 1 | EDIT-04 (shell guard) | D-09 | T-16-07, T-16-08, T-16-09 | shell exists + Props locked + renders nowhere | unit (Vitest) | `pnpm exec vitest run tests/build/past-edition-shell.test.ts` | ✅ | {status} |
    | 16-04-01 | 04 | 2 | all | D-11 | T-16-10 | zero-new-warnings build + green test suite | build + test | `pnpm build && pnpm test` | ✅ | {status} |

    Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky

    ## Wave 0 Requirements

    - [x] Vitest framework already installed (vitest.config.ts at repo root)
    - [x] `@` alias already wired
    - [x] tests/build/ folder conventions already in use

    Existing infrastructure covers all phase requirements — no Wave 0 scaffolding needed beyond the four new test files shipped in plans 01, 02, and 03.

    ## Manual-Only Verifications

    | Behavior | Requirement | Why Manual | Test Instructions |
    |----------|-------------|------------|-------------------|
    | prefers-reduced-motion reset actually disables a real animation in a real browser | A11Y-05 | No animated component ships in Phase 16 yet | Verified end-to-end in Phase 20 via Playwright `emulateMedia({ reducedMotion: 'reduce' })` on the testimonials marquee |
    | KCD logo contrast on --color-secondary | D-03 / RESEARCH.md Pitfall 5 | Subjective contrast read | Phase 19 visual review — not a Phase 16 blocker |
    | User stages 10 photo originals into _photo-drop/kcd2023/ | EDIT-04 (input) | Originals are local-only and never committed | Plan 16-02 Task 1 human gate; user types "originals ready" |

    ## Validation Sign-Off

    - [x] All tasks have `<automated>` verify (or are explicit manual gates with shell verification command)
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify
    - [x] No watch-mode flags
    - [x] Feedback latency < 16s ({actual measured from the baseline run})
    - [x] `nyquist_compliant: true` set in frontmatter
    - [x] `pnpm build` exits 0 with no NEW warnings (pre-existing v1.0 warnings per STATE.md carry-over list are out-of-scope)
    - [x] `pnpm test` exits 0
    - [x] Per-Task Verification Map covers every task across plans 16-01..16-04 (≥ 10 rows)

    Approval: approved {YYYY-MM-DD}
    ```

    Replace every `{…}` placeholder with the real measured value / date before writing. Do NOT leave template braces in the final file.

    The frontmatter MUST flip `nyquist_compliant: false` → `nyquist_compliant: true` and `wave_0_complete: false` → `wave_0_complete: true` and `status: draft` → `status: approved` only after both `pnpm build` and `pnpm test` exit 0.
  </action>
  <verify>
    <automated>pnpm build >/dev/null 2>&1 && pnpm test >/dev/null 2>&1 && grep -q "nyquist_compliant: true" .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md && grep -q "status: approved" .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md && ! grep -qE "\\{YYYY|\\{measured|\\{status|\\{actual" .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md && test "$(grep -cE '^\\| 16-0[1-4]-0[1-9]' .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md)" -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `pnpm test` exits 0 (all existing suites plus the four new ones from plans 01, 02, 03)
    - No NEW warnings in build or test output, where "NEW" = not on the STATE.md v1.0 carry-over list and attributable to a Phase 16 file
    - `grep -q "nyquist_compliant: true" .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md` succeeds
    - `grep -q "status: approved" .planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-VALIDATION.md` succeeds
    - No `{placeholder}` template braces remain in VALIDATION.md (`! grep -qE "\\{YYYY|\\{measured|\\{status|\\{actual" VALIDATION.md`)
    - Per-Task Verification Map covers ALL 11 task IDs (16-01-01..05, 16-02-01..03, 16-03-01..02, 16-04-01) with ✅ status (green) — `grep -cE '^\\| 16-0[1-4]-0[1-9]' VALIDATION.md` ≥ 10 (manual gate row may use n/a for File Exists but still counts)
  </acceptance_criteria>
  <done>Baseline green-build gate passed; VALIDATION.md populated and approved with full per-task map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — this plan only runs existing build + test tooling and edits a planning doc.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Tampering | VALIDATION.md sign-off | accept | Planning document; changes are git-tracked and reviewed alongside the phase merge |
</threat_model>

<verification>
- `pnpm build && pnpm test` exits 0
- VALIDATION.md has `nyquist_compliant: true` and `status: approved`
- Zero `{placeholder}` remnants in VALIDATION.md
- Per-Task Verification Map ≥ 10 rows (every task across plans 16-01..16-04)
</verification>

<success_criteria>
- Success criterion 5 met: `pnpm build` + Vitest suite pass with no new warnings
- VALIDATION.md is a living, populated document tracking per-task automated verifies for every Phase 16 task
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-assets-i18n-a11y-baseline-shared-shell/16-04-SUMMARY.md` documenting: measured build time, test runtime, any warnings encountered + fixed, the row count in the Per-Task Verification Map, and explicit confirmation that all 4 phase success criteria are met.
</output>
