---
phase: 22-a11y-uat-closeout
plan: 01
type: manual-uat
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/workstreams/past-editions-showcase/phases/19-integrate-2023-edition-section-and-lightbox/19-UAT.md
  - .planning/workstreams/past-editions-showcase/phases/19-integrate-2023-edition-section-and-lightbox/content-gates.md
  - .planning/workstreams/past-editions-showcase/phases/22-a11y-uat-closeout/22-SUMMARY.md
autonomous: false
requirements: [A11Y-01, A11Y-03, EDIT-05]
must_haves:
  truths:
    - "All 10 lightbox keyboard-journey checkboxes in 19-UAT.md are marked [x] (pass), [-] (n/a with reason), or accompanied by a one-line evidence note."
    - "Stitch approval entry exists in 19-*/content-gates.md §4 (one of: 'approved by user 2026-MM-DD', 'declined — see followup', or screenshot reference)."
    - "Lighthouse CLS score for /2023 and /en/2023 is captured in 19-UAT.md (paste exact number; pass = ≤0.02, fail triggers gap plan)."
    - "22-SUMMARY.md documents SC4 (Playwright) deferral to v1.2 with the rationale from 22-CONTEXT D-02."
  artifacts:
    - 19-UAT.md updated with all checkboxes resolved + evidence
    - content-gates.md §4 (Stitch) marked resolved
    - 22-SUMMARY.md created with verdict
---

# Plan 22-01 — A11y UAT Closeout (Manual)

## Scope

Walk through manual a11y validation items deferred during Phase 19. Phase complete when the checklist is filled in (pass/fail/n/a with evidence), not when every item passes — a `fail` becomes a follow-up gap plan.

## Prerequisites (run once before starting)

```bash
cd /home/smana/Sources/cndfrance-website
pnpm dev   # http://localhost:4321
```

Open `/2023` and `/en/2023` in the browser. Open DevTools (Lighthouse + Accessibility tabs).

## Tasks

### Task 1 — Lightbox keyboard journey (10 items, FR + EN)

Open `19-UAT.md`. For each unchecked item under `## Lightbox (EDIT-05 / A11Y-03)`, perform the action in the live page and update the checkbox:

- `[x]` — pass (works as described)
- `[-]` — n/a (with one-line reason)
- `[ ]` — fail (add `result: fail — <evidence>` line below; triggers follow-up gap)

Items to validate:
1. Tab to first photo tile, Enter/Space opens lightbox, focus lands on close button
2. Escape closes, focus returns to originating tile
3. ArrowRight cycles 1→10 then wraps to 1
4. ArrowLeft on photo 1 wraps to photo 10
5. Tab cycles within {close, prev, next} (focus trap)
6. Shift+Tab at first focusable wraps to last
7. Click backdrop closes
8. Screen reader (VoiceOver/NVDA/Orca) announces "dialog" + aria-label

Plus 2 photo-grid CLS items:
9. Network throttled to 3G — tiles don't reflow as images load
10. Lighthouse CLS on `/2023` and `/en/2023` (record exact numbers)

**Verify:** All 10 lightbox + grid items resolved in `19-UAT.md`.

**Commit:** `docs(22-01): close lightbox keyboard-journey UAT`

### Task 2 — Stitch approval for `/2023` (D-03 carry-over)

Review the live `/2023` and `/en/2023` pages against Stitch design system (DM Sans, dark theme + warm accents, design tokens only). Either:

- **Approve as-is:** add to `19-*/content-gates.md` §4: `Stitch visual approval: approved by user 2026-04-14 — live page reviewed against 15-CONTEXT design rhythm. No Stitch screen committed (post-code review, not pre-code mock).`
- **Decline:** describe the visual deltas; triggers a follow-up Phase 23 design-fix.

**Verify:** `content-gates.md` §4 status updated.

**Commit:** `docs(22-01): record Stitch approval for /2023`

### Task 3 — Lighthouse CLS measurement

Run Lighthouse (mobile + desktop) on `/2023` and `/en/2023`. Paste the four CLS numbers into `19-UAT.md` under "Photo Grid + CLS":

```
- /2023        mobile  CLS: 0.0XX  → pass / fail
- /2023        desktop CLS: 0.0XX  → pass / fail
- /en/2023     mobile  CLS: 0.0XX  → pass / fail
- /en/2023     desktop CLS: 0.0XX  → pass / fail
```

**Verify:** Four CLS numbers captured. If any > 0.02, log a follow-up gap.

**Commit:** `docs(22-01): record Lighthouse CLS for /2023 and /en/2023`

### Task 4 — Document SC4 (Playwright) defer

Write `22-SUMMARY.md` with frontmatter matching Phase 17/20 style. Include a section `## SC4 — Playwright reduced-motion + lightbox automation: DEFERRED to v1.2` with rationale from CONTEXT D-02:

- No browser test runner currently in the codebase (Vitest is unit/build-time only).
- Adding Playwright/Vitest-browser scaffolding is its own multi-task phase, not a closeout item.
- Static a11y contracts in Phase 19/20 cover the contract; live-browser keyboard journey covered manually in Task 1.

**Verify:** `22-SUMMARY.md` exists with the deferral entry.

**Commit:** `docs(22-01): summary + Playwright deferral to v1.2`

## Verification Summary

After all 4 tasks:

- All 10 manual UAT items resolved in `19-UAT.md` (pass/n/a/fail+evidence)
- Stitch approval status recorded
- Lighthouse CLS numbers captured
- SC4 deferral documented
- No source code changes (`git diff --stat src/ tests/` is empty)

## Failure Mode

If Task 1 produces ≥1 fail: open a Phase 23 gap plan with the failing items as `must_haves.truths` (do not block closeout of Phase 22 — record the gap and route).

If Task 2 declined: open Phase 23 design-fix.

If Task 3 fails CLS: open Phase 23 perf-fix.
