---
phase: 18-venue-page-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/i18n/ui.ts
  - tests/build/venue-cleanup.test.ts
autonomous: false
requirements: [VENUE-03]
must_haves:
  truths:
    - "`grep -rn \"venue\\.prev\" src/` returns zero hits (Roadmap SC3)."
    - "The 8 FR keys at src/i18n/ui.ts:136-143 (venue.prev.rail_label, .heading, .video_title, .participants, .talks, .partners, .thumb_alt, .gallery_link) are deleted."
    - "The 8 EN keys at src/i18n/ui.ts:404-411 (the EN counterparts of the same 8 keys) are deleted."
    - "No other i18n keys are touched (surrounding keys `venue.around.food_body` and `schedule.heading` remain byte-identical)."
    - "`tests/build/venue-cleanup.test.ts` gains one new assertion: `src/i18n/ui.ts` contains zero matches for `/venue\\.prev/`; the TODO comment from 18-01 is removed."
    - "`pnpm build` completes green; no runtime or build-time errors from missing `t()` lookups (SC5 — nothing in the codebase calls `t(\"venue.prev.*\")` anymore after 18-01 shipped)."
    - "`pnpm exec vitest run tests/build/venue-cleanup.test.ts` passes, including the new i18n-sweep assertion."
  artifacts:
    - path: "src/i18n/ui.ts"
      provides: "i18n table with 16 orphaned venue.prev.* keys removed (8 FR + 8 EN)."
    - path: "tests/build/venue-cleanup.test.ts"
      provides: "Extended regression suite asserting the i18n sweep landed."
      contains: "venue\\.prev"
  key_links:
    - from: "tests/build/venue-cleanup.test.ts"
      to: "src/i18n/ui.ts"
      via: "fs.readFileSync + negative-match regex on `venue\\.prev`"
      pattern: "venue\\.prev"
---

<objective>
Complete the Phase 18 cleanup by sweeping the now-orphaned `venue.prev.*` i18n keys from `src/i18n/ui.ts` (8 keys FR lines 136-143 + 8 keys EN lines 404-411 = 16 entries total). Extend `tests/build/venue-cleanup.test.ts` with the D-03 assertion that was deferred from 18-01.

Wave 2: depends on 18-01. The 18-01 commit removed every `t("venue.prev.*")` call site, so these keys are genuinely dead weight at this point — no runtime or build-time caller remains.

Purpose: Close Roadmap SC3 (`grep -rn "venue\.prev" src/` returns zero). Keep the i18n table tidy and prevent the dead keys from confusing future editors or translation tooling.

Output: Smaller `src/i18n/ui.ts`, strengthened regression suite, final `refactor(18-02):` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-venue-page-cleanup/18-CONTEXT.md
@.planning/phases/18-venue-page-cleanup/18-01-PLAN.md
@src/i18n/ui.ts
@tests/build/venue-cleanup.test.ts

<interfaces>
<!-- FR block to delete (src/i18n/ui.ts lines 136-143, verified on HEAD): -->
<!--   "venue.prev.rail_label":  "Edition 2026 — replay" -->
<!--   "venue.prev.heading":     "Revivez l'edition 2026" -->
<!--   "venue.prev.video_title": "Aftermovie Cloud Native Days France 2026" -->
<!--   "venue.prev.participants":"Participants" -->
<!--   "venue.prev.talks":       "Conferences" -->
<!--   "venue.prev.partners":    "Partenaires" -->
<!--   "venue.prev.thumb_alt":   "Ambiance Cloud Native Days France 2026" -->
<!--   "venue.prev.gallery_link":"Voir la galerie complete 2026" -->

<!-- EN block to delete (src/i18n/ui.ts lines 404-411, verified on HEAD): -->
<!--   "venue.prev.rail_label":  "2026 edition — replay" -->
<!--   "venue.prev.heading":     "Relive the 2026 edition" -->
<!--   "venue.prev.video_title": "Aftermovie Cloud Native Days France 2026" -->
<!--   "venue.prev.participants":"Attendees" -->
<!--   "venue.prev.talks":       "Talks" -->
<!--   "venue.prev.partners":    "Partners" -->
<!--   "venue.prev.thumb_alt":   "Cloud Native Days France 2026 atmosphere" -->
<!--   "venue.prev.gallery_link":"View the full 2026 gallery" -->

<!-- Adjacent keys that must remain byte-identical: -->
<!--   FR: "venue.around.food_body" (line 135) and "schedule.heading" (line 144) -->
<!--   EN: "venue.around.food_body" (line 403) and "schedule.heading" (line 412) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 16 venue.prev.* entries from src/i18n/ui.ts</name>
  <read_first>
    - src/i18n/ui.ts (full file — confirm exact line ranges)
    - .planning/phases/18-venue-page-cleanup/18-CONTEXT.md (D-01, D-03)
  </read_first>
  <files>src/i18n/ui.ts</files>
  <action>
In `src/i18n/ui.ts`:

1. **FR block — delete lines 136-143** (the 8 `venue.prev.*` keys inside the FR/`fr` translation object). The preceding key is `venue.around.food_body` and the following key is `schedule.heading`. After deletion, the closing `",` of `venue.around.food_body` must flow directly into `"schedule.heading": ...`.
2. **EN block — delete lines 404-411** (the 8 `venue.prev.*` keys inside the EN/`en` translation object). Same structural rule: `venue.around.food_body` flows directly into `schedule.heading`.

Preserve:
- Surrounding keys (byte-identical values)
- Trailing commas — ensure the last kept key before the deletion still terminates with `",` so the object literal stays valid
- File encoding + any `\u2014` em-dash escapes in surviving strings

**Do not** touch any other key. **Do not** reorder. **Do not** "tidy up" unrelated whitespace. Minimal diff — just the 16 deletions.

Per D-01 + D-04 this is commit B, committed separately as `refactor(18-02): remove venue.prev.* i18n keys (FR + EN)`.
  </action>
  <verify>
    <automated>grep -rn "venue\.prev" src/ ; test $? -eq 1</automated>
  </verify>
  <done>
    - `grep -rn "venue\.prev" src/` returns zero hits (Roadmap SC3).
    - `src/i18n/ui.ts` parses cleanly (TypeScript happy).
    - No unrelated whitespace / reorder changes in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/build/venue-cleanup.test.ts with the i18n-sweep assertion + green pipeline</name>
  <read_first>
    - tests/build/venue-cleanup.test.ts (as shipped in 18-01 — contains the TODO marker)
    - .planning/phases/18-venue-page-cleanup/18-CONTEXT.md (D-03)
  </read_first>
  <files>tests/build/venue-cleanup.test.ts</files>
  <action>
Open `tests/build/venue-cleanup.test.ts` (created in 18-01) and:

1. **Remove** the `// TODO(18-02): assert grep venue.prev src/i18n/ui.ts -> 0 matches ...` comment.
2. **Add** one new `it(...)` assertion inside the source-level `describe` block (or an adjacent one, whichever matches the file's existing structure):
   - Read `src/i18n/ui.ts` via `fs.readFileSync(..., "utf-8")`.
   - Assert `expect(content.match(/venue\.prev/g) ?? []).toHaveLength(0)` (or the equivalent negative-match pattern consistent with the other assertions in the file).

Keep the style consistent with Phase 17 test files. No other modifications to the suite.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build && pnpm exec vitest run tests/build/venue-cleanup.test.ts</automated>
  </verify>
  <done>
    - New assertion exists and passes (`venue.prev` → zero matches in `src/i18n/ui.ts`).
    - TODO comment referencing 18-02 is gone.
    - `pnpm build` succeeds green (SC5 re-verified after i18n sweep).
    - Roadmap SC3 is satisfied and test-enforced.
    - Commit (separate git command, single commit covering Tasks 1-2): `refactor(18-02): remove venue.prev.* i18n keys (FR + EN)`.
  </done>
</task>

</tasks>

<verification>
- Roadmap SC3: `grep -rn "venue\.prev" src/` returns zero hits → asserted in both Task 1 verify and the extended test in Task 2.
- Roadmap SC5: `pnpm build` succeeds with no broken `t()` calls → asserted in Task 2 verify. Since 18-01 removed every `t("venue.prev.*")` call site, deleting the keys in this plan introduces zero broken lookups.
- Roadmap SC1, SC2 already closed by 18-01; SC4 is closed by 18-VERIFICATION.md per D-01.
</verification>

<success_criteria>
1. 16 `venue.prev.*` entries gone from `src/i18n/ui.ts` (8 FR + 8 EN).
2. `tests/build/venue-cleanup.test.ts` now asserts the sweep and passes.
3. `pnpm build` is green; all existing Phase 16/17/18 tests remain green.
4. One atomic commit lands on `main`: `refactor(18-02): remove venue.prev.* i18n keys (FR + EN)`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-venue-page-cleanup/18-02-SUMMARY.md`. Then Phase 18 is ready for a VERIFICATION.md pass that folds in the anchor-audit finding (D-01) and signs off SC1-SC5.
</output>
