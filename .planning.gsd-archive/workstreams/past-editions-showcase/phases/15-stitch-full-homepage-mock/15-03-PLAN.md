---
phase: 15-stitch-full-homepage-mock
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified: []
autonomous: false
requirements: [EDIT-06]
tags: [design, stitch, testimonials, marquee]
must_haves:
  truths:
    - "A Stitch detail screen shows the testimonials marquee strip in its default state (right-to-left drift, single row, ~40s loop per D-07)"
    - "Card style is quote-led: large quote text dominates with small Name — Role attribution, no avatars (D-08)"
    - "Hover/focus pause affordance is visually indicated without chrome (D-09) — no pause/play button"
    - "User explicitly approves the marquee layout"
  artifacts:
    - path: ".planning/phases/15-stitch-full-homepage-mock/15-DISCUSSION-LOG.md"
      provides: "Record of testimonials marquee screen ID + approval"
      contains: "15-03"
  key_links:
    - from: "Marquee screen"
      to: "Phase 20 Animated Testimonials Strip implementation"
      via: "Visual contract: direction, card style, pause affordance locked"
      pattern: "marquee"
---

<objective>
Generate one Stitch detail screen that locks the testimonials marquee layout: right-to-left drift direction (D-07), quote-led card style with no avatars (D-08), and hover/focus pause-only affordance with no visible chrome (D-09). This is the visual contract that Phase 20 (Animated Testimonials Strip) will implement.

Purpose: ROADMAP success criterion 4 — "Testimonials strip layout (marquee direction, card style, pause affordance) is locked". A dedicated detail screen (separate from the full-homepage mock from 15-01) lets the user inspect card padding/radius/shadow choices and the default/hover states without scrolling the whole page.

Output: One approved Stitch screen showing the marquee strip at rest and — via annotations or a second frame — an indicative hover state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-stitch-full-homepage-mock/15-CONTEXT.md
@.planning/phases/15-stitch-full-homepage-mock/15-01-SUMMARY.md
@DESIGN.md
@.claude/projects/-home-smana-Sources-cndfrance-website/memory/reference_stitch.md
@.claude/projects/-home-smana-Sources-cndfrance-website/memory/feedback_stitch_ds_tokens.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate testimonials marquee detail screen</name>
  <read_first>
    - .planning/phases/15-stitch-full-homepage-mock/15-CONTEXT.md (D-07 direction + ~40s loop, D-08 quote-led card no avatars, D-09 hover/focus pause only)
    - .claude/projects/-home-smana-Sources-cndfrance-website/memory/reference_stitch.md
    - DESIGN.md (card tokens, radius tokens, shadow tokens — Card background, Border 1px, radius-lg default, shadow-md for elevation)
  </read_first>
  <files></files>
  <action>
    Call `mcp__stitch__generate_screen_from_text`:
    - projectId: `14858529831105057917`
    - designSystemId: `3926684191749761173`
    - Device: desktop 1280w AND mobile 390x844

    Prompt body (locked values verbatim):

    """
    Detail screen: Testimonials marquee strip, isolated view. Dark theme, DS tokens only (Background, Card, Foreground, Muted Foreground, Border, Accent — no raw hex).

    Layout:
    - Section heading "Ce qu'en disent les participants" (placeholder) at type-3xl, DM Sans semibold, -0.01em tracking.
    - A single horizontal row of quote cards that visually extends beyond the viewport on both left and right edges (indicate continuation with soft fade-out gradients using the Background token at both edges, 40-60px wide).

    Direction (D-07): cards drift RIGHT-TO-LEFT at a slow pace equivalent to a ~40-second loop (indicate direction with a subtle arrow annotation outside the frame and show motion blur-style trailing at the card edges OR annotate the direction explicitly in the mock legend). Single row only — no second opposite-direction row.

    Card style (D-08 — quote-led, NO avatars):
    - Background: Card token
    - Border: 1px Border token
    - Border radius: radius-lg (8px)
    - Padding: spacing-8 (32px) vertical and horizontal
    - Width: ~320-360px desktop, ~280px mobile
    - Content top to bottom: a large opening quote glyph (" ") in Muted Foreground at type-4xl opacity 40%, then the quote text at type-xl (20px) Foreground weight 500 (2-4 lines of placeholder FR text like "Une expérience inoubliable, des sessions techniques de grande qualité."), then at the bottom a small attribution "Prénom N. — Rôle" at type-sm Muted Foreground. NO avatar, NO photo — quotes only.
    - Shadow: shadow-md on default state.
    - Gap between cards: spacing-6 (24px).

    Pause affordance (D-09): NO visible pause/play button anywhere in the section. Instead, annotate or show a second frame where one card is in a subtle hover state — border transitions to Primary Blue at 50% opacity, shadow intensifies to shadow-lg — and the entire marquee is paused. This communicates "hover/focus pauses" without adding chrome. The annotation legend should note: "Marquee pauses on :hover and :focus-within. No pause button. Reduced-motion handled globally."

    Include 5-6 placeholder quote cards in the frame so the drift behavior is visually clear.

    Render at desktop 1280w AND mobile 390x844 so card sizing is visible at both breakpoints.
    """

    Capture screen ID. Share preview with user.
  </action>
  <verify>
    <automated>`mcp__stitch__get_screen` with the captured screenId confirms designSystemId `3926684191749761173` and returns a screen description referencing "testimonials" or "marquee".</automated>
  </verify>
  <acceptance_criteria>
    - Marquee detail screen exists in project 14858529831105057917
    - Prompt included verbatim D-07 (right-to-left, ~40s loop), D-08 (quote-led, no avatars, attribution format), D-09 (hover/focus pause only, no button) language
    - Both desktop and mobile renders produced
  </acceptance_criteria>
  <done>Marquee detail screen ID captured, preview shared with user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approves marquee layout (direction, card, pause affordance)</name>
  <read_first>
    - .planning/phases/15-stitch-full-homepage-mock/15-CONTEXT.md (acceptance bar: D-07, D-08, D-09)
  </read_first>
  <files></files>
  <what-built>Testimonials marquee detail screen with default state + hover-pause annotation.</what-built>
  <how-to-verify>
    1. Open the Stitch preview URL.
    2. Direction (D-07): confirm right-to-left drift is visually indicated (arrow, trailing, or legend). Single row only.
    3. Card style (D-08): confirm each card is quote-led — large quote text dominates, small "Name — Role" attribution at the bottom, NO avatars, NO photos.
    4. Card tokens: confirm Card token background, Border 1px, radius-lg, shadow-md on default + shadow-lg on hover annotation.
    5. Pause affordance (D-09): confirm there is NO visible pause/play button anywhere. The hover/focus-pause behavior is communicated via annotation/second frame only.
    6. Confirm DS tokens only — no foreign hex.
    7. Confirm the fade-out gradients at the left/right edges use the Background token (not a new color).
    8. Confirm mobile variant: cards are ~280px wide, still single-row, still drift right-to-left.
    9. If any check fails, use `mcp__stitch__edit_screens` to revise in Stitch FIRST, re-share, repeat.
    10. Log each revision + final approval in `15-DISCUSSION-LOG.md`.
  </how-to-verify>
  <resume-signal>User types "approved" (or equivalent) referencing the marquee screen. Record approved screen ID in 15-DISCUSSION-LOG.md.</resume-signal>
  <acceptance_criteria>
    - 15-DISCUSSION-LOG.md contains "15-03 approved" entry with the marquee screen ID
    - User approval explicit in chat
    - No code files modified
  </acceptance_criteria>
  <action>This is a human-verification checkpoint. See <what-built>, <how-to-verify>, and <resume-signal> above. Executor must pause and wait for the user's approval signal; no autonomous action is taken beyond presenting the preview and (if needed) invoking Stitch edit tools in response to user feedback.</action>
  <verify>
    <automated>User approval explicit in chat; `.planning/phases/15-stitch-full-homepage-mock/15-DISCUSSION-LOG.md` updated with approval entry.</automated>
  </verify>
  <done>- 15-DISCUSSION-LOG.md contains "15-03 approved" entry with the marquee screen ID
    - User approval explicit in chat
    - No code files modified</done>
</task>

</tasks>

<verification>
- Marquee detail screen exists in Stitch project 14858529831105057917 with DS 3926684191749761173
- 15-DISCUSSION-LOG.md records the approval
- D-07, D-08, D-09 visually locked (direction, card style, pause affordance)
</verification>

<success_criteria>
- Marquee layout locked: direction right-to-left, quote-led cards no avatars, hover/focus pause only (ROADMAP success criterion 4)
- User approval explicit
- Phase 20 can implement directly from this contract
</success_criteria>

<output>
After completion, create `.planning/phases/15-stitch-full-homepage-mock/15-03-SUMMARY.md` with the approved marquee screen ID, iteration count, and any deviations from D-07/D-08/D-09 the user amended (should be zero).
</output>
