---
phase: 26
plan: 03
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - src/components/past-editions/PastEditionSection.astro
  - src/components/past-editions/PastEditionMinimal.astro
  - src/components/testimonials/TestimonialsStrip.astro
autonomous: true
requirements:
  - LAYO-01
must_haves:
  truths:
    - "src/components/past-editions/PastEditionSection.astro is deleted (no longer exists)"
    - "src/components/past-editions/PastEditionMinimal.astro is deleted (no longer exists)"
    - "src/components/testimonials/TestimonialsStrip.astro is deleted (no longer exists)"
    - "BEFORE deletion: a re-verification grep across src/ confirms zero remaining importers (other than the homepage files Plan 26-01 already cleaned). If any unexpected importer surfaces, the plan ABORTS without deleting (safety gate)"
    - "AFTER deletion: bun run astro check exits with at most 5 errors (down from the 9-error post-26-01 baseline; the 4 TestimonialsStrip template-literal type errors are gone with the file)"
    - "AFTER deletion: bun run build still produces 156 pages — confirms no remaining importer was missed by the grep gate"
    - "src/components/testimonials/ directory is empty after the delete (or removed if Astro tolerates empty dirs); no other file lived in there"
  artifacts:
    - path: "(none — this plan only deletes files)"
      provides: "Post-deletion file system state with three orphan files removed"
  key_links:
    - from: "Plan 26-01 commit (homepage rewrite)"
      to: "Plan 26-03 (this plan)"
      via: "depends_on chain — homepage MUST be rewired before delete is safe"
      pattern: "depends_on:\\n\\s*-\\s*\"26-01\""
    - from: "src/ tree post-deletion"
      to: "PastEditionSection / PastEditionMinimal / TestimonialsStrip"
      via: "grep -rl returns ZERO matches across src/"
      pattern: "grep -rl 'PastEditionSection\\|PastEditionMinimal\\|TestimonialsStrip' src/ | wc -l == 0"
---

<objective>
Delete the three v1.1-era homepage components — `PastEditionSection.astro`, `PastEditionMinimal.astro`, `TestimonialsStrip.astro` — that Plan 26-01 superseded with the new Edition2026Combined / Edition2023Link / SponsorsPlatinumStrip mounts. The deletion is gated by a defensive re-verification grep: if ANY src/ file still imports any of the three, the plan aborts without deleting (the grep gate is the safety net against an importer that pre-26-01 mapping missed).

Purpose: ROADMAP success criterion #4 explicitly says "no orphaned component imports" — but it also implies no orphaned component FILES, otherwise the codebase carries dead Astro / Tailwind weight that drifts further from reality with every refactor. The three components were verified-only-imported-by-the-homepages during planning (`grep -rl` confirmed zero other consumers as of 2026-04-18); this plan re-runs that grep AS A GATE before each delete so a late-arriving importer (e.g. someone added a /test page that mounted TestimonialsStrip) cannot silently break the build. Incidentally the deletion fixes 4 of the remaining post-26-01 baseline astro-check errors (the `TestimonialsStrip.astro` template-literal type errors disappear with the file), bringing the total astro-check baseline from 9 -> 5 errors.

Output: Three files removed from the working tree (and from git via `git rm`); `bun run astro check` baseline drops by 4 errors; `bun run build` continues to produce 156 pages; one atomic commit with the three deletes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-homepage-wiring/26-01-PLAN.md
@src/components/past-editions/PastEditionSection.astro
@src/components/past-editions/PastEditionMinimal.astro
@src/components/testimonials/TestimonialsStrip.astro

<deletion_safety_gate>
Per Auto Mode rule #5 ("Do not take overly destructive actions"), file deletion requires
explicit verification that the files are truly orphan. The gate is a SINGLE grep command
that MUST return zero matches BEFORE the delete tasks run:

```bash
grep -rl 'PastEditionSection\|PastEditionMinimal\|TestimonialsStrip' src/
```

Expected output: empty (zero filenames). The only files that historically referenced
these three components are `src/pages/index.astro` and `src/pages/en/index.astro`,
both rewritten by Plan 26-01 to no longer import them. If any other file appears in
the grep output, the plan ABORTS before any delete and surfaces the unexpected
importer to the user — that file is not actually orphan and Phase 26's scope was wrong.

The grep MUST be re-run AT EXECUTION TIME (not at planning time), because the source
of truth is the working tree state when the delete commands fire — not the planner's
2026-04-18 snapshot.
</deletion_safety_gate>

<pre_existing_baseline>
Per STATE.md "Pending Todos", the pre-Phase-26 astro-check baseline was 11 errors:
- src/content.config.ts (3×) — Zod 13 LoaderConstraint regression
- src/components/past-editions/Edition2023PhotoGrid.astro (2×) — implicit-any
- src/components/testimonials/TestimonialsStrip.astro (4×) — template-literal keys ← DELETED HERE
- src/pages/index.astro (1×) — orphan editions.2026.gallery_cta ← FIXED BY 26-01
- src/pages/en/index.astro (1×) — same ← FIXED BY 26-01

After Plan 26-01 lands: 9 errors (the 2 orphan-key errors gone)
After Plan 26-03 lands (this plan): 5 errors (the 4 TestimonialsStrip errors gone)
After Plan 26-03: ONLY 5 errors remain — all in `content.config.ts` (3) +
`Edition2023PhotoGrid.astro` (2). These are NOT in Phase 26's scope — see
STATE.md "Pending Todos" for the per-file deferral note.
</pre_existing_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify deletion safety gate (abort if any unexpected importer exists)</name>
  <files>(none — read-only verification)</files>
  <action>
Execute the re-verification grep across the entire `src/` tree. The command MUST return ZERO matches. If it returns ANY filename, ABORT this plan immediately, surface the unexpected importer(s) to the user, and do NOT proceed to Task 2.

```bash
grep -rl 'PastEditionSection\|PastEditionMinimal\|TestimonialsStrip' src/ 2>/dev/null
```

This is a reachability check — at planning time (2026-04-18) only the two homepage files referenced any of the three components, and Plan 26-01 deletes those references. If a new importer materialises between planning and execution (e.g. a feature branch added a /demo page mounting TestimonialsStrip and merged after 26-01 landed but before 26-03 fires), the grep will catch it. This is the safety net for the destructive Task 2.

Also confirm Plan 26-01 actually landed (the `git log --oneline` should show the homepage rewrite commit; otherwise the dependency wave gating in the orchestrator was bypassed and this plan is firing too early):

```bash
git log --oneline -5 src/pages/index.astro src/pages/en/index.astro | head -5
```

Expected: the most recent commit on these files mentions Phase 26 / homepage wiring / atomic rewrite.

DO NOT:
- Skip this gate "because the planning doc said it's safe". The planning doc is a 2026-04-18 snapshot; the gate confirms the present-tense state.
- Proceed to Task 2 if the grep returns even one filename.
- Treat a `.planning/` doc match as a real importer — restrict the grep to `src/` (the command above already does this).
  </action>
  <verify>
<automated>
# Gate 1: zero unexpected importers in src/
MATCHES=$(grep -rl 'PastEditionSection\|PastEditionMinimal\|TestimonialsStrip' src/ 2>/dev/null | wc -l)
test "$MATCHES" = "0" && \
  echo "DELETION SAFETY GATE PASS — zero importers in src/" || \
  { echo "ABORT: $MATCHES unexpected importer(s) found:"; grep -rl 'PastEditionSection\|PastEditionMinimal\|TestimonialsStrip' src/ 2>/dev/null; exit 1; }

# Gate 2: Plan 26-01 commit exists
git log --oneline -10 src/pages/index.astro | head -3
git log --oneline -10 src/pages/en/index.astro | head -3
# Visually confirm the most recent commit is the 26-01 homepage rewrite
</automated>
  </verify>
  <done>
- `grep -rl 'PastEditionSection\|PastEditionMinimal\|TestimonialsStrip' src/` returns ZERO matches
- Plan 26-01's commit is visible in `git log` for both homepage files (proves the dependency landed before this plan fires)
- If gate fails: plan ABORTS, no delete attempted, unexpected importer(s) surfaced to user
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the three orphan component files (atomic commit)</name>
  <files>
src/components/past-editions/PastEditionSection.astro
src/components/past-editions/PastEditionMinimal.astro
src/components/testimonials/TestimonialsStrip.astro
  </files>
  <action>
Only proceed if Task 1's safety gate passed. Delete the three files via `git rm` so the deletion is staged in a single atomic commit (matches the Plan 26-01 atomic-commit discipline):

```bash
git rm src/components/past-editions/PastEditionSection.astro
git rm src/components/past-editions/PastEditionMinimal.astro
git rm src/components/testimonials/TestimonialsStrip.astro
```

Then verify the working tree state:

```bash
ls src/components/past-editions/ | grep -E 'PastEditionSection|PastEditionMinimal' | wc -l   # expect 0
ls src/components/testimonials/ 2>/dev/null | wc -l                                          # expect 0
```

If `src/components/testimonials/` is now empty, leave the directory in place — Astro tolerates empty directories and `git rm` does not auto-remove the parent dir. The empty directory is harmless and may be cleaned up in a future maintenance pass; deleting it here is OUT OF SCOPE for this plan (the plan's `files_modified` frontmatter intentionally lists only the three .astro files, not the parent dir).

Verify the build remains green and astro-check baseline drops:

```bash
bun run astro check 2>&1 | tail -5
bun run build 2>&1 | tail -5
```

Expected:
- `bun run astro check` total error count is at most 5 (down from the 9-error post-26-01 baseline; the 4 TestimonialsStrip errors are gone)
- `bun run build` exits 0 and ships 156 pages

DO NOT:
- Use `rm` instead of `git rm`. The deletion must be tracked in git so the next commit captures it atomically.
- Delete the `src/components/testimonials/` directory itself in this plan. Empty-directory cleanup is out of scope; if it bothers anyone it's a one-line follow-up.
- Delete any other files. Specifically, do NOT delete:
  - `src/components/past-editions/PastEditionPhotoMarquee.astro` (used by /2023 page)
  - `src/components/past-editions/Edition2023PhotoGrid.astro` (used by /2023 page)
  - `src/lib/testimonials-data.ts` (consumed by the new Edition2026Combined.astro at line 22)
  - Any other PastEdition* / Testimonial* file that didn't appear in this plan's `files_modified` frontmatter
- Modify any remaining file. This plan is delete-only — no .astro / .ts edits.
  </action>
  <verify>
<automated>
# All three files gone from the working tree:
test ! -f src/components/past-editions/PastEditionSection.astro && \
  test ! -f src/components/past-editions/PastEditionMinimal.astro && \
  test ! -f src/components/testimonials/TestimonialsStrip.astro && \
  echo "THREE FILES DELETED PASS"

# Git status shows the three deletes staged:
test "$(git status --porcelain | grep -c '^D ')" -ge "3" && \
  echo "DELETIONS STAGED IN GIT PASS"
git status --porcelain | grep -E 'PastEditionSection|PastEditionMinimal|TestimonialsStrip'

# Re-confirm zero importers in src/ (post-delete sanity check):
test "$(grep -rl 'PastEditionSection\|PastEditionMinimal\|TestimonialsStrip' src/ 2>/dev/null | wc -l)" = "0" && \
  echo "POST-DELETE GREP CLEAN PASS"

# Files NOT in scope are untouched (anti-overreach gate):
test -f src/lib/testimonials-data.ts && \
  test -f src/components/past-editions/Edition2023PhotoGrid.astro && \
  test -f src/components/past-editions/PastEditionPhotoMarquee.astro && \
  echo "NON-SCOPE FILES PRESERVED PASS"

# Astro check baseline drops by 4 (TestimonialsStrip errors gone):
bun run astro check 2>&1 | tail -5
test "$(bun run astro check 2>&1 | grep -c 'TestimonialsStrip')" = "0" && \
  echo "TESTIMONIALSSTRIP ERRORS GONE PASS"

# Build still ships 156 pages:
bun run build 2>&1 | tail -5
test "$(bun run build 2>&1 | grep -cE '^.*156 pages|built in')" -ge "1" && \
  echo "BUILD STILL GREEN PASS"
</automated>
  </verify>
  <done>
- All three files (`PastEditionSection.astro`, `PastEditionMinimal.astro`, `TestimonialsStrip.astro`) are absent from the working tree
- `git status --porcelain` shows the three deletions staged with `D` markers
- Post-delete `grep -rl` across src/ returns zero matches for any of the three component names (confirms no late-arriving importer)
- `src/lib/testimonials-data.ts`, `Edition2023PhotoGrid.astro`, and `PastEditionPhotoMarquee.astro` are still present (anti-overreach gate)
- `bun run astro check` total error count is at most 5 (down from 9 post-26-01 baseline)
- Zero remaining `TestimonialsStrip`-flavoured errors in astro-check output
- `bun run build` exits 0, ships 156 pages
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree -> git index | `git rm` is the trust gate; staging the deletes in git allows the next commit to capture them atomically. Bypass via raw `rm` would leave a dirty index that future commits could miss. |
| Pre-delete -> post-delete | The grep gate (Task 1) is the trust gate ensuring no live importer would be broken by the delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11 | Tampering | Working tree | mitigate | The Task 1 grep gate aborts the delete if any unexpected `src/` importer is found. The Task 2 anti-overreach gate (`test -f` for non-scope files) confirms only the three target files were affected. |
| T-26-12 | Denial of Service | Build | mitigate | `bun run build` post-delete confirms 156 pages still build. If a missed importer existed, the build would fail and the destructive change would be caught before the commit. |
| T-26-13 | Repudiation | git history | mitigate | `git rm` (vs raw `rm`) preserves the deletion in git history; the SUMMARY.md captures the file paths and the safety-gate output for posterity. |
| T-26-14 | Elevation of Privilege | n/a | accept | No privilege boundary in a delete-only plan. |
</threat_model>

<verification>
**Whole-plan verification:**

1. Pre-delete safety gate (Task 1): zero `src/` importers, Plan 26-01 commit landed.
2. All three files removed (Task 2 `test ! -f` checks).
3. Git tracks the deletions (`git status --porcelain` shows `D` markers).
4. Post-delete sanity grep: still zero importers (proves Task 2 didn't accidentally leave a half-state).
5. Anti-overreach: `testimonials-data.ts`, `Edition2023PhotoGrid.astro`, `PastEditionPhotoMarquee.astro` are still present.
6. Astro-check baseline drops by 4 (TestimonialsStrip template-literal errors gone with the file).
7. Build still produces 156 pages.

**End-state baseline summary:**
- Pre-Phase-26: 11 astro-check errors
- Post-26-01: 9 errors (2 orphan i18n key errors fixed)
- Post-26-03 (this plan): 5 errors (4 TestimonialsStrip type errors fixed)
- Remaining 5 errors are NOT in Phase 26's scope — `content.config.ts` Zod regression (3) + `Edition2023PhotoGrid.astro` implicit-any (2). These are documented in STATE.md "Pending Todos" and slated for a separate baseline-cleanup phase (CLO-XX equivalent or v1.3).
</verification>

<success_criteria>
- ROADMAP success criterion #4 (build completes with zero errors and **no orphaned component imports**) **fully satisfied** at the file-system level (Plan 26-01 already removed the imports; this plan removes the orphan files themselves)
- LAYO-01 requirement (the "and accessibility check" half) **structurally satisfied** at the dead-code-elimination layer; the visual a11y check is part of the v1.2 milestone-end UAT
- Astro-check baseline reduced by 4 errors (11 -> 9 -> 5 across Plans 26-01 and 26-03)
- Single atomic commit captures all three deletes
- Anti-overreach: `src/lib/testimonials-data.ts` (consumed by Edition2026Combined) survives; the testimonials data is moved INTO the new component, the data module stays
- Plan 26-02 (favicon) is independent and may have already shipped in wave 1; this plan does not depend on it
</success_criteria>

<output>
After completion, create `.planning/phases/26-homepage-wiring/26-03-SUMMARY.md` per the standard summary template, including:
- Output of the Task 1 safety gate (`grep -rl ...` returned zero — listed in the SUMMARY for audit)
- The three deleted files with their pre-delete line counts (for posterity — `wc -l` snapshot pulled before the delete)
- Astro-check baseline delta: 11 -> 9 (after 26-01) -> 5 (after this plan); per-file breakdown of the 4 errors that disappeared (all `TestimonialsStrip.astro` template-literal type errors)
- Anti-overreach confirmation: `src/lib/testimonials-data.ts` + `Edition2023PhotoGrid.astro` + `PastEditionPhotoMarquee.astro` are still present (`test -f` results)
- A "Phase 26 Complete" note: this plan is the last plan of the last phase of v1.2; once shipped, the milestone is feature-complete and ready for the v1.2 milestone-audit pass
</output>
